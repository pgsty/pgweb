--- a/data/featurematrix_zh.yaml
+++ b/data/featurematrix_zh.yaml
@@ -1,6 +1,6 @@
 groups:
   Backend: 后端
-  Data Types, Functions, & Operators: 数据类型、函数和运算符
+  Data Types, Functions, & Operators: 数据类型、函数和操作符
   Indexing & Constraints: 索引和约束
   SQL: SQL
   Data Definition Language (DDL): 数据定义语言 (DDL)
@@ -105,7 +105,7 @@
     description: 数组元素可以包含 NULL
   Phrase search:
     name: 短语搜索
-    description: 全文搜索支持短语和词语邻近度搜索。
+    description: 全文检索支持短语和词语邻近度搜索。
   Range types:
     name: 范围类型
     description: https://www.postgresql.org/docs/9.2/static/rangetypes.html
@@ -124,15 +124,15 @@
     name: 块范围 (BRIN) 索引
     description: https://www.postgresql.org/docs/current/static/brin-intro.html
   B-tree bottom-up index deletion:
-    name: B-tree 自底向上的索引删除
+    name: B-树 自底向上的索引删除
   B-tree deduplication:
-    name: B-tree 去重
+    name: B-树 去重
     description: https://www.postgresql.org/docs/13/btree-implementation.html#BTREE-DEDUPLICATION
   Concurrent GiST indexes:
     name: 并发 GiST 索引
     description: 用于并发修改 GiST 索引的高级锁定机制
   Covering Indexes for B-trees (INCLUDE):
-    name: B-tree 的覆盖索引 (INCLUDE)
+    name: B-树 的覆盖索引 (INCLUDE)
     description: '允许将额外的列作为"非键"列包含在索引中，如果满足条件，可以在仅索引扫描中返回这些列的数据。
 
 
@@ -147,10 +147,10 @@
     name: 可延迟唯一约束
     description: 允许将唯一约束检查延迟到事务结束时执行
   Exclusion constraints:
-    name: 排除约束
-    description: 将唯一性概念推广为支持任何可索引的交换运算符，而不仅限于等值运算符
+    name: 排他约束
+    description: 将唯一性概念推广为支持任何可索引的交换操作符，而不仅限于等值操作符
   GIN (Generalized Inverted Index) Indexes:
-    name: GIN (广义反向索引) 索引
+    name: GIN（广义倒排索引）
     description: https://www.postgresql.org/docs/current/static/gin.html
   GIN indexes partial match:
     name: GIN 索引部分匹配
@@ -181,22 +181,22 @@
     description: 为 contrib/pg_trgm 和 contrib/btree_gist 提供最近邻支持
   K-nearest neighbor SP-GiST Support:
     name: K-最近邻 SP-GiST 支持
-    description: 新增对 SP-GiST 索引上 K-最近邻 (K-NN) 搜索的支持，前提是定义了距离运算符 `<->`。
+    description: 新增对 SP-GiST 索引上 K-最近邻 (K-NN) 搜索的支持，前提是定义了距离操作符 `<->`。
   Non-blocking CREATE INDEX:
     name: 非阻塞 CREATE INDEX
     description: 在线创建索引而不锁定表
   Parallel B-tree index scans:
-    name: 并行 B-tree 索引扫描
-    description: B-tree 索引页面可以由多个并行工作进程进行搜索。
+    name: 并行 B-树 索引扫描
+    description: B-树 索引页面可以由多个并行工作进程进行搜索。
   Parallelized CREATE INDEX for BRIN indexes:
     name: BRIN 索引的并行 CREATE INDEX
   Parallelized CREATE INDEX for B-tree indexes:
-    name: B-tree 索引的并行 CREATE INDEX
+    name: B-树 索引的并行 CREATE INDEX
   Parallelized CREATE INDEX for GIN indexes:
     name: GIN 索引的并行 CREATE INDEX
   Skip scan on multicolumn B-tree indexes:
-    name: 多列 B-tree 索引上的跳跃扫描
-    description: 对于在一个或多个前导索引列上缺少 `=` 条件的查询，多列 B-tree 索引现在可以使用跳跃扫描来改善执行时间。
+    name: 多列 B-树 索引上的跳跃扫描
+    description: 对于在一个或多个前导索引列上缺少 `=` 条件的查询，多列 B-树 索引现在可以使用跳跃扫描来改善执行时间。
   Space-Partitioned GiST (SP-GiST) Indexes:
     name: 空间分区 GiST (SP-GiST) 索引
     description: https://www.postgresql.org/docs/current/static/spgist.html
@@ -208,7 +208,7 @@
     description: 默认情况下 `NULL` 彼此不同；在唯一索引或唯一约束上指定 `NULLS NOT DISTINCT` 后，`NULL` 会被视为不再互异。
   WAL support for hash indexes:
     name: 哈希索引的 WAL 支持
-    description: 哈希索引具有预写日志支持，这使其既具备崩溃安全性，又可进行复制。
+    description: 哈希索引具有预写式日志支持，这使其既具备崩溃安全性，又可进行复制。
   ANY_VALUE aggregate:
     name: ANY_VALUE 聚合函数
   FETCH FIRST .. WITH TIES:
@@ -312,7 +312,7 @@
     description: '`ALTER TABLE` 现在可以为现有表指定不同的表访问方法。'
   ALTER TABLE ... SET LOGGED / UNLOGGED:
     name: ALTER TABLE ... SET LOGGED / UNLOGGED
-    description: 允许表在已记录日志和未记录日志状态之间切换。
+    description: 允许表在记录 WAL 和不记录 WAL 两种状态之间切换。
   Changing column types (ALTER TABLE .. ALTER COLUMN TYPE):
     name: 更改列类型 (ALTER TABLE .. ALTER COLUMN TYPE)
     description: 允许在线将列的数据类型更改为另一种类型
@@ -331,7 +331,7 @@
     name: REINDEX CONCURRENTLY
     description: https://www.postgresql.org/docs/current/sql-reindex.html#SQL-REINDEX-CONCURRENTLY
   Stored Generated Columns:
-    name: 存储生成的列
+    name: 存储生成列
     description: https://www.postgresql.org/docs/current/ddl-generated-columns.html
   Temporal constraints:
     name: 时态约束
@@ -363,8 +363,8 @@
     name: 基础备份节流
     description: pg_basebackup 现在支持使用 -r <rate> 或 --max-rate <rate> 对备份进行节流，允许控制基础备份的数据传输速率。
   CREATE STATISTICS - most-common values (MCV) statistics:
-    name: CREATE STATISTICS - 最常用值 (MCV) 统计信息
-    description: CREATE STATISTICS 可以收集最常用值的统计信息，从而改善对包含非均匀分布的列的优化。
+    name: CREATE STATISTICS - 高频值 (MCV) 统计信息
+    description: CREATE STATISTICS 可以收集高频值的统计信息，从而改善对包含非均匀分布的列的优化。
   CREATE STATISTICS - multicolumn:
     name: CREATE STATISTICS - 多列
     description: 'CREATE STATISTICS 提供了收集多列统计信息的能力，优化器可以使用这些信息生成更好的查询计划。
@@ -387,7 +387,7 @@
     name: 已冻结页面映射
     description: 可见性映射中的冻结页面数据，防止在回卷期间重新清理已冻结的页面。
   Full Text Search:
-    name: 全文搜索
+    name: 全文检索
     description: https://www.postgresql.org/docs/current/static/textsearch.html
   Hash aggregation can use disk:
     name: 基于磁盘的哈希聚合
@@ -407,7 +407,7 @@
     name: FULL OUTER JOIN、LEFT OUTER JOIN 和 RIGHT OUTER JOIN 的哈希支持
     description: 允许 FULL OUTER JOIN 以哈希连接方式实现，并允许对 LEFT OUTER JOIN 或 RIGHT OUTER JOIN 的任一侧进行哈希
   Heap Only Tuples (HOT):
-    name: 仅堆元组 (HOT)
+    name: 堆内元组 (HOT)
     description: 针对频繁更新表的优化
   Improved performance for sorts exceeding working memory:
     name: 超出工作内存的排序性能提升
@@ -463,8 +463,8 @@
     name: 并行 JOIN、聚合
     description: 能够使用多个并行进程执行某些类型的 JOIN 以及聚合操作。
   Parallel merge joins:
-    name: 并行合并连接
-    description: 合并连接可以并行执行。
+    name: 并行归并连接
+    description: 归并连接可以并行执行。
   Parallel query:
     name: 并行查询
     description: 能够在并行进程中执行多个查询节点。
@@ -497,7 +497,7 @@
   SELECT ... FOR UPDATE/SHARE NOWAIT:
     name: SELECT ... FOR UPDATE/SHARE NOWAIT
   Set costs specific to TABLESPACEs:
-    name: 为 TABLESPACE 设置特定成本
+    name: 为表空间设置特定代价
     description: 允许在每个表空间级别设置 seq_page_cost 和 random_page_cost
   Shared row level locking:
     name: 共享行级锁定
@@ -521,20 +521,20 @@
     name: 表空间
     description: https://www.postgresql.org/docs/current/static/sql-createtablespace.html
   Unlogged tables:
-    name: 未记录表
-    description: 此类表比普通表提供更好的更新性能，但不具有崩溃安全性：在服务器崩溃时其内容会自动清除。其内容也不会传播到复制从属服务器。
+    name: 不记录 WAL 的表（UNLOGGED）
+    description: 此类表比普通表提供更好的更新性能，但不具有崩溃安全性：在服务器崩溃时其内容会自动清除。其内容也不会传播到从库。
   WAL Buffer auto-tuning:
     name: WAL 缓冲区自动调优
     description: wal_buffers 现在默认根据 shared_buffers 的大小自动调优
   Improved set of JSON functions and operators:
-    name: 改进的 JSON 函数和运算符集
-    description: 新增了从 JSON 数据字符串中提取值的运算符和函数，JSON 数据字符串现在可以转换为记录，并新增了将值、记录和 hstore 数据转换为 JSON 的函数。
+    name: 改进的 JSON 函数和操作符集
+    description: 新增了从 JSON 数据字符串中提取值的操作符和函数，JSON 数据字符串现在可以转换为记录，并新增了将值、记录和 hstore 数据转换为 JSON 的函数。
   JSONB data type:
     name: JSONB 数据类型
     description: https://www.postgresql.org/docs/9.4/static/datatype-json.html
   JSONB-modifying operators and functions:
-    name: JSONB 修改运算符和函数
-    description: 新增了允许删除、修改或向 JSONB 值中插入值的运算符和函数，包括在特定路径位置进行操作。
+    name: JSONB 修改操作符和函数
+    description: 新增了允许删除、修改或向 JSONB 值中插入值的操作符和函数，包括在特定路径位置进行操作。
   JSONB Subscripting:
     name: JSONB 下标访问
   JSON data type:
@@ -560,7 +560,7 @@
   SQL/JSON query functions:
     name: SQL/JSON 查询函数
   Accelerated partition pruning:
-    name: 加速分区裁剪
+    name: 加速分区剪枝
     description: 当仅需要访问有限的分区子集时，针对具有数千个分区的表的查询性能改进。
   Declarative table partitioning:
     name: 声明式表分区
@@ -569,15 +569,15 @@
     name: 默认分区
     description: 默认分区存储不匹配任何其他分区键的数据，即它是一个“兜底”分区。
   Foreign Key references for partitioned tables:
-    name: 分区表的外部键引用
-    description: 外部键可以引用分区表。
+    name: 分区表的外键引用
+    description: 外键可以引用分区表。
   Foreign table inheritance:
     name: 外部表继承
     description: 外部表现在可以从其他外部表或其他常规表继承。反过来也适用。
   Partitioning by a hash key:
     name: 按哈希键分区
   Partition pruning during query execution:
-    name: 查询执行期间的分区裁剪
+    name: 查询执行期间的分区剪枝
     description: 对分区表的查询可以在查询执行期间消除分区，而不仅仅是在查询计划时。这使 PostgreSQL 能够利用查询执行期间可用的信息进一步减少需要使用的分区数量，从而提高查询性能。
   Support for PRIMARY KEY, FOREIGN KEY, indexes, and triggers on partitioned tables:
     name: 分区表的 PRIMARY KEY、FOREIGN KEY、索引和触发器支持
@@ -586,7 +586,7 @@
     description: https://www.postgresql.org/docs/current/static/ddl-partitioning.html
   UPDATE on a partition key:
     name: 分区键上的 UPDATE
-    description: 当分区键上的行被更新时，该行会被移动到相应的分区。
+    description: 当行的分区键被更新时，该行会被移动到相应的分区。
   Materialized Views:
     name: 物化视图
     description: https://www.postgresql.org/docs/current/static/rules-materializedviews.html
@@ -610,9 +610,9 @@
     description: '`ALTER SUBSCRIPTION ... SKIP` 允许订阅端跳过某个事务的重放。'
   Cascading streaming replication:
     name: 级联流复制
-    description: 备用服务器现在可以向其他备用服务器进行流式传输，减少主服务器的复制负载。
+    description: 备库现在可以向其他备库进行流式传输，减少主库的复制负载。
   Configure max WAL retention for replication slots:
-    name: 配置复制槽的最大 WAL 保持时间
+    name: 配置复制槽的最大 WAL 保留量
     description: https://www.postgresql.org/docs/13/runtime-config-replication.html#GUC-MAX-SLOT-WAL-KEEP-SIZE
   Failover support for logical replication slots:
     name: 逻辑复制槽的故障切换支持
@@ -623,7 +623,7 @@
     name: 逻辑复制避免复制环路
   Logical replication column lists:
     name: 逻辑复制的列列表
-    description: 逻辑复制中的 publication 现在可以指定要发布的列列表，以往必须复制整行。
+    description: 逻辑复制中的 发布 现在可以指定要发布的列列表，以往必须复制整行。
   Logical replication for partitioned tables:
     name: 分区表的逻辑复制
     description: 完全支持分区表的逻辑复制。
@@ -641,8 +641,8 @@
   Logical replication parallel apply of transactions:
     name: 逻辑复制对事务的并行应用
   Logical replication publish all tables in schema:
-    name: 逻辑复制发布 schema 中的全部表
-    description: 创建 publication 时，现在可以指定发布某个 schema 下的全部表。
+    name: 逻辑复制发布 模式 中的全部表
+    description: 创建 发布 时，现在可以指定发布某个 模式 下的全部表。
   Logical replication row filtering:
     name: 逻辑复制的行过滤
   Logical replication slots migrate through pg_upgrade migrate:
@@ -658,31 +658,28 @@
     name: 逻辑复制写冲突日志
     description: 逻辑复制期间发生的写冲突现在会同时记录到日志以及 `pg_stat_subscription_stats` 视图中。
   Quorum commit for synchronous replication:
-    name: 同步复制的仲裁提交
-    description: 使用 synchronous_standby_names 配置参数，可以设置同步复制以允许任意数量的备用服务器确认写入已提交，而不考虑其顺序。这也被称为“仲裁提交”。
+    name: 同步复制的基于法定人数的提交
+    description: 使用 synchronous_standby_names 配置参数，可以设置同步复制以允许任意数量的备库确认写入已提交，而不考虑其顺序。这也被称为“基于法定人数的提交”。
   Replication Slots:
     name: 复制槽
     description: https://www.postgresql.org/docs/9.4/static/warm-standby.html#STREAMING-REPLICATION-SLOTS
   Streaming-only cascading replication:
     name: 仅流式复制的级联复制
-    description: 级联复制以前要求 xlog 归档可用，以便新主服务器的备用服务器正确切换到新时间线。此更改移除了这一依赖。
+    description: 级联复制以前要求 xlog 归档可用，以便新主库的备库正确切换到新时间线。此更改移除了这一依赖。
   Streaming Replication:
     name: 流复制
     description: 基于流式 WAL 数据的集成复制支持
   Synchronous replication:
     name: 同步复制
-    description: 允许主服务器在确认提交之前等待备用服务器将事务信息写入磁盘。通过 synchronous_standby_names 设置控制，一次可以有一个备用服务器担任同步备用服务器的角色。可以使用 synchronous_commit 设置按事务启用或禁用同步复制。
+    description: 允许主库在确认提交之前等待备库将事务信息写入磁盘。通过 synchronous_standby_names 设置控制，一次可以有一个备库担任同步备库的角色。可以使用 synchronous_commit 设置按事务启用或禁用同步复制。
   Archive modules:
     name: 归档模块
   Checksum on data pages:
     name: 数据页校验和
-    description: 现在可以在创建集群时对页面启用校验和，以检测和报告页面损坏。
+    description: 现在可以在创建集簇时对页面启用校验和，以检测和报告页面损坏。
   Enable/Disable page checksums in an offline cluster:
-    name: 在离线集群中启用/禁用页面校验和
-    description: '可以通过 pg_checksums 命令在离线 PostgreSQL 集群中启用或禁用页面校验和：
-
-
-      https://www.postgresql.org/docs/12/app-pgchecksums.html'
+    name: 在离线集簇中启用/禁用页面校验和
+    description: "可以通过 pg_checksums 命令在离线 PostgreSQL 集簇中启用或禁用页面校验和：\n\nhttps://www.postgresql.org/docs/12/app-pgchecksums.html"
   Generic WAL facility:
     name: 通用 WAL 机制
     description: 一致、简单的数据库对象 WAL 日志记录 API，支持新的索引访问方法和其他存储对象。
@@ -690,7 +687,7 @@
     name: 热备
     description: 允许在恢复期间进行只读连接
   lz4 and Zstandard (zstd) compression for WAL full page writes:
-    name: WAL 全页写的 lz4 与 Zstandard (zstd) 压缩
+    name: WAL 整页写入的 lz4 与 Zstandard (zstd) 压缩
   min_wal_size / max_wal_size:
     name: min_wal_size / max_wal_size
     description: '替代 `checkpoint_segments` 参数。
@@ -698,8 +695,8 @@
 
       更多信息请参阅 [WAL 配置](https://www.postgresql.org/docs/current/wal-configuration.html) 文档。'
   Multiple synchronous standbys:
-    name: 多个同步备用服务器
-    description: 在提交同步事务时，能够要求多个优先级备用服务器确认同步消息。
+    name: 多个同步备库
+    description: 在提交同步事务时，能够要求多个优先级备库确认同步消息。
   Named restore points:
     name: 命名恢复点
     description: 使用 pg_create_restore_point 创建命名的恢复点以执行恢复操作
@@ -720,7 +717,7 @@
     description: '`pg_basebackup` 现在支持在服务端创建备份时直接执行压缩。'
   pg_basebackup tool:
     name: pg_basebackup 工具
-    description: pg_basebackup 是一个用于对 PostgreSQL 集群进行基础备份的工具
+    description: pg_basebackup 是一个用于对 PostgreSQL 集簇进行基础备份的工具
   pg_dump, pg_dumpall, pg_restore --filter:
     name: pg_dump、pg_dumpall、pg_restore --filter
     description: '`pg_dump`、`pg_dumpall` 和 `pg_restore` 新增 `--filter` 选项，可按规则包含或排除对象。'
@@ -734,10 +731,10 @@
     name: 恢复期间预取 WAL
   remote_apply mode:
     name: remote_apply 模式
-    description: 能够要求同步副本在提交之前与主服务器保持应用变更的同步。支持“一致性集群”。
+    description: 能够要求同步副本在提交之前与主库保持应用变更的同步。支持“一致性集群”。
   Time-delayed Standbys:
-    name: 延迟备用服务器
-    description: 一个名为 recovery_min_apply_delay 的新设置允许备用服务器按指定的时间量落后于主服务器。
+    name: 延迟备库
+    description: 一个名为 recovery_min_apply_delay 的新设置允许备库按指定的时间量落后于主库。
   Verify backup integrity (pg_verifybackup):
     name: 验证备份完整性 (pg_verifybackup)
     description: https://www.postgresql.org/docs/13/app-pgverifybackup.html
@@ -746,12 +743,12 @@
     description: https://www.postgresql.org/docs/current/static/warm-standby.html
   pg_upgrade --swap:
     name: pg_upgrade --swap
-    description: '`--swap` 选项会把旧集群的数据目录直接交换到新集群，并用新版本生成的目录元数据替换之。'
+    description: '`--swap` 选项会把旧集簇的数据目录直接交换到新集簇，并用新版本生成的目录元数据替换之。'
   Planner statistics preserved on major version upgrade:
     name: 主版本升级后保留规划器统计信息
-    description: 主版本升级后会保留规划器统计信息，使集群在升级完成后更快恢复预期性能。
+    description: 主版本升级后会保留规划器统计信息，使集簇在升级完成后更快恢复预期性能。
   COPY from/to STDIN/STDOUT:
-    name: COPY 到/从 STDIN/STDOUT
+    name: COPY 从 STDIN 导入／向 STDOUT 导出
     description: COPY 和 psql 的 \copy 现在分别支持从 STDIN 和到 STDOUT 的管道传输。
   COPY FROM ... WHERE:
     name: COPY FROM ... WHERE
@@ -895,8 +892,8 @@
     name: 保存点
     description: https://www.postgresql.org/docs/current/static/sql-savepoint.html
   Serializable Snapshot Isolation:
-    name: 可串行快照隔离
-    description: 实现了真正的可串行快照隔离。此前，请求可串行隔离级别仅保证在整个事务中使用单个 MVCC 快照，这允许某些已知的异常现象发生。
+    name: 可串行化快照隔离
+    description: 实现了真正的可串行化快照隔离。此前，请求可串行化隔离级别仅保证在整个事务中使用单个 MVCC 快照，这允许某些已知的异常现象发生。
   Two Phase commit:
     name: 两阶段提交
     description: https://www.postgresql.org/docs/current/static/sql-prepare-transaction.html
@@ -904,10 +901,10 @@
     name: 可更新游标
     description: https://www.postgresql.org/docs/current/static/sql-declare.html
   Inserted data can trigger autovacuum:
-    name: 插入的数据可触发自动 vacuum
+    name: 插入的数据可触发自动清理
     description: 这一新行为减少了表需要冻结时的必要工作量，并允许页面被设置为全可见。全可见页面允许仅索引扫描访问更少的堆行。
   Integrated autovacuum daemon:
-    name: 集成自动 vacuum 守护进程
+    name: 集成自动清理 守护进程
     description: https://www.postgresql.org/docs/current/static/routine-vacuuming.html#AUTOVACUUM
   Page freezing optimizations:
     name: 页面冻结优化
@@ -928,8 +925,8 @@
     name: Vacuum “紧急模式”
     description: 当 PostgreSQL 检测到事务 ID 回卷风险时，会更激进地执行 vacuum；该行为由 `vacuum_failsafe_age` 控制。
   Visibility Map for Vacuuming:
-    name: 用于 Vacuuming 的可见性图
-    description: 这减少了 vacuum 的开销，因为可见性图只跟踪需要被 vacuum 的页面。
+    name: 清理使用的可见性映射
+    description: 可见性映射记录页面的全可见状态，使 VACUUM 能够跳过无需扫描的页面，从而减少清理开销。
   Certificate authentication with postgres_fdw:
     name: postgres_fdw 的证书身份验证
     description: '[postgres_fdw](https://www.postgresql.org/docs/13/postgres-fdw.html) 现在可以使用内置的 [证书身份验证](https://www.postgresql.org/docs/current/auth-cert.html) 机制。
@@ -953,7 +950,7 @@
     description: https://www.postgresql.org/docs/9.5/static/sql-importforeignschema.html
   Import foreign table partitions:
     name: 导入外部表分区
-    description: '`IMPORT FOREIGN SCHEMA` 现在可以导入分区表的 schema。'
+    description: '`IMPORT FOREIGN SCHEMA` 现在可以导入分区表的定义。'
   Parallel query execution on remote databases:
     name: 远程数据库上的并行查询执行
     description: '`postgres_fdw` 现在可以利用远程数据库上的查询并行能力。'
@@ -961,7 +958,7 @@
     name: postgres_fdw 并行提交
   postgres_fdw pushdown:
     name: postgres_fdw 下推
-    description: 能够在 postgres_fdw 驱动中将 JOIN、排序、UPDATE 和 DELETE 下推到远程数据库，理论上其他驱动也支持。同时支持一些通用的运算符/函数下推。
+    description: 能够在 postgres_fdw 驱动中将 JOIN、排序、UPDATE 和 DELETE 下推到远程数据库，理论上其他驱动也支持。同时支持一些通用的操作符/函数下推。
   postgres_fdw SCRAM authentication passthrough:
     name: postgres_fdw SCRAM 认证透传
   PostgreSQL Foreign Data Wrapper:
@@ -1073,7 +1070,7 @@
     description: 在 pl/pgsql 中返回任意查询结果的简便方法
   ROWS and COST specification for functions:
     name: 函数的 ROWS 和 COST 规范
-    description: 现在可以为过程函数指定 CPU 成本和预期行数，以向规划器提供更好的提示
+    description: 现在可以为过程函数指定 CPU 代价和预期行数，以向规划器提供更好的提示
   Scrollable and updatable cursor support for pl/pgsql:
     name: pl/pgsql 的可滚动和可更新游标支持
     description: pl/pgsql 函数中对可滚动和可更新游标的支持
@@ -1087,7 +1084,7 @@
     description: 允许通过 RAISE 生成用户定义的异常
   Validator function for pl/perl:
     name: pl/perl 的验证器函数
-    description: pl/pgsql 代码的编译时验证
+    description: PL/Perl 代码的编译时验证
   CREATE EXTENSION .. CASCADE:
     name: CREATE EXTENSION .. CASCADE
     description: 能够为扩展依赖树自动拉取所需的额外扩展。
@@ -1096,7 +1093,7 @@
     description: https://www.postgresql.org/docs/current/static/external-extensions.html
   Trusted Extensions:
     name: 受信任的扩展
-    description: "被超级用户标记为\"受信任\"的扩展随后可以由非特权用户通过 `CREATE EXTENSION` 安装。某些函数默认标记为\"受信任\"。\r\n\r\n更多信息请参阅 [`CREATE EXTENSION`](https://www.postgresql.org/docs/current/sql-createextension.html) 文档。"
+    description: "标记为“受信任”的扩展可由对当前数据库具有 CREATE 权限的非超级用户通过 `CREATE EXTENSION` 安装。某些内置扩展默认标记为“受信任”。\r\n\r\n更多信息请参阅 [`CREATE EXTENSION`](https://www.postgresql.org/docs/current/sql-createextension.html) 文档。"
   Built-in, platform independent immutable collation:
     name: 内置、平台无关且不可变的排序规则
     description: PostgreSQL 新增内置、平台无关且不可变的排序规则提供器，支持 `C` 和 `C.UTF-8` 排序规则。
@@ -1109,8 +1106,8 @@
     name: 数据库级别排序规则
     description: 支持在每个数据库级别设置不同的排序顺序和字符分类
   Default ICU collations for clusters/databases:
-    name: 集群/数据库的默认 ICU 排序规则
-    description: ICU 排序规则现在可以作为整个 PostgreSQL 集群或单个数据库的默认排序规则类型。
+    name: 集簇/数据库的默认 ICU 排序规则
+    description: ICU 排序规则现在可以作为整个 PostgreSQL 集簇或单个数据库的默认排序规则类型。
   EUC_JIS_2004/ SHIFT_JIS_2004 support:
     name: EUC_JIS_2004/ SHIFT_JIS_2004 支持
   ICU collations:
@@ -1130,8 +1127,8 @@
   pg_unicode_fast collation:
     name: pg_unicode_fast 排序规则
   Unicode string literals and identifiers:
-    name: Unicode 字符串文字和标识符
-    description: 允许使用代码点指定 Unicode 字符串文字和标识符
+    name: Unicode 字符串字面量和标识符
+    description: 允许使用代码点指定 Unicode 字符串字面量和标识符
   UTF8 support on Windows:
     name: Windows 上的 UTF8 支持
     description: 完整支持在 Microsoft Windows 上使用 UTF8
@@ -1160,7 +1157,7 @@
     name: pg_walsummary
   pg_xlogdump:
     name: contrib/pg_xlogdump
-    description: 以人类可读的格式显示 PostgreSQL 数据库集群的预写式日志。
+    description: 以人类可读的格式显示 PostgreSQL 数据库集簇的预写式日志。
   psql \bind:
     name: psql \bind
   psql \dconfig:
@@ -1182,7 +1179,7 @@
     name: contrib/auth_delay
     description: auth_delay 使服务器在报告认证失败前短暂暂停，从而增加对数据库密码进行暴力破解攻击的难度。
   auto_explain:
-    name: contrib/autoexplain
+    name: contrib/auto_explain
     description: 自动记录慢查询的执行计划
   btree_gin:
     name: contrib/btree_gin
@@ -1271,7 +1268,7 @@
     description: https://www.postgresql.org/docs/9.2/static/tcn.html
   tsearch2 compatibility wrapper:
     name: tsearch2 兼容性包装器
-    description: 用于从 contrib/tsearch2 迁移到 8.3 集成全文搜索的兼容性包装器
+    description: 用于从 contrib/tsearch2 迁移到 8.3 集成全文检索的兼容性包装器
   unaccent:
     name: contrib/unaccent
     description: https://www.postgresql.org/docs/current/static/unaccent.html
@@ -1297,7 +1294,7 @@
     name: Microsoft Visual C++ 支持
     description: 允许使用 Microsoft Visual Studio 编译后端
   Native Windows Port:
-    name: 原生 Windows 端口
+    name: Windows 原生移植版
     description: 原生移植到 Microsoft Windows 平台
   Spinlock support for the SuperH hardware platform:
     name: SuperH 硬件平台的自旋锁支持
--- a/data/page_metadata.yaml
+++ b/data/page_metadata.yaml
@@ -1,6 +1,6 @@
 pages:
   /about/:
-    title: PostgreSQL：项目简介与核心特性
+    title: PostgreSQL 项目简介与核心特性
     description: 了解 PostgreSQL 的对象关系型数据库特性、可靠性、性能、扩展能力与社区资源。
     lang: zh
   /about/casestudies/mohawk/:
@@ -21,48 +21,47 @@
       with the goal of providing cost-effective s…
     lang: en
   /about/contact/:
-    title: PostgreSQL：联系我们
-    description: PostgreSQL 社区始终致力于提供能够可靠存储数据的软件。如果您认为发现了 Bug，请点击下方按钮，并按照说明提交 Bug 报告
+    title: PostgreSQL 联系我们
+    description: 汇总 PostgreSQL 的 Bug、安全问题、技术支持、媒体与社区联系渠道，并说明如何反馈 pg.center 的译文和网站问题。
     lang: zh
   /about/donate/:
-    title: PostgreSQL：捐赠与非营利组织
+    title: PostgreSQL 捐赠与非营利组织
     description: 了解支持 PostgreSQL 项目的认可非营利组织，以及向这些组织捐赠的方式。
     lang: zh
   /about/donate_pg_org/:
-    title: PostgreSQL：捐赠
+    title: PostgreSQL 捐赠
     description: PostgreSQL 的捐赠由资金小组管理。捐赠的资金可用于 PostgreSQL 社区所需的各种服务，包括推广材料、项目基础设施、法律费用等
     lang: zh
   /about/governance/:
-    title: PostgreSQL：治理
-    description: 本页列出了构成项目治理层级的各个团队和委员会的页面链接。请查看各个链接页面以了解每个小组的更多信息，包括成员和章程
+    title: PostgreSQL 治理
+    description: 了解 PostgreSQL 的项目治理结构，查阅核心团队、各委员会与工作小组的职责、成员和章程。
     lang: zh
   /about/governance/contributors/:
-    title: PostgreSQL：贡献者委员会
-    description: 所有贡献者的认可由贡献者委员会负责管理，委员会根据个人情况决定每位贡献者或潜在贡献者的状态。决定依据认可的贡献者政策作出。当前确定谁有资格成为贡献者的方式是通过委员会成员的简单多数投票。委员会的决定是最终决定，除非被核心团队推翻
+    title: PostgreSQL 贡献者委员会
+    description: 了解 PostgreSQL 贡献者委员会的职责、成员与工作方式，以及贡献者认可政策和联系渠道。
     lang: zh
   /about/governance/sysadmin/:
-    title: PostgreSQL：基础设施团队
-    description: 基础设施团队负责运行所有 postgresql.org 基础设施，包括各种公共和非公共服务。PostgreSQL 网站开发在 pgsql-www 邮件列表上讨论。postgresql.org
-      网站的源代码存储在公共的 GIT 代码仓库中
+    title: PostgreSQL 基础设施团队
+    description: 了解 PostgreSQL 基础设施团队负责的服务、工作方式与联系渠道，以及网站开发和基础设施管理的分工。
     lang: zh
   /about/licence/:
-    title: PostgreSQL：许可证
+    title: PostgreSQL 许可证
     description: PostgreSQL 采用 PostgreSQL 许可证发布，这是一种宽松的开源许可证，类似于 BSD 或 MIT 许可证
     lang: zh
   /about/policies/:
-    title: PostgreSQL：项目政策
+    title: PostgreSQL 项目政策
     description: 浏览 PostgreSQL 项目的正式政策，包括行为准则、隐私、商标、版本、服务与社区认可政策。
     lang: zh
   /about/policies/archives/:
-    title: PostgreSQL：归档政策
-    description: PostgreSQL 邮件列表在 www.postgresql.org 上进行归档。本站旨在提供对列表活动的准确记录，因此不会进行修改
+    title: PostgreSQL 邮件列表归档政策
+    description: 了解 PostgreSQL 邮件列表的归档原则、准确记录要求及隐私相关说明。
     lang: zh
   /about/policies/coc-translations/:
-    title: PostgreSQL：行为准则翻译与审批流程
+    title: PostgreSQL 行为准则翻译与审批流程
     description: PostgreSQL 行为准则委员会负责管理将行为准则从英语翻译成其他语言的审批流程，以及在政策修订时更新翻译
     lang: zh
   /about/policies/coc/:
-    title: PostgreSQL：行为准则
+    title: PostgreSQL 行为准则
     description: 阅读 PostgreSQL 社区行为准则，了解参与项目时应遵循的专业、礼貌与包容原则。
     lang: zh
     alternates:
@@ -181,7 +180,7 @@
       ru: /about/policies/coc/ru/
       zh: /about/policies/coc/
   /about/policies/coc/zh/:
-    title: PostgreSQL：行为准则
+    title: PostgreSQL 行为准则
     description: PostgreSQL 项目以我们的代码和工作质量，以及社区的技术和专业成就而自豪。我们希望所有的参与者品行专业，以礼相待，互惠互利，尊重所有用户和开发者
     lang: zh
     canonical: /about/policies/coc/
@@ -194,26 +193,25 @@
       ru: /about/policies/coc/ru/
       zh: /about/policies/coc/
   /about/policies/coc_committee/:
-    title: PostgreSQL：行为准则委员会
+    title: PostgreSQL 行为准则委员会
     description: 联系 PostgreSQL 行为准则委员会，查看现任成员与委员会职责。
     lang: zh
   /about/policies/conferences/:
-    title: PostgreSQL：社区会议认可政策
+    title: PostgreSQL 社区会议认可政策
     description: 社区会议认可计划是一项自愿方案，向 PostgreSQL 网站活动列表提交活动的提交者可以根据以下标准进行自我评估，如果符合要求，可以将其活动宣传为 PostgreSQL
       社区活动
     lang: zh
   /about/policies/contributors/:
-    title: PostgreSQL：认可贡献者政策
+    title: PostgreSQL 认可贡献者政策
     description: PostgreSQL 贡献者简介页面列出了对 PostgreSQL 项目做出了长期投入时间和精力的实质性贡献的人员。一次性或短期的贡献通常不被视为足以列入，除非涉及相当大量的代码和时间
     lang: zh
   /about/policies/funds-group/:
-    title: PostgreSQL：资金小组
+    title: PostgreSQL 资金小组
     description: PostgreSQL 项目与 Software in the Public Interest, Inc. 之间关系的治理章程
     lang: zh
   /about/policies/news-and-events/:
-    title: PostgreSQL：新闻与活动审批政策
-    description: postgresql.org 首页包含新闻和活动列表，这些列表完全为 PostgreSQL 社区的利益而维护。由于这些版位若商业化提供会非常有价值（每条公告最高可达 400
-      美元），管理员对首页允许发布的内容拥有完全裁量权
+    title: PostgreSQL 新闻与活动审批政策
+    description: 查阅 PostgreSQL 新闻与活动的收录政策，了解提交要求、审核标准、发布频率及活动公告规则。
     lang: zh
   /about/policies/npos/:
     title: 认可的 PostgreSQL 非营利组织
@@ -221,32 +219,32 @@
       PostgreSQL 社区所期望的开放性标准
     lang: zh
   /about/policies/planet-postgresql/:
-    title: Planet PostgreSQL
+    title: Planet PostgreSQL 博客收录政策
     description: Planet PostgreSQL 是由 PostgreSQL 社区运营的博客聚合服务。除了 Planet 主站之外，文章也会被发布（“聚合”）到 www.postgresql.org
       网站上
     lang: zh
   /about/policies/privacy/:
-    title: PostgreSQL：隐私政策
+    title: PostgreSQL 隐私政策
     description: PostgreSQL 网站隐私政策说明访问日志、个人信息处理和相关权利。
     lang: zh
   /about/policies/project-name/:
-    title: PostgreSQL：项目名称政策
-    description: Postgres 是 PostgreSQL 项目的一个公认别名。但它只是别名或昵称，并非项目的官方名称。引用 PostgreSQL 核心团队成员 Dave Page 的话：
+    title: PostgreSQL 项目名称政策
+    description: 了解 PostgreSQL 的正式项目名称、Postgres 简称的由来与使用惯例，以及社区对项目名称的说明。
     lang: zh
   /about/policies/services-and-hosting/:
-    title: PostgreSQL：专业服务与托管审批政策
-    description: www.postgresql.org 主页上包含专业服务和托管（统称为“服务”）列表，这些列表完全是为了 PostgreSQL 社区的利益而维护的。由于这些空间如果商业化将具有相当的价值，因此我们的管理员有权对主页上允许展示的内容进行严格限制
+    title: PostgreSQL 专业服务与托管审批政策
+    description: 查阅 PostgreSQL 专业服务与托管服务的收录政策，了解企业资料、服务描述、覆盖地区和审核要求。
     lang: zh
   /about/policies/sponsorship/:
-    title: PostgreSQL：赞助
+    title: PostgreSQL 赞助政策
     description: PostgreSQL 赞助政策说明赞助商级别、资格与支持社区工作的方式。
     lang: zh
   /about/policies/trademarks/:
-    title: PostgreSQL：商标政策
+    title: PostgreSQL 商标政策
     description: 本政策涉及我们的商标。具体而言，Postgres 和 PostgreSQL 以及大象标志（Slonik）均为 PostgreSQL 加拿大社区协会的注册商标。如果您希望使用这些标志，必须遵守本政策
     lang: zh
   /about/policies/twitter/:
-    title: PostgreSQL：@postgresql Twitter 账户政策
+    title: PostgreSQL @postgresql Twitter 账户政策
     description: PostgreSQL @postgresql Twitter 账户人工运营与内容发布政策。
     lang: zh
   /about/policies/user-groups/:
@@ -254,13 +252,12 @@
     description: 认可的 PostgreSQL 用户组，又称"PUG"，可以作为 PostgreSQL 网站上的认可用户组列出。要成为认可的 PostgreSQL 用户组，用户组必须自我证明其满足以下标准，这些标准旨在确保其达到 PostgreSQL 社区期望的开放性要求
     lang: zh
   /about/press/:
-    title: PostgreSQL：新闻与新闻资料包
-    description: 如有任何新闻咨询，请联系 PostgreSQL 公关团队：press@postgresql.org
+    title: PostgreSQL 媒体资料与新闻资料包
+    description: 获取 PostgreSQL 新闻资料包、媒体常见问题和公关团队联系方式，查阅各版本发布资料及不同语言版本。
     lang: zh
   /about/press/faq/:
-    title: PostgreSQL：新闻常见问题
-    description: 问：PostgreSQL 的当前版本是什么？答：18，于 2025 年 9 月 25 日发布。这是我们在超过 39 年开发历程中的第 35 个主要版本。我们每年发布一个新的
-      PostgreSQL 版本，这在 SQL 数据库中是独一无二的
+    title: PostgreSQL 媒体常见问题
+    description: 面向媒体介绍 PostgreSQL 的版本发布、许可证、项目治理、用户与社区支持，并解答常见的数据库选型问题。
     lang: zh
   /about/press/presskit10/:
     title: PostgreSQL 10 Press Kit
@@ -285,7 +282,7 @@
       vi: /about/press/presskit10/vi/
       zh: /about/press/presskit10/cn/
   /about/press/presskit10/cn/:
-    title: PostgreSQL：10.0 版本发布说明
+    title: PostgreSQL 10 新闻资料包
     description: 2017-10-05 - PostgreSQL 全球开发组今天宣布，PostgreSQL 10 正式发布。PostgreSQL 作为世界上最先进的开源关系型数据库，10
       是迄今为止的最新版本
     lang: zh
@@ -624,7 +621,7 @@
       vi: /about/press/presskit11/vi/
       zh: /about/press/presskit11/cn/
   /about/press/presskit11/cn/:
-    title: PostgreSQL：11 新闻资料包
+    title: PostgreSQL 11 新闻资料包
     description: 2018-10-18，全球开发组今天宣布，PostgreSQL 11 正式发布。PostgreSQL 作为世界上最先进的开源关系型数据库，11.0 是迄今为止的最新版本
     lang: zh
     alternates:
@@ -1077,8 +1074,8 @@
       ru: /about/press/presskit12/ru/
       zh: /about/press/presskit12/zh/
   /about/press/presskit12/zh/:
-    title: PostgreSQL：12 新闻资料包
-    description: PostgreSQL 全球开发组今天宣布发布 PostgreSQL 12，这是世界上最先进的开源数据库的最新版本
+    title: PostgreSQL 12 新闻资料包
+    description: PostgreSQL 12 新闻资料包：介绍查询性能、索引优化、SQL/JSON 路径等改进，并提供下载和文档资源。
     lang: zh
     alternates:
       de: /about/press/presskit12/de/
@@ -1311,8 +1308,8 @@
       ru: /about/press/presskit13/ru/
       zh: /about/press/presskit13/zh/
   /about/press/presskit13/zh/:
-    title: PostgreSQL：13 新闻资料包
-    description: PostgreSQL 全球开发组今天宣布 PostgreSQL 13 正式发布。作为世界上最先进的开源数据库，PostgreSQL 13 是目前的最新版本
+    title: PostgreSQL 13 新闻资料包
+    description: PostgreSQL 13 新闻资料包：介绍索引、查询性能、并行清理与受信任扩展等改进，并提供下载和文档资源。
     lang: zh
     alternates:
       de: /about/press/presskit13/de/
@@ -1465,8 +1462,8 @@
       ru: /about/press/presskit14/ru/
       zh: /about/press/presskit14/zh/
   /about/press/presskit14/zh/:
-    title: PostgreSQL：14 新闻资料包
-    description: PostgreSQL 全球开发组今天宣布 PostgreSQL 14 正式发布，这是世界上最先进的开源数据库的最新版本
+    title: PostgreSQL 14 新闻资料包
+    description: PostgreSQL 14 新闻资料包：介绍多范围类型、查询性能、逻辑复制与监控等改进，并提供下载和文档资源。
     lang: zh
     alternates:
       de: /about/press/presskit14/de/
@@ -1729,7 +1726,7 @@
       uk: /about/press/presskit15/uk/
       zh: /about/press/presskit15/zh/
   /about/press/presskit15/zh/:
-    title: PostgreSQL：15 新闻资料包
+    title: PostgreSQL 15 新闻资料包
     description: 2022 年 10 月 13 日 - PostgreSQL 全球开发组今日宣布发布 PostgreSQL 15，这是世界上最先进的开源数据库的最新版本
     lang: zh
     alternates:
@@ -1998,7 +1995,7 @@
       uk: /about/press/presskit16/uk/
       zh: /about/press/presskit16/zh/
   /about/press/presskit16/zh/:
-    title: PostgreSQL：16 新闻资料包
+    title: PostgreSQL 16 新闻资料包
     description: 2023 年 9 月 14 日 - PostgreSQL 全球开发组今天宣布 PostgreSQL 16 正式发布，作为世界上最先进的开源数据库，PostgreSQL 16 是目前的最新版本
     lang: zh
     alternates:
@@ -2372,7 +2369,7 @@
       uk: /about/press/presskit17/uk/
       zh: /about/press/presskit17/zh/
   /about/press/presskit17/zh/:
-    title: PostgreSQL：17 新闻资料包
+    title: PostgreSQL 17 新闻资料包
     description: 2024 年 9 月 26 日 - PostgreSQL 全球开发组今日宣布发布 PostgreSQL 17，这是世界上最先进的开源数据库的最新版本
     lang: zh
     alternates:
@@ -2713,7 +2710,7 @@
       uk: /about/press/presskit18/uk/
       zh: /about/press/presskit18/zh/
   /about/press/presskit18/zh/:
-    title: PostgreSQL：18 新闻资料包
+    title: PostgreSQL 18 新闻资料包
     description: 2025 年 9 月 25 日 - PostgreSQL 全球开发组今日宣布发布 PostgreSQL 18，这是世界上最先进的开源数据库的最新版本
     lang: zh
     alternates:
@@ -2999,7 +2996,7 @@
       tr: /about/press/presskit90/tr/
       zh: /about/press/presskit90/zh_cn/
   /about/press/presskit90/zh_cn/:
-    title: PostgreSQL：9.0 版发布宣传资料
+    title: PostgreSQL 9.0 版发布宣传资料
     description: '2010/09/20: PostgreSQL 全球开发组宣布业界领先的开源关系数据库 PostgreSQL 9.0 版发布，该版本进一步强化了安全、应用程序支持、监控、性能和特殊数据存储等方面的领先功能。用户尤其关注
       9.0 版本中的数据复制和热备功能，这些功能也将加速 PostgreSQL 在云计算和大规模可伸缩应用方面的使用。'
     lang: zh
@@ -3206,8 +3203,8 @@
       ru: /about/press/presskit91/ru/
       zh: /about/press/presskit91/zh_cn/
   /about/press/presskit91/zh_cn/:
-    title: PostgreSQL：9.1 版发布宣传资料
-    description: '2011/09/12: PostgreSQL 全球开发组宣布业界领先的开源关系数据库 PostgreSQL 9.1 版发布，该版本增加了很多创新性的技术、强大的可扩展性以及类似同步复制、最近相邻索引和外部数据封装等功能。'
+    title: PostgreSQL 9.1 版发布宣传资料
+    description: '2011/09/12: PostgreSQL 全球开发组宣布业界领先的开源关系数据库 PostgreSQL 9.1 版发布，该版本增加了很多创新性的技术、强大的可扩展性以及类似同步复制、最近邻索引和外部数据包装器等功能。'
     lang: zh
     alternates:
       de: /about/press/presskit91/de/
@@ -3503,8 +3500,8 @@
       uk: /about/press/presskit92/ua/
       zh: /about/press/presskit92/zh_CN/
   /about/press/presskit92/zh_CN/:
-    title: PostgreSQL：9.2 版发布宣传资料
-    description: '2012/09/10: PostgreSQL 全球开发组宣布业界领先的开源关系数据库 PostgreSQL 9.2 版发布，由于 Beta 测试版本在 5 月即已发布，开发人员和软件厂商称赞该版本在性能、可扩展性和灵活性方面得到大的提升。大量的用户期待切换至新的版本。'
+    title: PostgreSQL 9.2 版发布宣传资料
+    description: '2012/09/10: PostgreSQL 全球开发组宣布业界领先的开源关系数据库 PostgreSQL 9.2 版发布，由于 Beta 测试版本在 5 月即已发布，开发人员和软件厂商称赞该版本在性能、可伸缩性和灵活性方面得到大的提升。大量的用户期待切换至新的版本。'
     lang: zh
     alternates:
       ca: /about/press/presskit92/ca/
@@ -3728,7 +3725,7 @@
       uk: /about/press/presskit93/uk/
       zh: /about/press/presskit93/zh_CN/
   /about/press/presskit93/zh_CN/:
-    title: PostgreSQL：9.3 版本发布说明
+    title: PostgreSQL 9.3 新闻资料包
     description: '2013/9/9: 全球开发组宣布业界领先的开源关系数据库 PostgreSQL 9.3 版发布，这个版本扩展了可靠性、可用性及与其他数据库易集成性，用户将会发现，他们可以使用 9.3
       版本创建一些对以前版本是不可能实现的新功能。'
     lang: zh
@@ -4099,51 +4096,51 @@
       pt: /about/press/presskit96/pt/
       ru: /about/press/presskit96/ru/
   /community/:
-    title: PostgreSQL：社区｜交流、用户组与社区资源
+    title: PostgreSQL 社区｜交流、用户组与社区资源
     description: 了解 PostgreSQL 社区的邮件列表、用户组、IRC、国际站点与其他交流资源。
     lang: zh
   /community/contributors/:
-    title: PostgreSQL：贡献者档案
+    title: PostgreSQL 贡献者档案
     description: 浏览 PostgreSQL 核心组、主要贡献者和其他贡献者名录，了解贡献者资料与更新方式。
     lang: zh
   /community/international/:
-    title: PostgreSQL：国际社区站点
+    title: PostgreSQL 国际社区站点
     description: 浏览各国和地区 PostgreSQL 社区网站及其语言站点。
     lang: zh
   /community/irc/:
-    title: PostgreSQL：IRC 社区与频道
+    title: PostgreSQL IRC 社区与频道
     description: 查找 PostgreSQL 在 Libera Chat 上的 IRC 频道、语言频道和管理信息。
     lang: zh
   /community/recognised-npos/:
-    title: PostgreSQL：认可的非营利组织
+    title: PostgreSQL 认可的非营利组织
     description: 了解支持 PostgreSQL 项目的认可非营利组织及其社区活动。
     lang: zh
   /community/user-groups/:
-    title: PostgreSQL：本地用户组
+    title: PostgreSQL 本地用户组
     description: 按国家和地区浏览 PostgreSQL 本地用户组，了解加入或创建用户组的方式。
     lang: zh
   /developer/:
-    title: PostgreSQL：开发者资源｜参与开发与贡献
+    title: PostgreSQL 开发者资源｜参与开发与贡献
     description: 面向 PostgreSQL 开发者的路线图、源代码、测试、CommitFest 与参与贡献指南。
     lang: zh
   /developer/backend/:
-    title: PostgreSQL：后端流程图｜查询处理路径
+    title: PostgreSQL 后端流程图｜查询处理路径
     description: 通过后端流程图了解 PostgreSQL 查询从连接、解析到执行的处理过程。
     lang: zh
   /developer/beta/:
-    title: PostgreSQL：Beta 与候选版本测试指南
+    title: PostgreSQL Beta 与候选版本测试指南
     description: 了解 PostgreSQL Beta 和候选发布版本的用途、风险、下载、文档与测试方法；不适用于生产环境。
     lang: zh
   /developer/coding/:
-    title: PostgreSQL：源代码与编码指南
+    title: PostgreSQL 源代码与编码指南
     description: 查找 PostgreSQL 源代码、Git 提交、开发文档与编码相关资源。
     lang: zh
   /developer/committers/:
-    title: PostgreSQL：提交者名单与说明
+    title: PostgreSQL 提交者名单与说明
     description: 查看 PostgreSQL 当前提交者名单、提交权限与提交日志说明。
     lang: zh
   /developer/core/:
-    title: PostgreSQL：核心团队｜成员与职责
+    title: PostgreSQL 核心团队｜成员与职责
     description: 了解 PostgreSQL 核心团队的职责、成员、活动报告与联系方式。
     lang: zh
   /developer/corereports/january2022_december2022/:
@@ -4159,15 +4156,15 @@
     description: High-level report of PostgreSQL Core Team activities from June 2019 to December 2021.
     lang: en
   /developer/related-projects/:
-    title: PostgreSQL：相关项目｜社区基础设施与工具
+    title: PostgreSQL 相关项目｜社区基础设施与工具
     description: 浏览为 PostgreSQL 社区提供基础设施、开发工具、软件包和活动支持的相关项目。
     lang: zh
   /developer/roadmap/:
-    title: PostgreSQL：开发路线图｜版本发布时间表
+    title: PostgreSQL 开发路线图｜版本发布时间表
     description: 查看 PostgreSQL 小版本发布时间表和下一个主版本的开发计划。
     lang: zh
   /developer/summerofcode/:
-    title: PostgreSQL：Google 编程之夏（GSoC）｜2018 项目
+    title: PostgreSQL Google 编程之夏（GSoC）｜2018 项目
     description: PostgreSQL 参加 Google Summer of Code 2018 的项目、导师、已接受项目和常见问题。
     lang: zh
   /developer/summerofcode2011/:
@@ -4201,103 +4198,103 @@
       documentation and references.
     lang: en
   /developer/testing/:
-    title: PostgreSQL：测试资源｜Beta 与开发构建
+    title: PostgreSQL 测试资源｜Beta 与开发构建
     description: 查找 PostgreSQL Beta、每日开发构建、测试代码和文档更新相关资源。
     lang: zh
   /docs/:
-    title: PostgreSQL：文档与手册｜在线版及 PDF
+    title: PostgreSQL 文档与手册｜在线版及 PDF
     description: 查阅 PostgreSQL 中文手册、各版本在线文档、PDF 下载和其他语言翻译。
     lang: zh
   /docs/books/:
-    title: PostgreSQL：图书｜社区书目
+    title: PostgreSQL 图书｜社区书目
     description: 浏览 PostgreSQL 社区维护的图书目录，查看作者、语言、版本和出版信息。
     lang: zh
   /docs/faq/:
-    title: PostgreSQL：常见问题 FAQ
+    title: PostgreSQL 常见问题 FAQ
     description: 汇总 PostgreSQL 新闻、通用、开发和平台相关常见问题入口。
     lang: zh
   /docs/manuals/archive/:
-    title: PostgreSQL：旧版手册归档
+    title: PostgreSQL 旧版手册归档
     description: 浏览不再受支持的 PostgreSQL 版本手册和 PDF，并了解升级建议。
     lang: zh
   /docs/online-resources/:
-    title: PostgreSQL：教程与学习资源
+    title: PostgreSQL 教程与学习资源
     description: 浏览 PostgreSQL 教程、在线练习、百科和其他学习资源。
     lang: zh
   /docs/release/:
-    title: PostgreSQL：发布说明归档
+    title: PostgreSQL 发布说明归档
     description: 浏览 PostgreSQL 各个版本的完整发布说明和小版本修复记录。
     lang: zh
   /download/:
-    title: PostgreSQL：下载｜各平台安装包与源代码
+    title: PostgreSQL 下载｜各平台安装包与源代码
     description: 获取 PostgreSQL 在 Linux、macOS、Windows、BSD 等平台的安装包、源代码与相关软件。
     lang: zh
   /download/bsd/:
-    title: PostgreSQL：BSD 平台下载｜各发行版安装包
+    title: PostgreSQL BSD 平台下载｜各发行版安装包
     description: 查看 PostgreSQL 在 BSD 系列系统上的发行版和安装包获取方式。
     lang: zh
   /download/freebsd/:
-    title: PostgreSQL：FreeBSD 安装包
+    title: PostgreSQL FreeBSD 安装包
     description: 通过 FreeBSD Ports 与 Packages Collection 获取 PostgreSQL 安装包。
     lang: zh
   /download/linux/:
-    title: PostgreSQL：Linux 下载｜其他发行版安装
+    title: PostgreSQL Linux 下载｜其他发行版安装
     description: 了解在未列出具体发行版的 Linux 系统上通过发行版软件包或源代码安装 PostgreSQL。
     lang: zh
   /download/linux/debian/:
-    title: PostgreSQL：Debian 下载｜APT 仓库与安装
+    title: PostgreSQL Debian 下载｜APT 仓库与安装
     description: 在 Debian 上通过系统软件包或 PostgreSQL APT 仓库安装受支持的 PostgreSQL 版本。
     lang: zh
   /download/linux/redhat/:
-    title: PostgreSQL：Linux 下载（Red Hat 系列）
+    title: PostgreSQL Linux 下载（Red Hat 系列）
     description: 在 RHEL、Rocky Linux、AlmaLinux、Fedora 等 Red Hat 系列发行版上安装 PostgreSQL。
     lang: zh
   /download/linux/suse/:
-    title: PostgreSQL：SUSE 下载｜RPM 仓库与安装
+    title: PostgreSQL SUSE 下载｜RPM 仓库与安装
     description: 在 SUSE Linux Enterprise Server 与 openSUSE 上通过 RPM 仓库或系统包安装 PostgreSQL。
     lang: zh
   /download/linux/ubuntu/:
-    title: PostgreSQL：Ubuntu 下载｜APT 仓库与安装
+    title: PostgreSQL Ubuntu 下载｜APT 仓库与安装
     description: 在 Ubuntu 上通过系统软件包或 PostgreSQL APT 仓库安装受支持的 PostgreSQL 版本。
     lang: zh
   /download/macosx/:
-    title: PostgreSQL：macOS 安装包｜Postgres.app、Homebrew 等
+    title: PostgreSQL macOS 安装包｜Postgres.app、Homebrew 等
     description: 获取适用于 macOS 的 PostgreSQL 安装程序，并了解 Postgres.app、Homebrew、MacPorts 等安装方式。
     lang: zh
   /download/netbsd/:
-    title: PostgreSQL：NetBSD 安装包
+    title: PostgreSQL NetBSD 安装包
     description: 通过 NetBSD Packages Collection（pkgsrc）获取 PostgreSQL 安装包。
     lang: zh
   /download/openbsd/:
-    title: PostgreSQL：OpenBSD 安装包
+    title: PostgreSQL OpenBSD 安装包
     description: 通过 OpenBSD Ports 与 Packages Collection 获取 PostgreSQL 安装包。
     lang: zh
   /download/product-categories/:
-    title: PostgreSQL：软件目录｜驱动、扩展与工具
+    title: PostgreSQL 软件目录｜驱动、扩展与工具
     description: 浏览 PostgreSQL 相关驱动、扩展、接口和工具的软件目录。
     lang: zh
   /download/snapshots/:
-    title: PostgreSQL：Beta、RC 与开发快照下载
+    title: PostgreSQL Beta、RC 与开发快照下载
     description: 下载 PostgreSQL Beta、候选发布版和开发快照，用于测试新版本，不适合生产环境。
     lang: zh
   /download/solaris/:
-    title: PostgreSQL：Solaris｜源码构建与测试状态
+    title: PostgreSQL Solaris｜源码构建与测试状态
     description: 了解在 Solaris 上从源代码构建 PostgreSQL 的方式及社区构建测试状态。
     lang: zh
   /download/windows/:
-    title: PostgreSQL：Windows 安装程序
+    title: PostgreSQL Windows 安装程序
     description: 获取由 EDB 提供的 PostgreSQL Windows 安装程序及安装说明。
     lang: zh
   /support/:
-    title: PostgreSQL：支持｜文档、社区与专业服务
+    title: PostgreSQL 支持｜文档、社区与专业服务
     description: 获取 PostgreSQL 文档、社区交流、专业服务、托管方案和 Bug 报告入口。
     lang: zh
   /support/professional_hosting/:
-    title: PostgreSQL：托管服务
+    title: PostgreSQL 托管服务
     description: 按地区查找提供 PostgreSQL 托管方案及相关支持服务的公司。
     lang: zh
   /support/professional_support/:
-    title: PostgreSQL：专业支持服务
+    title: PostgreSQL 专业支持服务
     description: 按地区查找为 PostgreSQL 社区提供商业支持和专业服务的公司。
     lang: zh
   /support/security/faq/2013-04-04/:
@@ -4306,30 +4303,30 @@
       and CVE-2013-1899.
     lang: en
   /support/versioning/:
-    title: PostgreSQL：版本策略与升级指南
+    title: PostgreSQL 版本策略与升级指南
     description: 了解 PostgreSQL 版本编号、支持周期、主版本和小版本升级方式。
     lang: zh
   /about/pgcenter/:
-    title: PostgreSQL：关于 pg.center
+    title: PostgreSQL 关于 pg.center
     description: 了解 pg.center 中文站的内容来源、Pigsty 维护方式、译文许可、署名说明与参与渠道。
     lang: zh
   /about/featurematrix/:
-    title: PostgreSQL：特性矩阵与版本支持对照
+    title: PostgreSQL 特性矩阵与版本支持对照
     description: 按功能类别和 PostgreSQL 版本对照各项特性的支持情况，查看特性引入版本、详细说明与文档链接。
     lang: zh
   /about/sponsors/:
-    title: PostgreSQL：项目赞助者
+    title: PostgreSQL 项目赞助者
     description: 了解支持 PostgreSQL 项目的贡献赞助者、财务赞助者与服务器赞助者，以及各类赞助的支持方式。
     lang: zh
   /about/contributing/:
-    title: PostgreSQL：贡献赞助者
+    title: PostgreSQL 贡献赞助者
     description: 查看投入员工时间支持 PostgreSQL 开发、社区和生态项目的企业与组织，以及贡献赞助的认可说明。
     lang: zh
   /about/financial/:
-    title: PostgreSQL：财务赞助者
+    title: PostgreSQL 财务赞助者
     description: 了解向 PostgreSQL 认可的非营利组织捐款的财务赞助者，以及 PostgreSQL 社区协会的赞助等级和记录。
     lang: zh
   /about/servers/:
-    title: PostgreSQL：服务器赞助者
+    title: PostgreSQL 服务器赞助者
     description: 查看为 PostgreSQL 官方网站提供服务器、托管和 CDN 服务的赞助者，以及相关基础设施信息。
     lang: zh
--- a/pgweb/account/forms.py
+++ b/pgweb/account/forms.py
@@ -22,22 +22,22 @@
     if not re.match(r'^[a-z0-9\.-]+$', username):
         # XXX: Note! Should we ever allow @ signs in usernames again, we need to also
         #      update util/auth.py and the code for identifying email addresses.
-        raise forms.ValidationError("Invalid character in user name. Only a-z, 0-9, . and - allowed for compatibility with third party software.")
+        raise forms.ValidationError("用户名包含无效字符。为兼容第三方软件，只能使用 a-z、0-9、点号（.）和连字符（-）。")
     try:
         User.objects.get(username=username)
     except User.DoesNotExist:
         return username
-    raise forms.ValidationError("This username is already in use")
+    raise forms.ValidationError("此用户名已被使用。")
 
 
 def _clean_email(email):
     email = email.lower()
 
     if User.objects.filter(email=email).exists():
-        raise forms.ValidationError("A user with this email address is already registered")
+        raise forms.ValidationError("此邮箱地址已注册。")
 
     if SecondaryEmail.objects.filter(email=email).exists():
-        raise forms.ValidationError("This email address is already attached to a different user")
+        raise forms.ValidationError("此邮箱地址已关联其他用户。")
 
     return email
 
@@ -52,38 +52,38 @@
                 # This is *probably* a user trying to log in with an account that has not
                 # been set up properly yet. It could be an actually unsupported hashing
                 # algorithm, but we'll deal with that when we get there.
-                self._errors["__all__"] = self.error_class(["This account appears not to be properly initialized. Make sure you complete the signup process with the instructions in the email received before trying to use the account."])
+                self._errors["__all__"] = self.error_class(["此账户似乎尚未完成初始化。请先按照注册邮件中的说明完成注册，再尝试登录。"])
                 log.warning("User {0} tried to log in with invalid hash, probably because signup was completed.".format(self.cleaned_data['username']))
                 return self.cleaned_data
             raise e
 
 
 class CommunityAuthConsentForm(forms.Form):
-    consent = forms.BooleanField(help_text='Consent to sharing this data')
+    consent = forms.BooleanField(help_text='同意共享上述信息')
     next = forms.CharField(widget=forms.widgets.HiddenInput())
 
     def __init__(self, orgname, *args, **kwargs):
         self.orgname = orgname
         super(CommunityAuthConsentForm, self).__init__(*args, **kwargs)
 
-        self.fields['consent'].label = 'Consent to sharing data with {0}'.format(self.orgname)
+        self.fields['consent'].label = '同意向 {0} 提供上述信息'.format(self.orgname)
 
     def clean(self):
         cleaned_data = super().clean()
         if 'next' not in cleaned_data:
-            self.add_error(None, "Next URL must be set")
+            self.add_error(None, "缺少登录后的跳转地址。")
         if not cleaned_data['next'].startswith('/'):
-            self.add_error(None, "Invalid next url")
+            self.add_error(None, "登录后的跳转地址无效。")
         return cleaned_data
 
 
 class SignupForm(forms.Form):
-    username = forms.CharField(max_length=30)
-    first_name = forms.CharField(max_length=30)
-    last_name = forms.CharField(max_length=30)
-    email = forms.EmailField()
-    email2 = forms.EmailField(label="Repeat email")
-    captcha = ReCaptchaField()
+    username = forms.CharField(label='用户名', max_length=30)
+    first_name = forms.CharField(label='名字', max_length=30)
+    last_name = forms.CharField(label='姓氏', max_length=30)
+    email = forms.EmailField(label='邮箱地址')
+    email2 = forms.EmailField(label='再次输入邮箱地址')
+    captcha = ReCaptchaField(label='验证码')
 
     def __init__(self, remoteip, *args, **kwargs):
         super(SignupForm, self).__init__(*args, **kwargs)
@@ -98,7 +98,7 @@
         email2 = self.cleaned_data['email2'].lower()
 
         if email1 != email2:
-            raise forms.ValidationError("Email addresses don't match")
+            raise forms.ValidationError("两次输入的邮箱地址不一致。")
         return email2
 
     def clean_username(self):
@@ -109,11 +109,11 @@
 
 
 class SignupOauthForm(forms.Form):
-    username = forms.CharField(max_length=30)
-    first_name = forms.CharField(max_length=30, required=False)
-    last_name = forms.CharField(max_length=30, required=False)
-    email = forms.EmailField()
-    captcha = ReCaptchaField()
+    username = forms.CharField(label='用户名', max_length=30)
+    first_name = forms.CharField(label='名字', max_length=30, required=False)
+    last_name = forms.CharField(label='姓氏', max_length=30, required=False)
+    email = forms.EmailField(label='邮箱地址')
+    captcha = ReCaptchaField(label='验证码')
 
     def __init__(self, *args, **kwargs):
         super(SignupOauthForm, self).__init__(*args, **kwargs)
@@ -135,6 +135,14 @@
     class Meta:
         model = UserProfile
         exclude = ('user',)
+        labels = {
+            'sshkey': 'SSH 公钥',
+            'block_oauth': '禁用 OAuth 登录',
+        }
+        help_texts = {
+            'sshkey': '粘贴 OpenSSH 格式的公钥，每行一个，可填写多个。',
+            'block_oauth': '禁止通过 Google、Microsoft 等 OAuth 提供方登录此账户。',
+        }
 
     def __init__(self, user, *args, **kwargs):
         self.user = user
@@ -149,7 +157,7 @@
 
 
 class UserForm(forms.ModelForm):
-    primaryemail = forms.ChoiceField(choices=[], required=True, label='Primary email address')
+    primaryemail = forms.ChoiceField(choices=[], required=True, label='主要邮箱地址')
 
     def __init__(self, can_change_email, secondaryaddresses, *args, **kwargs):
         super(UserForm, self).__init__(*args, **kwargs)
@@ -158,16 +166,20 @@
         if can_change_email:
             self.fields['primaryemail'].choices = [(self.instance.email, self.instance.email), ] + [(a.email, a.email) for a in secondaryaddresses if a.confirmed]
             if not secondaryaddresses:
-                self.fields['primaryemail'].help_text = "To change the primary email address, first add it as a secondary address below"
+                self.fields['primaryemail'].help_text = "如需更改主要邮箱地址，请先在下方将新地址添加为备用邮箱。"
         else:
             self.fields['primaryemail'].choices = [(self.instance.email, self.instance.email), ]
-            self.fields['primaryemail'].help_text = "You cannot change the primary email of this account since it is connected to an external authentication system"
+            self.fields['primaryemail'].help_text = "此账户关联了外部认证系统，无法在这里更改主要邮箱地址。"
             self.fields['primaryemail'].widget.attrs['disabled'] = True
             self.fields['primaryemail'].required = False
 
     class Meta:
         model = User
         fields = ('primaryemail', 'first_name', 'last_name', )
+        labels = {
+            'first_name': '名字',
+            'last_name': '姓氏',
+        }
 
 
 class ContributorForm(forms.ModelForm):
@@ -177,8 +189,8 @@
 
 
 class AddEmailForm(forms.Form):
-    email1 = forms.EmailField(label="New email", required=False)
-    email2 = forms.EmailField(label="Repeat email", required=False)
+    email1 = forms.EmailField(label='新邮箱地址', required=False)
+    email2 = forms.EmailField(label='再次输入邮箱地址', required=False)
 
     def __init__(self, user, *args, **kwargs):
         super().__init__(*args, **kwargs)
@@ -188,17 +200,17 @@
         email = self.cleaned_data['email1'].lower()
 
         if email == self.user.email:
-            raise forms.ValidationError("This is your existing email address!")
+            raise forms.ValidationError("这已经是您当前的邮箱地址。")
 
         if User.objects.filter(email=email).exists():
-            raise forms.ValidationError("A user with this email address is already registered")
+            raise forms.ValidationError("此邮箱地址已注册。")
 
         try:
             s = SecondaryEmail.objects.get(email=email)
             if s.user == self.user:
-                raise forms.ValidationError("This email address is already connected to your account")
+                raise forms.ValidationError("此邮箱地址已关联您的账户。")
             else:
-                raise forms.ValidationError("A user with this email address is already registered")
+                raise forms.ValidationError("此邮箱地址已注册。")
         except SecondaryEmail.DoesNotExist:
             pass
 
@@ -213,17 +225,17 @@
         email2 = self.cleaned_data['email2'].lower()
 
         if email1 != email2:
-            raise forms.ValidationError("Email addresses don't match")
+            raise forms.ValidationError("两次输入的邮箱地址不一致。")
         return email2
 
 
 class PgwebPasswordResetForm(forms.Form):
-    email = forms.EmailField()
+    email = forms.EmailField(label='邮箱地址')
 
 
 class ConfirmSubmitForm(forms.Form):
-    confirm = forms.BooleanField(required=True, help_text='Confirm')
+    confirm = forms.BooleanField(label='确认', required=True, help_text='Confirm')
 
     def __init__(self, objtype, *args, **kwargs):
         super().__init__(*args, **kwargs)
-        self.fields['confirm'].help_text = 'Confirm that you are ready to submit this {}.'.format(objtype)
+        self.fields['confirm'].help_text = '确认提交这条{}进行审核。'.format(objtype)
--- a/pgweb/account/views.py
+++ b/pgweb/account/views.py
@@ -418,7 +418,7 @@
 
 def changepwd(request):
     if hasattr(request.user, 'password') and request.user.password == OAUTH_PASSWORD_STORE:
-        return HttpSimpleResponse(request, "Account error", "This account cannot change password as it's connected to a third party login site.")
+        return HttpSimpleResponse(request, "账户错误", "此账户使用第三方登录，无法在这里修改密码。")
 
     log.info("Initiating password change from {0}".format(get_client_ip(request)))
     return authviews.PasswordChangeView.as_view(template_name='account/password_change.html',
@@ -435,7 +435,7 @@
                 return HttpResponse("Email must be specified", status=400)
             u = User.objects.get(email__iexact=request.POST['email'])
             if u.password == OAUTH_PASSWORD_STORE:
-                return HttpSimpleResponse(request, "Account error", "This account cannot change password as it's connected to a third party login site.")
+                return HttpSimpleResponse(request, "账户错误", "此账户使用第三方登录，无法在这里修改密码。")
         except User.DoesNotExist:
             log.info("Attempting to reset password of {0}, user not found".format(request.POST['email']))
             return HttpResponseRedirect('/account/reset/done/')
@@ -491,7 +491,7 @@
 @frame_sources('https://www.google.com/')
 def signup(request):
     if request.user.is_authenticated:
-        return HttpSimpleResponse(request, "Account error", "You must log out before you can sign up for a new account")
+        return HttpSimpleResponse(request, "账户错误", "请先退出当前账户，再注册新账户。")
 
     if request.method == 'POST':
         # Attempt to create user then, eh?
@@ -528,15 +528,12 @@
 
     return render_pgweb(request, 'account', 'base/form.html', {
         'form': form,
-        'formitemtype': 'Account',
+        'formitemtype': '账户',
         'form_intro': """
-To sign up for a free community account, enter your preferred userid and email address.
-Note that a community account is only needed if you want to submit information - all
-content is available for reading without an account. A confirmation email will be sent
-to the specified address, and once confirmed a password for the new account can be specified.
+填写您希望使用的用户名和邮箱地址，即可注册免费的社区账户。只有提交内容时才需要账户；浏览网站内容无需登录。我们会向您填写的邮箱发送确认邮件，完成确认后即可设置账户密码。
 """,
-        'savebutton': 'Sign up',
-        'operation': 'New',
+        'savebutton': '注册',
+        'operation': '创建',
         'recaptcha': True,
     })
 
@@ -560,11 +557,11 @@
     if 'oauth_email' not in cookiedata \
        or 'oauth_firstname' not in cookiedata \
        or 'oauth_lastname' not in cookiedata:
-        return HttpSimpleResponse(request, "OAuth error", 'Invalid redirect received')
+        return HttpSimpleResponse(request, "OAuth 登录错误", '收到的跳转信息无效。')
 
     # Is this email already on a different account as a secondary one?
     if SecondaryEmail.objects.filter(email=cookiedata['oauth_email'].lower()).exists():
-        return HttpSimpleResponse(request, "OAuth error", 'This email address is already attached to a different account')
+        return HttpSimpleResponse(request, "OAuth 登录错误", '此邮箱地址已关联其他账户。')
 
     if request.method == 'POST':
         # Second stage, so create the account. But verify that the
@@ -795,9 +792,9 @@
 
     return render_pgweb(request, 'account', 'base/form.html', {
         'form': form,
-        'operation': 'Authentication',
-        'form_intro': 'The site you are about to log into is run by {0}. If you choose to proceed with this authentication, your name and email address will be shared with <em>{1}</em>.</p><p>Please confirm that you consent to this sharing.'.format(org.orgname, org.orgname),
-        'savebutton': 'Proceed with login',
+        'operation': '社区认证',
+        'form_intro': '您即将登录的网站由 {0} 运营。继续认证将向 <em>{1}</em> 提供您的姓名和邮箱地址。</p><p>请确认您同意共享这些信息。'.format(org.orgname, org.orgname),
+        'savebutton': '继续登录',
     })
 
 
--- a/pgweb/core/forms.py
+++ b/pgweb/core/forms.py
@@ -12,33 +12,30 @@
 
 
 class OrganisationForm(forms.ModelForm):
-    new_form_intro = """<em>Note!</em> An organisation record is only needed to post news, events,
-products or professional services. In particular, it is <em>not</em> necessary to register an
-organisation in order to ask questions or otherwise participate on the PostgreSQL mailing lists, file a bug
-report, or otherwise interact with the community."""
-
-    remove_email = forms.ModelMultipleChoiceField(required=False, queryset=None, label="Current email addresses", help_text="Select one or more email addresses to remove")
-    add_email = forms.EmailField(required=False, help_text="Enter an email address to add")
-    remove_manager = forms.ModelMultipleChoiceField(required=False, queryset=None, label="Current manager(s)", help_text="Select one or more managers to remove")
-    add_manager = forms.EmailField(required=False, help_text="Enter an email address of an existing account to add as manager")
+    new_form_intro = """<em>提示：</em>只有发布新闻、活动、产品或专业服务信息时，才需要创建组织资料。在 PostgreSQL 邮件列表提问或参与讨论、提交 Bug 报告，以及其他一般社区交流，都<em>不需要</em>注册组织。"""
+
+    remove_email = forms.ModelMultipleChoiceField(required=False, queryset=None, label="当前邮箱地址", help_text="选择要移除的一个或多个邮箱地址")
+    add_email = forms.EmailField(required=False, label="添加邮箱地址", help_text="输入要添加的邮箱地址")
+    remove_manager = forms.ModelMultipleChoiceField(required=False, queryset=None, label="当前管理员", help_text="选择要移除的一个或多个管理员")
+    add_manager = forms.EmailField(required=False, label="添加管理员", help_text="输入已注册账户的邮箱地址，将其添加为管理员")
 
     fieldsets = [
         {
             'id': 'general',
-            'legend': 'General',
+            'legend': '基本信息',
             'description': '',
             'fields': ['name', 'address', 'url', 'orgtype', ],
         },
         {
             'id': 'managers',
-            'legend': 'Managers',
-            'description': 'Managers are the accounts that can use and modify this organisation. To add a manager they must have an existing account.',
+            'legend': '管理员',
+            'description': '管理员可以使用和修改此组织的资料。添加管理员前，对方需要先注册账户。',
             'fields': ['remove_manager', 'add_manager'],
         },
         {
             'id': 'emails',
-            'legend': 'E-mail addresses',
-            'description': 'E-mail addresses registered here can be used to post news. If no news will be posted, there is no need to register one or more email addresses.',
+            'legend': '邮箱地址',
+            'description': '在此登记的邮箱地址可用于发布新闻。如不发布新闻，则无需添加邮箱地址。',
             'fields': ['remove_email', 'add_email'],
         },
     ]
@@ -46,6 +43,12 @@
     class Meta:
         model = Organisation
         exclude = ('lastconfirmed', 'approved', 'managers', 'mailtemplate', 'fromnameoverride')
+        labels = {
+            'name': '组织名称',
+            'address': '地址',
+            'url': '网站地址',
+            'orgtype': '组织类型',
+        }
 
     def __init__(self, *args, **kwargs):
         super(OrganisationForm, self).__init__(*args, **kwargs)
@@ -69,7 +72,7 @@
     def clean_add_email(self):
         if self.cleaned_data['add_email']:
             if OrganisationEmail.objects.filter(org=self.instance, address=self.cleaned_data['add_email'].lower()).exists():
-                raise ValidationError("This email is already registered for your organisation.")
+                raise ValidationError("此邮箱地址已登记在您的组织下。")
         return self.cleaned_data['add_email']
 
     def clean_add_manager(self):
@@ -78,7 +81,7 @@
             try:
                 User.objects.get(email=self.cleaned_data['add_manager'].lower())
             except User.DoesNotExist:
-                raise ValidationError("User with email %s not found" % self.cleaned_data['add_manager'])
+                raise ValidationError("未找到使用邮箱 %s 的用户。" % self.cleaned_data['add_manager'])
 
         return self.cleaned_data['add_manager']
 
@@ -90,14 +93,14 @@
                     removecount += 1
 
             if len(self.instance.managers.all()) - removecount <= 0:
-                raise ValidationError("Cannot remove all managers from an organsation!")
+                raise ValidationError("组织必须至少保留一位管理员。")
         return self.cleaned_data['remove_manager']
 
     def clean_remove_email(self):
         if self.cleaned_data['remove_email']:
             for e in self.cleaned_data['remove_email']:
                 if e.newsarticle_set.exists():
-                    raise ValidationError("Cannot remove an email address that has been used to post news articles. Please contact webmaster@postgresql.org to have this removed.")
+                    raise ValidationError("无法移除曾用于发布新闻的邮箱地址。如需移除，请联系 webmaster@postgresql.org。")
         return self.cleaned_data['remove_email']
 
     def save(self, commit=True):
--- a/pgweb/core/templatetags/pgfilters.py
+++ b/pgweb/core/templatetags/pgfilters.py
@@ -45,7 +45,7 @@
     'about': (('/about/',), ()),
     'download': (('/download/', '/ftp/'), ()),
     'docs': (('/docs/',), ()),
-    'ext': (('/ext/',), ()),
+    'ext': (('/ext/', '/e/'), ()),
     'community': (('/community/',), ()),
     'developer': (('/developer/',), ()),
     'support': (('/support/',), ()),
@@ -237,6 +237,9 @@
 
 @register.simple_tag(takes_context=True)
 def git_changes_link(context):
+    language = (context.get('seo') or {}).get('lang', 'zh')
+    if language.startswith('zh'):
+        return mark_safe('<a href="https://git.postgresql.org/gitweb/?p=pgweb.git;a=history;f=templates/{}">查看修订历史</a>。'.format(context.template_name))
     return mark_safe('<a href="https://git.postgresql.org/gitweb/?p=pgweb.git;a=history;f=templates/{}">View</a> change history.'.format(context.template_name))
 
 
--- a/pgweb/core/templatetags/pgseo.py
+++ b/pgweb/core/templatetags/pgseo.py
@@ -10,8 +10,13 @@
     # truncation cannot split an escaped entity such as &quot; in the middle.
     seo = context.get('seo') or {}
     og = context.get('og') or {}
+    title = seo.get('title')
+    if not title:
+        title = og.get('title') or ''
+        if title and not title.startswith('PostgreSQL'):
+            title = 'PostgreSQL ' + title
     return {
         'description': seo.get('description') or og.get('description') or '',
         'canonical': seo.get('canonical') or og.get('url') or '',
-        'title': seo.get('title') or og.get('title') or '',
+        'title': title,
     }
--- a/pgweb/core/test_home_seo.py
+++ b/pgweb/core/test_home_seo.py
@@ -59,7 +59,7 @@
         self.assertEqual(head.values('meta', 'content', name='twitter:title'), [title])
         descriptions = head.values('meta', 'content', name='description')
         self.assertEqual(descriptions, [
-            'PostgreSQL 官方网站中文翻译，由 Pigsty 维护的中文文档，信息资讯，软件目录，与知识库',
+            'pg.center 是由 Pigsty 团队维护的 PostgreSQL 官方网站中文翻译站，提供中文文档、技术资讯、软件目录与知识库。',
         ])
         self.assertNotIn('官方网站中文版', html)
         self.assertEqual(head.values('meta', 'content', property='og:description'), descriptions)
@@ -96,7 +96,7 @@
 
     def test_existing_hero_and_dismissible_notice_remain(self):
         response = self.client.get('/')
-        self.assertContains(response, '<h1 class="pg-hero__title">PostgreSQL：世界上最先进的开源关系型数据库</h1>', count=1)
+        self.assertContains(response, '<h1 class="pg-hero__title">PostgreSQL 世界上最先进的开源关系型数据库</h1>', count=1)
         self.assertContains(response, 'data-pg-shout-close')
         self.assertContains(response, 'class="pg-hero__chip"')
         self.assertContains(response, 'href="/about/news/release-3365/"')
--- a/pgweb/core/test_seo.py
+++ b/pgweb/core/test_seo.py
@@ -6,6 +6,7 @@
 from django.http import Http404
 from django.template.loader import render_to_string
 from django.test import RequestFactory, SimpleTestCase
+from django.utils.html import escape
 
 from pgweb.core.struct import get_struct
 from pgweb.core.views import fallback, robots
@@ -60,6 +61,25 @@
 
 
 class PublicMetadataTests(SimpleTestCase):
+    def test_html_and_sharing_titles_use_one_brand_and_preserve_explicit_metadata(self):
+        cases = (
+            ({'og': {'title': '特性：全文检索'}}, 'PostgreSQL 特性：全文检索'),
+            ({'og': {'title': 'PostgreSQL 18 发布'}}, 'PostgreSQL 18 发布'),
+            ({'og': {'title': 'SQL UPDATE 2026'}}, 'PostgreSQL SQL UPDATE 2026'),
+            ({'og': {'title': '专业服务 - 亚洲'}}, 'PostgreSQL 专业服务 - 亚洲'),
+            ({'og': {'title': '测试 "引号" <SQL> & 标题'}}, 'PostgreSQL 测试 "引号" <SQL> & 标题'),
+            ({'seo': {'title': 'PostgreSQL 18 文档 · ECPG PREPARE'},
+              'og': {'title': 'PREPARE'}}, 'PostgreSQL 18 文档 · ECPG PREPARE'),
+            ({'seo': {'title': 'PostgreSQL: Documentation', 'lang': 'en'},
+              'og': {'title': 'Documentation'}}, 'PostgreSQL: Documentation'),
+            ({'seo': {'title': 'Planet PostgreSQL 博客收录政策'}}, 'Planet PostgreSQL 博客收录政策'),
+        )
+        for context, expected in cases:
+            with self.subTest(title=expected):
+                source = render_to_string('base/base.html', context)
+                self.assertIn('<title>' + escape(expected) + '</title>', source)
+                self.assertEqual(HeadParser(source).values('meta', 'content', property='og:title'), [expected])
+
     def test_static_and_dynamic_metadata_each_render_once_and_escape_safely(self):
         description = '引号 " 与 SQL a < b & c > d '
         for context in (
--- a/pgweb/core/views.py
+++ b/pgweb/core/views.py
@@ -98,7 +98,7 @@
             'url': '/',
             'type': 'website',
             'title': title,
-            'description': 'PostgreSQL 官方网站中文翻译，由 Pigsty 维护的中文文档，信息资讯，软件目录，与知识库',
+            'description': 'pg.center 是由 Pigsty 团队维护的 PostgreSQL 官方网站中文翻译站，提供中文文档、技术资讯、软件目录与知识库。',
             'sitename': 'PostgreSQL 中文社区',
         },
     })
--- a/pgweb/docs/ecosystem.py
+++ b/pgweb/docs/ecosystem.py
@@ -15,7 +15,7 @@
 
 
 COMPONENTS = {
-    'patroni': {'name': 'Patroni', 'icon': 'fa-heartbeat', 'description': 'PostgreSQL 高可用与自动故障转移'},
+    'patroni': {'name': 'Patroni', 'icon': 'fa-heartbeat', 'description': 'PostgreSQL 高可用与自动故障切换'},
     'pgbouncer': {'name': 'PgBouncer', 'icon': 'fa-exchange-alt', 'description': '轻量级 PostgreSQL 连接池'},
     'pgbackrest': {'name': 'pgBackRest', 'icon': 'fa-database', 'description': '可靠的备份、恢复与归档管理'},
     'pgbadger': {'name': 'pgBadger', 'icon': 'fa-chart-bar', 'description': 'PostgreSQL 日志分析与性能报告'},
@@ -256,6 +256,10 @@
     languages.sort(key=lambda r: r['language'] != 'zh')
     attribution = project_obj.meta.get('import', {}).get('attribution', {})
     title = document.title if document.path else project_obj.name + (' Documentation' if english else ' 文档')
+    if document.path:
+        seo_title = '{} · {} {} · PostgreSQL'.format(title, project_obj.name, version)
+    else:
+        seo_title = '{} {}{} · PostgreSQL'.format(project_obj.name, version, ' Documentation' if english else ' 文档')
     response = render(request, 'docs/ecosystem/page.html', {
         'project': project_obj, 'component': COMPONENTS[project], 'components': library.cards(),
         'revision': revision, 'page': document, 'language': language, 't': labels, 'title': title,
@@ -267,7 +271,7 @@
         'license': revision.meta.get('license') or project_obj.license,
         'notice_url': safe_external(urljoin(project_obj.meta.get('import', {}).get('source_site', ''), attribution.get('notice', ''))),
         'copyright': attribution.get('copyright', ''),
-        'seo': {'title': '{} · {} {} · PostgreSQL'.format(title, project_obj.name, version),
+        'seo': {'title': seo_title,
                 'description': document.description, 'canonical': canonical, 'lang': language, 'alternates': alternates},
     })
     response['xkey'] = 'pgecosystem pgecosystem_' + project
--- a/pgweb/docs/test_docpage_seo.py
+++ b/pgweb/docs/test_docpage_seo.py
@@ -24,11 +24,11 @@
 
         self.assertEqual(
             _doc_page_title(ecpg),
-            'PostgreSQL：文档：18：ECPG DECLARE',
+            'PostgreSQL 18 文档 · ECPG DECLARE',
         )
         self.assertEqual(
             _doc_page_title(sql),
-            'PostgreSQL：文档：18：DECLARE',
+            'PostgreSQL 18 文档 · DECLARE',
         )
 
     @patch('pgweb.docs.views.render', return_value=HttpResponse())
@@ -55,7 +55,7 @@
         docpage(RequestFactory().get('/docs/18/ecpg-sql-prepare.html'), '18', 'ecpg-sql-prepare')
 
         context = render.call_args.args[2]
-        self.assertEqual(context['seo']['title'], 'PostgreSQL：文档：18：ECPG PREPARE')
+        self.assertEqual(context['seo']['title'], 'PostgreSQL 18 文档 · ECPG PREPARE')
         self.assertEqual(context['og']['title'], context['seo']['title'])
         self.assertEqual(context['og']['modified_time'], loaded_at)
         self.assertNotIn('time', context['og'])
@@ -63,10 +63,10 @@
     def test_document_modified_time_is_not_published_time(self):
         source = render_to_string('base/base.html', {
             'link_root': 'https://pg.center',
-            'seo': {'title': 'PostgreSQL：文档：18：ECPG PREPARE'},
+            'seo': {'title': 'PostgreSQL 18 文档 · ECPG PREPARE'},
             'og': {
                 'url': '/docs/18/ecpg-sql-prepare.html',
-                'title': 'PostgreSQL：文档：18：ECPG PREPARE',
+                'title': 'PostgreSQL 18 文档 · ECPG PREPARE',
                 'modified_time': datetime(2026, 9, 8, 12, 34, 56),
             },
         })
--- a/pgweb/docs/views.py
+++ b/pgweb/docs/views.py
@@ -100,7 +100,7 @@
     title = (page.title or '').strip()
     if page.file.startswith('ecpg-sql-') and not title.upper().startswith('ECPG '):
         title = 'ECPG ' + title
-    return 'PostgreSQL：文档：{}：{}'.format(page.display_version(), title)
+    return 'PostgreSQL {} 文档 · {}'.format(page.display_version(), title)
 
 
 def _release_major_label(major):
--- a/pgweb/downloads/views.py
+++ b/pgweb/downloads/views.py
@@ -253,7 +253,7 @@
     return render_pgweb(request, 'download', 'downloads/categorylist.html', {
         'categories': categories,
         'og': {
-            'title': 'PostgreSQL：软件目录与产品分类',
+            'title': 'PostgreSQL 软件目录与产品分类',
             'description': '按类别查找 PostgreSQL 相关的接口、扩展和软件，了解产品介绍、许可证与发行商。此目录不构成 PostgreSQL 全球开发组的推荐或背书。',
             'url': '/download/product-categories/',
         },
@@ -268,7 +268,7 @@
         'products': products,
         'productcount': len(products),
         'og': {
-            'title': 'PostgreSQL：软件目录：{}'.format(category.catname),
+            'title': 'PostgreSQL 软件目录：{}'.format(category.catname),
             'description': summarize_html(category.blurb) or 'PostgreSQL {}软件目录，列出相关产品、许可证、价格和发行商信息。'.format(category.catname),
             'url': '/download/products/{}/'.format(category.pk),
         },
--- a/pgweb/events/forms.py
+++ b/pgweb/events/forms.py
@@ -6,7 +6,7 @@
 
 
 class EventForm(forms.ModelForm):
-    form_intro = 'Before submitting an event, please read the <a href="/about/policies/news-and-events/">current policy</a> for News and Events'
+    form_intro = '提交活动前，请先阅读现行的<a href="/about/policies/news-and-events/">新闻与活动收录政策</a>。'
 
     toggle_fields = [
         {
@@ -18,8 +18,8 @@
 
     def __init__(self, *args, **kwargs):
         super(EventForm, self).__init__(*args, **kwargs)
-        self.fields['startdate'].help_text = 'Use format YYYY-MM-DD'
-        self.fields['enddate'].help_text = 'Use format YYYY-MM-DD'
+        self.fields['startdate'].help_text = '请使用 YYYY-MM-DD 格式'
+        self.fields['enddate'].help_text = '请使用 YYYY-MM-DD 格式'
 
     def filter_by_user(self, user):
         self.fields['org'].queryset = Organisation.objects.filter(managers=user, approved=True)
@@ -30,27 +30,48 @@
             # Non online events require city and country
             # (we don't require state, since many countries have no such thing)
             if not cleaned_data.get('city'):
-                self._errors['city'] = self.error_class(['City must be specified for non-online events'])
+                self._errors['city'] = self.error_class(['线下活动必须填写城市。'])
                 del cleaned_data['city']
             if not cleaned_data.get('country'):
-                self._errors['country'] = self.error_class(['Country must be specified for non-online events'])
+                self._errors['country'] = self.error_class(['线下活动必须填写国家。'])
                 del cleaned_data['country']
         return cleaned_data
 
     def clean_startdate(self):
         if self.instance.pk and self.instance.approved:
             if self.cleaned_data['startdate'] != self.instance.startdate:
-                raise ValidationError("You cannot change the dates on events that have been approved")
+                raise ValidationError("已获批准的活动不能更改日期。")
         return self.cleaned_data['startdate']
 
     def clean_enddate(self):
         if self.instance.pk and self.instance.approved:
             if self.cleaned_data['enddate'] != self.instance.enddate:
-                raise ValidationError("You cannot change the dates on events that have been approved")
+                raise ValidationError("已获批准的活动不能更改日期。")
         if 'startdate' in self.cleaned_data and self.cleaned_data['enddate'] < self.cleaned_data['startdate']:
-            raise ValidationError("End date cannot be before start date!")
+            raise ValidationError("结束日期不能早于开始日期。")
         return self.cleaned_data['enddate']
 
     class Meta:
         model = Event
         exclude = ('submitter', 'approved', 'description_for_badged')
+        labels = {
+            'org': '组织',
+            'title': '标题',
+            'isonline': '线上活动',
+            'city': '城市',
+            'state': '州／省',
+            'country': '国家',
+            'language': '活动语言',
+            'badged': '社区活动',
+            'startdate': '开始日期',
+            'enddate': '结束日期',
+            'summary': '摘要',
+            'details': '详细信息',
+        }
+        help_texts = {
+            'org': '如果这里没有可选组织，请查看<a href="/account/orglist/">组织列表</a>并联系组织管理员；如未列出管理员，请联系 <a href="mailto:webmaster@postgresql.org">webmaster@postgresql.org</a>。如果您的组织尚未登记，可以<a href="/account/edit/organisations/">创建组织资料</a>。',
+            'language': '填写活动的主要语言；如使用多种语言，请在活动说明中注明。',
+            'badged': '如果活动符合<a href="/about/policies/conferences/" target="_blank" rel="noopener">社区活动准则</a>并获社区认可，请勾选“社区活动”。',
+            'summary': '简要介绍，显示在活动列表页。',
+            'details': '完整的活动说明。',
+        }
--- a/pgweb/ext/views.py
+++ b/pgweb/ext/views.py
@@ -1,139 +1,327 @@
+from collections import Counter
 from urllib.parse import quote
 
 from django.core.paginator import Paginator
 from django.db import connection
-from django.http import Http404
+from django.http import Http404, HttpResponsePermanentRedirect
 from django.shortcuts import render
 from django.views.decorators.http import require_safe
 from pgweb.util.decorators import queryparams
 
-from .catalog import CATEGORIES, DIMENSIONS, FIELDS, catalog, facets, localized, present, safe_url, url, value_label
+from .catalog import (CATEGORIES, DIMENSIONS, LEGACY_DIMENSIONS, PAGE_SIZE, PGEXT, QUERY_PARAMS, SORTS, VIEWS,
+                      browse_url, catalog, category_blurb, category_color, category_label, detail_url, dimension_url,
+                      facets, localized, present, safe_url, short_number, sql_readout, value_color, value_label, with_lang)
 from .markdown import render_document
 
 
-def context(request, rows):
-    language = 'en' if request.GET.get('lang') == 'en' else 'zh'
+LABELS = {
+    'catalog': ('扩展目录', 'Extension Catalog'), 'all': ('全部扩展', 'All extensions'),
+    'indexes': ('索引', 'Indexes'), 'list_all': ('全部索引', 'All indexes'),
+    'search': ('搜索', 'Search'), 'sidebar_search': ('搜索扩展…', 'Search extensions…'),
+    'categories': ('功能分类', 'Categories'), 'license': ('许可证', 'License'),
+    'language': ('编程语言', 'Language'), 'repo': ('仓库来源', 'Repository'),
+    'any': ('不限', 'Any'), 'sort': ('排序', 'Sort'),
+    'sort_stars': ('星标', 'Stars'), 'sort_name': ('名称', 'Name'), 'sort_recent': ('最近活跃', 'Recently active'),
+    'view': ('视图', 'View'), 'view_table': ('表格', 'Table'), 'view_card': ('卡片', 'Cards'),
+    'results': ('个匹配扩展', 'matching extensions'), 'active': ('当前筛选', 'Active filters'),
+    'clear': ('清除筛选', 'Clear filters'), 'query': ('关键词', 'Query'),
+    'empty': ('没有找到匹配的扩展', 'No matching extensions'),
+    'empty_help': ('试试更短的关键词，或移除部分筛选条件。', 'Try a shorter query or remove a filter.'),
+    'previous': ('上一页', 'Previous'), 'next': ('下一页', 'Next'), 'page': ('页', 'Page'),
+    'col_name': ('名称', 'Name'), 'col_version': ('版本', 'Version'), 'col_desc': ('简介', 'Description'),
+    'col_category': ('分类', 'Category'), 'col_license': ('许可证', 'License'), 'col_lang': ('语言', 'Language'),
+    'col_pg': ('PG 版本', 'PG'), 'col_stars': ('星标', 'Stars'), 'col_count': ('扩展数', 'Extensions'),
+    'col_share': ('占比', 'Share'), 'col_examples': ('代表扩展', 'Examples'), 'col_value': ('取值', 'Value'),
+    'overview': ('概览', 'Overview'), 'documentation': ('使用文档', 'Documentation'),
+    'related': ('相关扩展', 'Related extensions'), 'requires': ('依赖扩展', 'Requires'),
+    'required_by': ('被依赖', 'Required by'), 'see_also': ('参见', 'See also'), 'family': ('同项目扩展', 'Same package'),
+    'version': ('版本', 'Version'), 'package': ('扩展包', 'Package'), 'compatibility': ('PostgreSQL 版本', 'PostgreSQL versions'),
+    'kind': ('扩展形态', 'Kind'), 'lifecycle': ('上游状态', 'Upstream status'), 'kernel': ('适用内核', 'Kernel'),
+    'vendor': ('厂商', 'Vendor'), 'need_ddl': ('需要 CREATE EXTENSION', 'Needs CREATE EXTENSION'),
+    'need_load': ('需要预加载', 'Needs preload'), 'trusted': ('可信扩展', 'Trusted'), 'relocatable': ('可迁移', 'Relocatable'),
+    'libs': ('共享库', 'Libraries'), 'schemas': ('Schema', 'Schemas'), 'maintained': ('目录更新', 'Catalog updated'),
+    'stars': ('星标', 'Stars'), 'last_commit': ('最近提交', 'Last commit'), 'last_release': ('最近发布', 'Last release'),
+    'links': ('上游链接', 'Upstream links'), 'source': ('源码仓库', 'Source repository'), 'home': ('项目主页', 'Homepage'),
+    'docs': ('官方文档', 'Documentation'), 'license_text': ('许可证原文', 'License text'),
+    'control': ('Control 文件', 'Control file'), 'pgxn': ('PGXN', 'PGXN'), 'pgext_page': ('pgext.cloud', 'pgext.cloud'),
+    'contents': ('本页目录', 'On this page'), 'yes': ('是', 'Yes'), 'no': ('否', 'No'),
+    'document_empty': ('尚无使用文档', 'Documentation is not available yet'),
+    'doc_fallback': ('当前语言的文档尚未收录，以下显示另一语言版本。',
+                     'Documentation in this language is unavailable; the other language is shown below.'),
+    'repo_note': ('来源标签由目录记录提供，不代表特定平台的安装包可用性。',
+                  'Repository labels describe catalog provenance, not binary availability on a specific platform.'),
+    'hero_eyebrow': ('pgext.cloud · PostgreSQL 的一切可能', 'pgext.cloud · everything PostgreSQL can become'),
+    'hero_title': ('PostgreSQL 扩展目录', 'PostgreSQL Extension Catalog'),
+    'field_note': ('一格一个扩展，颜色即分类；实心为已打包，浅色为仅源码。',
+                   'One square per extension, coloured by category; solid squares are packaged, faded ones are source-only.'),
+    'index_intro': ('从不同角度浏览扩展生态。点击任意一行，查看该条目下的全部扩展。',
+                    'Browse the ecosystem from different angles. Pick a row to list its extensions.'),
+    'source_note': ('数据来自 PGEXT 元数据库', 'Data from the PGEXT catalog'),
+    'snapshot': ('目录快照', 'Snapshot'), 'bilingual': ('中英文文档', 'Bilingual docs'),
+    'packaged': ('已打包', 'Packaged'), 'extensions': ('个扩展', 'extensions'),
+    'copy_sql': ('点击复制这条查询', 'Click to copy this query'), 'copied': ('已复制', 'Copied'),
+    'skip': ('跳到正文', 'Skip to content'), 'menu': ('目录与筛选', 'Catalog menu'),
+}
+PROPERTY_LABELS = {
+    'kind': {'standard': ('标准扩展', 'Standard'), 'preload': ('预加载扩展', 'Preloaded'),
+             'puresql': ('纯 SQL', 'Pure SQL'), 'headless': ('无 DDL 扩展', 'No extension DDL')},
+    'lifecycle': {'active': ('活跃', 'Active'), 'abandoned': ('停止维护', 'Abandoned'), 'archived': ('已归档', 'Archived'),
+                  'deprecated': ('已弃用', 'Deprecated'), 'preview': ('预览', 'Preview')},
+}
+
+
+def ui_language(request):
+    return 'en' if request.GET.get('lang') == 'en' else 'zh'
+
+
+def intcomma(value):
+    return '{:,}'.format(value or 0)
+
+
+def context(request, rows, language, selected=None, dimension=None):
+    """Sidebar, language switch and labels shared by every catalogue page."""
+    selected = selected or {}
     all_facets = facets(rows, language)
-    labels = {
-        'catalog': ('扩展目录', 'Extensions'), 'all': ('全部扩展', 'All extensions'),
-        'indexes': ('分类索引', 'Browse indexes'), 'search': ('搜索扩展', 'Search extensions'),
-        'placeholder': ('搜索名称、功能或关键词…', 'Search names, features, or keywords…'),
-        'categories': ('功能分类', 'Categories'), 'language': ('编程语言', 'Language'),
-        'license': ('许可证', 'License'), 'repository': ('仓库来源', 'Repository'),
-        'any': ('不限', 'Any'), 'sort': ('排序', 'Sort'), 'name': ('名称', 'Name'),
-        'stars': ('仓库关注度', 'Repository stars'), 'recent': ('最近更新', 'Recently updated'),
-        'results': ('个扩展', 'extensions'), 'clear': ('清除筛选', 'Clear filters'),
-        'empty': ('没有找到匹配的扩展', 'No matching extensions'),
-        'empty_help': ('试试更短的关键词，或移除部分筛选条件。', 'Try a shorter query or remove a filter.'),
-        'previous': ('上一页', 'Previous'), 'next': ('下一页', 'Next'), 'page': ('页', 'Page'),
-        'browse': ('浏览扩展', 'Explore extensions'), 'overview': ('概览', 'Overview'),
-        'documentation': ('使用文档', 'Documentation'), 'source': ('源码仓库', 'Source repository'),
-        'links': ('上游链接', 'Upstream links'), 'related': ('相关扩展', 'Related extensions'),
-        'requires': ('依赖扩展', 'Dependencies'), 'required_by': ('被依赖', 'Required by'),
-        'family': ('同项目扩展', 'Project extensions'), 'version': ('版本', 'Version'),
-        'compatibility': ('PostgreSQL 版本', 'PostgreSQL versions'), 'kind': ('扩展形态', 'Extension kind'),
-        'lifecycle': ('上游状态', 'Upstream status'), 'maintained': ('目录更新', 'Catalog updated'),
-        'contents': ('本页目录', 'On this page'), 'filters': ('筛选扩展', 'Filter extensions'),
-        'bilingual': ('中英文文档', 'Bilingual docs'), 'document_empty': ('尚无使用文档', 'Documentation is not available yet'),
-        'doc_fallback': ('当前语言的文档尚未收录，以下显示另一语言版本。', 'Documentation in this language is unavailable; the other language is shown below.'),
-        'repo_note': ('来源标签由目录记录提供，不代表特定平台的安装包可用性。', 'Repository labels describe catalog provenance, not binary availability on a specific platform.'),
+    params = {key: value for key, value in request.GET.items() if key in QUERY_PARAMS and key != 'lang'}
+    labels = {key: localized(value, language) for key, value in LABELS.items()}
+    return {
+        'language': language, 't': labels, 'facets': all_facets,
+        'home_url': browse_url(language), 'list_url': with_lang('/ext/list/', language),
+        'total': len(rows), 'total_text': intcomma(len(rows)),
+        'packaged': sum(1 for row in rows if row.get('packaged')),
+        'bilingual': sum(1 for row in rows if row['has_zh'] and row['has_en']),
+        'snapshot': max((row['mtime'] for row in rows if row.get('mtime')), default=None),
+        'sidebar_categories': [{**item, 'active': selected.get('category') == item['value']} for item in all_facets['category']],
+        'index_links': [{'key': key, 'label': localized(spec['label'], language), 'count': len(all_facets[key]),
+                         'url': dimension_url(key, language), 'active': dimension == key} for key, spec in DIMENSIONS.items()],
+        'language_zh_url': with_lang(request.path, 'zh', **params),
+        'language_en_url': with_lang(request.path, 'en', **params),
+        'ext_source_url': PGEXT + '/',
     }
-    params = {key: value for key, value in request.GET.items() if key in ('q', 'sort', 'page', *FIELDS)}
-    params.pop('lang', None)
-    return {
-        'language': language, 't': {key: localized(value, language) for key, value in labels.items()},
-        'home_url': url(ui_lang=language), 'facets': all_facets,
-        'index_links': [{'key': key, 'label': localized(label, language), 'count': len(all_facets[key]),
-                         'href': url('/ext/list/{}/'.format(key), language)} for key, label in DIMENSIONS.items()],
-        'language_zh_url': url(request.path, 'zh', **params),
-        'language_en_url': url(request.path, 'en', **params),
-        'total': len(rows), 'bilingual': sum(row['has_zh'] and row['has_en'] for row in rows),
-        'ext_source_url': 'https://pgext.cloud/',
-    }
-
-
-def seo(request, ctx, title, description):
-    canonical = url(request.path, ctx['language'], **{k: v for k, v in request.GET.items() if k in ('q', 'sort', 'page', *FIELDS)})
+
+
+def seo(request, ctx, title, description, canonical=None, noindex=False):
+    ctx['title'] = title
+    ctx['description'] = description
+    ctx['noindex'] = noindex
     ctx['seo'] = {'title': title + ' · PostgreSQL', 'description': description,
-                  'canonical': canonical, 'lang': ctx['language'],
+                  'canonical': canonical or with_lang(request.path, ctx['language']), 'lang': ctx['language'],
                   'alternates': {'zh': ctx['language_zh_url'], 'en': ctx['language_en_url']}}
 
 
-@require_safe
-@queryparams('lang', 'q', 'category', 'license', 'language', 'repository', 'sort', 'page')
-def browse(request):
+def universe(rows, language):
+    """One square per extension in a fixed 120-column field, grouped by
+    category in canonical order with a one-cell gap between groups."""
+    columns, pitch, size = 120, 8, 6
+    ordered = sorted(rows, key=lambda row: (list(CATEGORIES).index(row['category']) if row['category'] in CATEGORIES else 99,
+                                           -(row['stars'] or 0), row['name']))
+    groups, cell, previous = [], 0, None
+    for row in ordered:
+        if previous is not None and row['category'] != previous:
+            groups[-1]['names'] = ' '.join(groups[-1]['names'])
+            cell += 1
+        if previous != row['category']:
+            groups.append({'code': row['category'], 'label': category_label(row['category'], language),
+                           'color': category_color(row['category']), 'url': browse_url(language, {'category': row['category']}),
+                           'count': 0, 'cells': [], 'names': []})
+        groups[-1]['cells'].append({'x': (cell % columns) * pitch, 'y': (cell // columns) * pitch, 'packaged': row.get('packaged')})
+        groups[-1]['names'].append(row['name'])
+        groups[-1]['count'] += 1
+        previous = row['category']
+        cell += 1
+    if groups and isinstance(groups[-1]['names'], list):
+        groups[-1]['names'] = ' '.join(groups[-1]['names'])
+    height = ((cell + columns - 1) // columns) * pitch
+    return {'groups': groups, 'width': columns * pitch, 'height': max(height, pitch), 'size': size}
+
+
+def selection(request, fixed):
+    selected = {}
+    for key, spec in DIMENSIONS.items():
+        value = fixed.get(key) or request.GET.get(spec['param'], '')
+        if key == 'repo' and not value:
+            value = request.GET.get('repository', '')
+        selected[key] = value.strip()[:100]
+    if selected['category']:
+        selected['category'] = selected['category'].upper()
+    return selected
+
+
+@require_safe
+@queryparams(*QUERY_PARAMS)
+def browse(request, fixed=None):
     rows = catalog()
-    ctx = context(request, rows)
-    language = ctx['language']
-    selected = {key: request.GET.get(key, '') for key in FIELDS}
+    language = ui_language(request)
+    fixed = fixed or {}
+    selected = selection(request, fixed)
     query = request.GET.get('q', '').strip()[:255]
     words = query.casefold().split()
-    matches = [row for row in rows if all(not value or row[FIELDS[key]] == value for key, value in selected.items())
-               and all(word in row['search_text'] for word in words)]
     sort = request.GET.get('sort', 'stars')
-    if sort not in ('stars', 'name', 'recent'):
-        sort = 'stars'
+    sort = sort if sort in SORTS else 'stars'
+    view = request.GET.get('view', 'table')
+    view = view if view in VIEWS else 'table'
+    ctx = context(request, rows, language, selected)
+
+    def matches(row, skip=None):
+        return (all(not value or key == skip or row[DIMENSIONS[key]['field']] == value for key, value in selected.items())
+                and all(word in row['search_text'] for word in words))
+
+    results = [row for row in rows if matches(row)]
     if sort == 'name':
-        matches.sort(key=lambda row: row['name'].casefold())
+        results.sort(key=lambda row: row['name'].casefold())
     elif sort == 'recent':
-        matches.sort(key=lambda row: (-(row['last_active'] or row['mtime']).toordinal(), row['name']))
+        results.sort(key=lambda row: (-(row['last_active'] or row['mtime']).toordinal(), row['name']))
     else:
-        matches.sort(key=lambda row: (-(row['stars'] or 0), row['name']))
+        results.sort(key=lambda row: (-(row['stars'] or 0), row['name']))
     if words:
-        matches.sort(key=lambda row: (row['name'].casefold() != query.casefold(), not row['name'].casefold().startswith(query.casefold())))
-    pager = Paginator(matches, 36)
+        needle = query.casefold()
+        results.sort(key=lambda row: (row['name'].casefold() != needle, not row['name'].casefold().startswith(needle)))
+    pager = Paginator(results, PAGE_SIZE)
     page = pager.get_page(request.GET.get('page'))
-    params = {**selected, 'q': query, 'sort': sort}
+    keep = {'q': query, 'sort': sort, 'view': view}
+
+    dropdowns = []
+    for key, spec in DIMENSIONS.items():
+        counts = Counter(row[spec['field']] for row in rows if matches(row, skip=key))
+        options = []
+        for item in ctx['facets'][key]:
+            count = counts.get(item['value'], 0)
+            if not count and selected[key] != item['value']:
+                continue
+            options.append({**item, 'count': count, 'current': selected[key] == item['value'],
+                            'url': browse_url(language, {**selected, key: item['value']}, **keep)})
+        dropdowns.append({
+            'key': key, 'param': spec['param'], 'label': localized(spec['label'], language), 'options': options,
+            'value': selected[key], 'value_label': value_label(key, selected[key], language) if selected[key] else '',
+            'color': value_color(key, selected[key]) if selected[key] else '',
+            'any_url': browse_url(language, {**selected, key: ''}, **keep), 'index_url': dimension_url(key, language),
+        })
+    active = [{'dimension': localized(DIMENSIONS[key]['label'], language), 'value': value,
+               'label': value_label(key, value, language), 'color': value_color(key, value),
+               'remove_url': browse_url(language, {**selected, key: ''}, **keep)}
+              for key, value in selected.items() if value]
+    sql, sql_html = sql_readout(query, selected, sort)
     ctx.update({
-        'query': query, 'selected': selected, 'sort': sort, 'page': page,
-        'cards': [present(row, language) for row in page],
-        'active_filters': [{'label': value_label(key, value, language)} for key, value in selected.items() if value],
+        'query': query, 'selected': selected, 'sort': sort, 'view': view, 'page': page,
+        'rows': [present(row, language) for row in page], 'dropdowns': dropdowns, 'active_filters': active,
+        'clear_url': browse_url(language, {}, sort=sort, view=view),
+        'query_remove_url': browse_url(language, selected, sort=sort, view=view),
+        'sorts': [{'value': value, 'label': ctx['t']['sort_' + value], 'current': value == sort,
+                   'url': browse_url(language, selected, **{**keep, 'sort': value})} for value in SORTS],
+        'views': [{'value': value, 'label': ctx['t']['view_' + value], 'current': value == view,
+                   'url': browse_url(language, selected, **{**keep, 'view': value, 'page': page.number})} for value in VIEWS],
         'pagination': [{'label': number, 'current': number == page.number,
-                        'href': url(ui_lang=language, **params, page=number) if isinstance(number, int) else ''}
+                        'url': browse_url(language, selected, **keep, page=number) if isinstance(number, int) else ''}
                        for number in pager.get_elided_page_range(page.number, on_each_side=1, on_ends=1)],
-        'previous_url': url(ui_lang=language, **params, page=page.previous_page_number()) if page.has_previous() else '',
-        'next_url': url(ui_lang=language, **params, page=page.next_page_number()) if page.has_next() else '',
+        'previous_url': browse_url(language, selected, **keep, page=page.previous_page_number()) if page.has_previous() else '',
+        'next_url': browse_url(language, selected, **keep, page=page.next_page_number()) if page.has_next() else '',
+        'sql': sql, 'sql_html': sql_html, 'result_count': intcomma(pager.count),
         'is_home': not query and not any(selected.values()),
+        'form_action': '/ext/', 'search_placeholder': localized(
+            ('搜索 {} 个扩展：名称、功能、关键词…', 'Search {} extensions by name, feature or keyword…'), language).format(ctx['total_text']),
     })
-    # One square per catalog extension, grouped by the canonical category order.
-    position = 0
-    universe = []
-    for category in ctx['facets']['category']:
-        points = []
-        for unused in range(category['count']):
-            points.append({'x': (position % 90) * 4, 'y': (position // 90) * 4})
-            position += 1
-        universe.append({**category, 'points': points})
-    ctx['universe'] = universe
-    ctx['universe_height'] = ((len(rows) + 89) // 90) * 4 or 4
-    title = localized(('PostgreSQL 扩展目录', 'PostgreSQL Extension Catalog'), language)
-    description = localized(('检索 PostgreSQL 扩展，按功能、许可证、编程语言和仓库来源浏览，阅读中英文使用文档。',
-                             'Find PostgreSQL extensions by category, license, language, and repository, with English and Chinese documentation.'), language)
-    ctx.update({'title': title, 'description': description})
-    seo(request, ctx, title, description)
+    fixed_key = next(iter(fixed), None)
+    if fixed_key == 'category':
+        code = selected['category']
+        ctx['category'] = {'code': code, 'label': category_label(code, language), 'color': category_color(code),
+                           'blurb': category_blurb(code, language), 'count': sum(1 for row in rows if row['category'] == code)}
+        ctx['ext_source_url'] = PGEXT + '/cate/' + quote(code, safe='')
+        title = '{} {} · {}'.format(code, ctx['category']['label'], ctx['t']['catalog'])
+        description = ctx['category']['blurb']
+    elif fixed_key:
+        label = value_label(fixed_key, selected[fixed_key], language)
+        ctx['value_page'] = {'dimension': localized(DIMENSIONS[fixed_key]['label'], language), 'label': label,
+                             'color': value_color(fixed_key, selected[fixed_key]), 'count': pager.count,
+                             'index_url': dimension_url(fixed_key, language)}
+        ctx['ext_source_url'] = PGEXT + '/{}/{}'.format(DIMENSIONS[fixed_key]['pgext'], quote(selected[fixed_key], safe=''))
+        title = '{} · {} · {}'.format(label, ctx['value_page']['dimension'], ctx['t']['catalog'])
+        description = localized(('{}：{} 个 PostgreSQL 扩展。', '{}: {} PostgreSQL extensions.'), language).format(label, pager.count)
+    else:
+        title = ctx['t']['hero_title']
+        description = localized(('检索 PostgreSQL 扩展，按功能、许可证、编程语言和仓库来源浏览，阅读中英文使用文档。',
+                                 'Find PostgreSQL extensions by category, license, language and repository, with English and Chinese documentation.'), language)
+        if ctx['is_home']:
+            ctx['universe'] = universe(rows, language)
+    if query:
+        title = localized(('搜索 “{}” · {}', 'Search “{}” · {}'), language).format(query, ctx['t']['catalog'])
+    seo(request, ctx, title, description, canonical=browse_url(language, selected, **keep, page=page.number), noindex=bool(query))
     return render(request, 'ext/browse.html', ctx)
 
 
 @require_safe
-@queryparams('lang', 'q')
-def index(request, dimension):
-    if dimension not in DIMENSIONS:
+@queryparams(*QUERY_PARAMS)
+def index(request, dimension=None):
+    rows = catalog()
+    language = ui_language(request)
+    ctx = context(request, rows, language, dimension=dimension)
+    by_stars = sorted(rows, key=lambda row: (-(row['stars'] or 0), row['name']))
+    sections = []
+    for key in ([dimension] if dimension else DIMENSIONS):
+        spec = DIMENSIONS[key]
+        values = []
+        for item in ctx['facets'][key]:
+            examples = [present(row, language) for row in by_stars if row[spec['field']] == item['value']][:3]
+            values.append({**item, 'percent': max(1, round(item['count'] / max(len(rows), 1) * 100)), 'examples': examples,
+                           'blurb': category_blurb(item['value'], language) if key == 'category' else ''})
+        sections.append({'key': key, 'label': localized(spec['label'], language), 'values': values,
+                         'url': dimension_url(key, language), 'note': ctx['t']['repo_note'] if key == 'repo' else ''})
+    ctx.update({'dimension': dimension, 'sections': sections})
+    if dimension:
+        ctx['ext_source_url'] = PGEXT + '/list/' + DIMENSIONS[dimension]['pgext']
+        title = '{} · {}'.format(sections[0]['label'], ctx['t']['catalog'])
+    else:
+        ctx['ext_source_url'] = PGEXT + '/list'
+        title = '{} · {}'.format(ctx['t']['list_all'], ctx['t']['catalog'])
+    seo(request, ctx, title, ctx['t']['index_intro'])
+    return render(request, 'ext/index.html', ctx)
+
+
+@require_safe
+@queryparams(*QUERY_PARAMS)
+def section(request, segment):
+    """/ext/<segment>/: a dimension index, a category, or an old detail URL."""
+    key = LEGACY_DIMENSIONS.get(segment, segment)
+    if key in DIMENSIONS:
+        return index(request, key)
+    code = segment.upper()
+    if code in CATEGORIES:
+        if segment != code.lower():
+            return HttpResponsePermanentRedirect(browse_url(ui_language(request), {'category': code}))
+        return browse(request, {'category': code})
+    if any(row['name'] == segment for row in catalog()):
+        return HttpResponsePermanentRedirect(detail_url(segment, ui_language(request)))
+    raise Http404('Unknown catalogue section')
+
+
+@require_safe
+@queryparams(*QUERY_PARAMS)
+def value(request, dimension, value):
+    """/ext/<dimension>/<value>/: one licence, language or repository."""
+    key = LEGACY_DIMENSIONS.get(dimension, dimension)
+    language = ui_language(request)
+    value = value.rstrip('/')
+    if key == 'category':
+        if value.upper() not in CATEGORIES:
+            raise Http404('Unknown category')
+        return HttpResponsePermanentRedirect(browse_url(language, {'category': value.upper()}))
+    if key not in DIMENSIONS or key == 'category':
+        raise Http404('Unknown catalogue index')
+    known = {row[DIMENSIONS[key]['field']] for row in catalog()}
+    if value not in known:
+        match = next((item for item in known if item.casefold() == value.casefold()), None)
+        if match is None:
+            raise Http404('Unknown value')
+        return HttpResponsePermanentRedirect(browse_url(language, {key: match}))
+    return browse(request, {key: value})
+
+
+@require_safe
+@queryparams(*QUERY_PARAMS)
+def legacy_index(request, dimension):
+    key = LEGACY_DIMENSIONS.get(dimension, dimension)
+    if key not in DIMENSIONS:
         raise Http404('Unknown extension index')
-    rows = catalog()
-    ctx = context(request, rows)
-    language = ctx['language']
-    title = localized(DIMENSIONS[dimension], language)
-    values = ctx['facets'][dimension]
-    query = request.GET.get('q', '').strip()[:255]
-    if query:
-        values = [v for v in values if query.casefold() in (v['label'] + ' ' + v['value']).casefold()]
-    for value in values:
-        value['percent'] = max(1, round(value['count'] / max(len(rows), 1) * 100))
-    ctx.update({'dimension': dimension, 'values': values, 'query': query, 'title': title,
-                'description': localized(('从不同角度浏览扩展生态。选择一个条目，查看对应的全部扩展。',
-                                          'Explore the ecosystem from different angles. Choose an entry to browse its extensions.'), language)})
-    seo(request, ctx, title + ' · ' + ctx['t']['catalog'], ctx['description'])
-    return render(request, 'ext/index.html', ctx)
+    return HttpResponsePermanentRedirect(dimension_url(key, ui_language(request)))
 
 
 @require_safe
@@ -143,62 +331,95 @@
     by_name = {row['name']: row for row in rows}
     if name not in by_name:
         raise Http404('Extension not found')
-    ctx = context(request, rows)
-    language = ctx['language']
+    language = ui_language(request)
+    ctx = context(request, rows, language, {'category': by_name[name]['category']})
     row = by_name[name]
     extension = present(row, language)
+    labels = ctx['t']
     with connection.cursor() as cursor:
         cursor.execute('SELECT en_doc, zh_doc FROM pgext.doc WHERE id = %s AND ext = %s', [row['id'], name])
         pair = cursor.fetchone() or ('', '')
     primary = pair[1 if language == 'zh' else 0]
     fallback = pair[0 if language == 'zh' else 1]
     content, toc = render_document(primary or fallback or '', by_name, language, row.get('doc_url') or extension['upstream'])
+
     links = []
-    for field, label in (
-        ('repo_url', ('源码仓库', 'Source repository')), ('home_url', ('项目主页', 'Homepage')),
-        ('doc_url', ('官方文档', 'Official documentation')), ('license_url', ('许可证原文', 'License text')),
-        ('control_url', ('Control 文件', 'Control file')), ('pgxn_url', ('PGXN', 'PGXN')),
-    ):
+    for field, label in (('repo_url', labels['source']), ('home_url', labels['home']), ('doc_url', labels['docs']),
+                         ('license_url', labels['license_text']), ('control_url', labels['control']), ('pgxn_url', labels['pgxn'])):
         target = safe_url(row.get(field))
-        if target:
-            links.append({'label': localized(label, language), 'url': target})
+        if target and target not in [link['url'] for link in links]:
+            links.append({'label': label, 'url': target})
     if not links and extension['upstream']:
-        links.append({'label': ctx['t']['source'], 'url': extension['upstream']})
+        links.append({'label': labels['source'], 'url': extension['upstream']})
+    pgext_url = PGEXT + '/ext/' + quote(name, safe='')
+    links.append({'label': labels['pgext_page'], 'url': pgext_url})
+
+    def yes_no(flag):
+        return labels['yes'] if flag else labels['no']
+    overview = [(labels['package'], row['pkg'], browse_url(language, {}, q=row['pkg']) if row['pkg'] != name else '')]
+    overview.append((labels['version'], row.get('version') or '—', ''))
+    if row.get('pg_ver'):
+        overview.append((labels['compatibility'], ' · '.join(str(v) for v in row['pg_ver']), ''))
+    overview.append((labels['repo'], extension['repository_label'], extension['repository_href']))
+    for field in ('kind', 'lifecycle'):
+        if row.get(field):
+            overview.append((labels[field], localized(PROPERTY_LABELS[field].get(row[field], (row[field], row[field])), language), ''))
+    for field in ('kernel', 'vendor'):
+        if row.get(field):
+            overview.append((labels[field], row[field], ''))
+    for field in ('need_ddl', 'need_load', 'trusted', 'relocatable'):
+        if row.get(field) is not None:
+            overview.append((labels[field], yes_no(row[field]), ''))
+    for field in ('libs', 'schemas'):
+        if row.get(field):
+            overview.append((labels[field], ', '.join(row[field]), ''))
+    if row.get('stars') is not None:
+        overview.append((labels['stars'], intcomma(row['stars']), extension['upstream']))
+    for field in ('last_commit', 'last_release'):
+        if row.get(field):
+            overview.append((labels[field], row[field].strftime('%Y-%m-%d'), ''))
+    if row.get('mtime'):
+        overview.append((labels['maintained'], row['mtime'].strftime('%Y-%m-%d'), ''))
+
     relationships = []
     for field in ('requires', 'required_by', 'see_also'):
         members = [present(by_name[n], language) for n in row.get(field) or [] if n in by_name and n != name]
         if members:
-            relationships.append({'label': ctx['t'].get(field, ctx['t']['related']), 'members': members})
+            relationships.append({'key': field, 'label': labels[field], 'members': members})
     family = [present(member, language) for member in rows if member['pkg'] == row['pkg'] and member['name'] != name]
     if family:
-        relationships.append({'label': ctx['t']['family'], 'members': family})
-    properties = []
-    property_labels = {
-        'kind': {'standard': ('标准扩展', 'Standard'), 'preload': ('预加载扩展', 'Preloaded'),
-                 'puresql': ('纯 SQL', 'Pure SQL'), 'headless': ('无 DDL 扩展', 'No extension DDL')},
-        'lifecycle': {'active': ('活跃', 'Active'), 'abandoned': ('停止维护', 'Abandoned'),
-                      'archived': ('已归档', 'Archived'), 'deprecated': ('已弃用', 'Deprecated'), 'preview': ('预览', 'Preview')},
-    }
-    for field, label in (('kind', ctx['t']['kind']), ('lifecycle', ctx['t']['lifecycle']),
-                         ('kernel', localized(('适用内核', 'Kernel'), language)),
-                         ('vendor', localized(('厂商', 'Vendor'), language))):
-        if row.get(field):
-            value = localized(property_labels.get(field, {}).get(row[field], (row[field], row[field])), language)
-            properties.append({'label': label, 'value': value})
-    for field, label in (('need_load', ('需要显式加载', 'Requires loading')), ('trusted', ('可信扩展', 'Trusted extension')), ('relocatable', ('可迁移模式', 'Relocatable'))):
-        if row.get(field) is not None:
-            properties.append({'label': localized(label, language), 'value': localized(('是', 'Yes') if row[field] else ('否', 'No'), language)})
-    ctx.update({'extension': extension, 'title': name, 'description': extension['description'],
-                'document': content, 'toc': toc, 'doc_fallback': not primary and bool(fallback),
-                'links': links, 'relationships': relationships, 'properties': properties,
-                'create_sql': 'CREATE EXTENSION "{}";'.format(name.replace('"', '""')) if row['need_ddl'] else '',
-                'ext_source_url': 'https://pgext.cloud/ext/' + quote(name, safe='')})
-    seo(request, ctx, name + ' · ' + ctx['t']['catalog'], extension['description'])
+        relationships.append({'key': 'family', 'label': labels['family'], 'members': family})
+
+    outline = [{'id': 'overview', 'name': labels['overview'], 'level': 2}, {'id': 'documentation', 'name': labels['documentation'], 'level': 2}]
+    outline += [{**item, 'level': 3 if item['level'] >= 3 else 2} for item in toc if item['level'] >= 2]
+    if relationships:
+        outline.append({'id': 'related', 'name': labels['related'], 'level': 2})
+
+    lifecycle = row.get('lifecycle')
+    ctx.update({
+        'extension': extension, 'document': content, 'doc_fallback': not primary and bool(fallback),
+        'doc_language': (language if primary else ('en' if language == 'zh' else 'zh')) if (primary or fallback) else '',
+        'links': links, 'overview': [{'label': l, 'value': v, 'url': u} for l, v, u in overview],
+        'relationships': relationships, 'outline': outline,
+        'lifecycle': localized(PROPERTY_LABELS['lifecycle'].get(lifecycle, (lifecycle, lifecycle)), language) if lifecycle and lifecycle != 'active' else '',
+        'create_sql': 'CREATE EXTENSION "{}";'.format(name.replace('"', '""')) if row['need_ddl'] else '',
+        'ext_source_url': pgext_url, 'breadcrumb_category': browse_url(language, {'category': row['category']}),
+    })
+    seo(request, ctx, '{} · {}'.format(name, labels['catalog']), extension['description'], canonical=detail_url(name, language))
     return render(request, 'ext/detail.html', ctx)
 
 
 @require_safe
+def detail_root(request):
+    return HttpResponsePermanentRedirect('/ext/')
+
+
+@require_safe
 def sitemap(request):
-    paths = ['/ext/'] + ['/ext/list/{}/'.format(key) for key in DIMENSIONS]
-    paths += ['/ext/{}/'.format(quote(row['name'], safe='')) for row in catalog()]
+    rows = catalog()
+    paths = ['/ext/', '/ext/list/'] + ['/ext/{}/'.format(key) for key in DIMENSIONS]
+    paths += ['/ext/{}/'.format(code.lower()) for code in CATEGORIES if any(row['category'] == code for row in rows)]
+    for key in ('license', 'language', 'repo'):
+        paths += sorted({browse_url('zh', {key: row[DIMENSIONS[key]['field']]}) for row in rows})
+    paths += [detail_url(row['name']) for row in rows]
     return render(request, 'ext/sitemap.xml', {'paths': paths}, content_type='application/xml')
--- a/pgweb/news/forms.py
+++ b/pgweb/news/forms.py
@@ -7,12 +7,11 @@
 
 
 class NewsArticleForm(forms.ModelForm):
-    form_intro = 'Before submitting news, please read the <a href="/about/policies/news-and-events/">current policy</a> for News and Events'
+    form_intro = '提交新闻前，请先阅读现行的<a href="/about/policies/news-and-events/">新闻与活动收录政策</a>。'
 
     def __init__(self, *args, **kwargs):
         super().__init__(*args, **kwargs)
         self.fields['email'].required = True
-        self.fields['email'].help_text = "Pick a confirmed email associated with the organisation. This will be used as the reply address of posted news. If no correct address is available, you need to register one on the <a href=\"/account/edit/organisations/\">organisation</a> before submitting."
 
     def filter_by_user(self, user):
         self.fields['org'].queryset = Organisation.objects.filter(managers=user, approved=True)
@@ -21,7 +20,7 @@
     def clean_date(self):
         if self.instance.pk and self.instance.modstate != ModerationState.CREATED:
             if self.cleaned_data['date'] != self.instance.date:
-                raise ValidationError("You cannot change the date on an article that has been submitted or approved")
+                raise ValidationError("已提交审核或获批准的新闻不能更改日期。")
         return self.cleaned_data['date']
 
     @property
@@ -35,17 +34,17 @@
 
         if data.get('email', None):
             if data['email'].org != data['org']:
-                self.add_error('email', 'You must pick an email address associated with the organisation')
+                self.add_error('email', '请选择与该组织关联的邮箱地址。')
 
         if 'tags' not in data:
-            self.add_error('tags', 'Select one or more tags')
+            self.add_error('tags', '请选择一个或多个标签。')
         else:
             for t in data['tags']:
                 # Check each tag for permissions. This is not very db-efficient, but people
                 # don't save news articles that often...
                 if t.allowed_orgs.exists() and not t.allowed_orgs.filter(pk=data['org'].pk).exists():
                     self.add_error('tags',
-                                   'The organisation {} is not allowed to use the tag {}.'.format(
+                                   '组织 {} 无权使用标签 {}。'.format(
                                        data['org'],
                                        t,
                                    ))
@@ -55,6 +54,18 @@
     class Meta:
         model = NewsArticle
         exclude = ('date', 'submitter', 'modstate', 'postedto', 'firstmoderator')
+        labels = {
+            'org': '组织',
+            'email': '回复邮箱',
+            'title': '标题',
+            'content': '正文',
+            'tags': '标签',
+        }
+        help_texts = {
+            'org': '如果这里没有可选组织，请查看<a href="/account/orglist/">组织列表</a>并联系组织管理员；如未列出管理员，请联系 <a href="mailto:webmaster@postgresql.org">webmaster@postgresql.org</a>。如果您的组织尚未登记，可以<a href="/account/edit/organisations/">创建组织资料</a>。',
+            'email': '请选择与组织关联且已确认的邮箱地址，作为新闻发布后的回复地址。如没有合适的地址，请先在<a href="/account/edit/organisations/">组织资料</a>中添加，再提交新闻。',
+            'tags': '选择适合这篇新闻的标签。',
+        }
         widgets = {
             'tags': forms.CheckboxSelectMultiple,
         }
--- a/pgweb/profserv/views.py
+++ b/pgweb/profserv/views.py
@@ -24,7 +24,7 @@
         'regions': regions,
         'what': what,
         'og': {
-            'title': 'PostgreSQL：{}'.format(title),
+            'title': 'PostgreSQL {}'.format(title),
             'description': '按地区查找 PostgreSQL {}提供商，了解服务范围和联系方式。'.format(title),
             'url': '/support/professional_{}/'.format(what),
         },
@@ -54,7 +54,7 @@
         'regionname': regname,
         'services': services,
         'og': {
-            'title': 'PostgreSQL：{}：{}'.format(whatname, regname),
+            'title': 'PostgreSQL {}'.format(title),
             'description': '查找在{}提供 PostgreSQL {}的企业及其服务范围和联系方式。'.format(regname, whatname),
             'url': '/support/professional_{}/{}/'.format(what, regionname),
         },
--- a/pgweb/search/views.py
+++ b/pgweb/search/views.py
@@ -29,7 +29,7 @@
 
     if pagenum > 1:
         # Prev link
-        yield '<a href="%s&p=%s">Prev</a>' % (querystring, pagenum - 1)
+        yield '<a href="%s&p=%s">上一页</a>' % (querystring, pagenum - 1)
 
     if pagenum > 10:
         start = pagenum - 10
@@ -43,7 +43,7 @@
             yield '<a href="%s&p=%s">%s</a>' % (querystring, i, i)
 
     if pagenum != min(start + 20, totalpages):
-        yield '<a href="%s&p=%s">Next</a>' % (querystring, pagenum + 1)
+        yield '<a href="%s&p=%s">下一页</a>' % (querystring, pagenum + 1)
 
 
 @csrf_exempt
@@ -107,17 +107,17 @@
             dateval = 365
 
         sortoptions = (
-            {'val': 'r', 'text': 'Rank', 'selected': request.GET.get('s', '') not in ('d', 'i')},
-            {'val': 'd', 'text': 'Date', 'selected': request.GET.get('s', '') == 'd'},
-            {'val': 'i', 'text': 'Reverse date', 'selected': request.GET.get('s', '') == 'i'},
+            {'val': 'r', 'text': '相关性', 'selected': request.GET.get('s', '') not in ('d', 'i')},
+            {'val': 'd', 'text': '按日期排序', 'selected': request.GET.get('s', '') == 'd'},
+            {'val': 'i', 'text': '按日期反向排序', 'selected': request.GET.get('s', '') == 'i'},
         )
         dateoptions = (
-            {'val': -1, 'text': 'anytime'},
-            {'val': 1, 'text': 'within last day'},
-            {'val': 7, 'text': 'within last week'},
-            {'val': 31, 'text': 'within last month'},
-            {'val': 186, 'text': 'within last 6 months'},
-            {'val': 365, 'text': 'within last year'},
+            {'val': -1, 'text': '不限时间'},
+            {'val': 1, 'text': '最近一天'},
+            {'val': 7, 'text': '最近一周'},
+            {'val': 31, 'text': '最近一个月'},
+            {'val': 186, 'text': '最近六个月'},
+            {'val': 365, 'text': '最近一年'},
         )
     else:
         searchlists = False
@@ -127,7 +127,7 @@
     if request.GET.get('q', '') == '':
         if searchlists:
             return render(request, 'search/listsearch.html', {
-                'search_error': "No search term specified.",
+                'search_error': "请输入搜索词。",
                 'sortoptions': sortoptions,
                 'lists': MailingList.objects.all().order_by("group__sortkey"),
                 'listid': listid,
@@ -137,18 +137,18 @@
             })
         else:
             return render(request, 'search/sitesearch.html', {
-                'search_error': "No search term specified.",
+                'search_error': "请输入搜索词。",
             })
     query = request.GET['q'].strip()
     if '\0' in query or ((not searchlists) and suburl and '\0' in suburl):
         return render(request, 'search/sitesearch.html', {
-            'search_error': "Invalid character in search.",
+            'search_error': "搜索词中包含无效字符。",
         })
 
     # Anti-stefan prevention
     if len(query) > 1000:
         return render(request, 'search/sitesearch.html', {
-            'search_error': "Search term too long.",
+            'search_error': "搜索词过长，请缩短后重试。",
         })
 
     # Is the request being paged?
@@ -199,16 +199,16 @@
                 )
             except requests.exceptions.Timeout:
                 return render(request, 'search/listsearch.html', {
-                    'search_error': 'Timeout when talking to search server. Please try your search again later, or with a more restrictive search terms.',
+                    'search_error': '连接搜索服务器超时。请稍后重试，或缩小搜索范围。',
                 })
             except Exception as e:
                 return render(request, 'search/listsearch.html', {
-                    'search_error': 'General error when talking to search server.',
+                    'search_error': '连接搜索服务器时发生错误。',
                 })
             if r.status_code != 200:
                 memc = None
                 return render(request, 'search/listsearch.html', {
-                    'search_error': 'Error talking to search server: %s' % r.reason,
+                    'search_error': '连接搜索服务器时发生错误：%s' % r.reason,
                 })
             hits = r.json()
             if has_memcached and memc:
@@ -287,7 +287,7 @@
             })
         except psycopg2.ProgrammingError:
             return render(request, 'search/sitesearch.html', {
-                'search_error': 'Error executing search query.'
+                'search_error': '执行搜索时发生错误。'
             })
 
         hits = curs.fetchall()
--- a/pgweb/util/contexts.py
+++ b/pgweb/util/contexts.py
@@ -58,6 +58,15 @@
             {'title': 'pgBadger', 'link': '/docs/pgbadger/'},
         ]},
     ],
+    'ext': [
+        {'title': '扩展目录', 'link': '/ext/'},
+        {'title': '全部索引', 'link': '/ext/list/', 'submenu': [
+            {'title': '功能分类', 'link': '/ext/category/'},
+            {'title': '许可证', 'link': '/ext/license/'},
+            {'title': '编程语言', 'link': '/ext/language/'},
+            {'title': '仓库来源', 'link': '/ext/repo/'},
+        ]},
+    ],
     'community': [
         {'title': '社区', 'link': '/community/'},
         {'title': '贡献者', 'link': '/community/contributors/'},
@@ -75,7 +84,7 @@
         {'title': '提交者', 'link': '/developer/committers/'},
         {'title': '路线图', 'link': '/developer/roadmap/'},
         {'title': '编码', 'link': '/developer/coding/'},
-        {'title': '提交清单', 'link': 'https://commitfest.postgresql.org'},
+        {'title': 'CommitFest', 'link': 'https://commitfest.postgresql.org'},
         {'title': '测试', 'link': '/developer/testing/', 'submenu': [
             {'title': 'Beta 版信息', 'link': '/developer/beta/'},
         ]},
--- a/templates/account/communityauth_cooloff.html
+++ b/templates/account/communityauth_cooloff.html
@@ -1,4 +1,5 @@
 {%extends "base/page.html"%}
+{%block title%}社区认证暂不可用{%endblock%}
 {%block contents%}
 <h1>社区认证</h1>
 <p>
--- a/templates/account/communityauth_nogroup.html
+++ b/templates/account/communityauth_nogroup.html
@@ -1,4 +1,5 @@
 {%extends "base/page.html"%}
+{%block title%}访问被拒绝{%endblock%}
 {%block contents%}
 <h1>访问被拒绝</h1>
 <p>
--- a/templates/account/communityauth_noinfo.html
+++ b/templates/account/communityauth_noinfo.html
@@ -1,4 +1,5 @@
 {%extends "base/page.html"%}
+{%block title%}社区认证信息不完整{%endblock%}
 {%block contents%}
 <h1>社区认证</h1>
 <p>
--- a/templates/account/login.html
+++ b/templates/account/login.html
@@ -1,4 +1,5 @@
 {%extends "base/page.html"%}
+{%block title%}账户登录{%endblock%}
 {%block contents%}
 <h1>登录 <i class="fas fa-sign-in-alt"></i></h1>
 <p>
--- a/templates/account/password_reset_complete.html
+++ b/templates/account/password_reset_complete.html
@@ -1,4 +1,5 @@
 {%extends "base/page.html"%}
+{%block title%}密码重置完成{%endblock%}
 {%block contents%}
 <h1>密码重置完成 <i class="fas fa-key"></i></h1>
 <p>您的密码已重置。</p>
--- a/templates/account/signup_complete.html
+++ b/templates/account/signup_complete.html
@@ -1,4 +1,5 @@
 {%extends "base/page.html"%}
+{%block title%}账户已创建{%endblock%}
 {%block contents%}
 <h1>账户已创建</h1>
 
--- a/templates/account/submit_preview.html
+++ b/templates/account/submit_preview.html
@@ -4,7 +4,7 @@
 <h1>确认提交{{objtype}}</h1>
 
 <p>
-  您即将提交以下{{objtype}}进行审核。请注意，一旦提交，内容将无法再修改。
+  您即将提交以下{{objtype}}进行审核。提交后如需修改，请先在待审核列表中撤回提交。
 </p>
 {%if obj.extramodnotice %}
 <p>
--- a/templates/account/userprofileform.html
+++ b/templates/account/userprofileform.html
@@ -57,7 +57,7 @@
     <h2>备用邮箱地址</h2>
     <p>您可以为账户添加一个或多个备用邮箱地址，例如用于订阅邮件列表。</p>
     {%if secondaryaddresses%}
-    <p>请注意，删除此处的任何地址将级联到关联系统，例如可能导致自动取消邮件列表订阅。</p>
+    <p>请注意，删除这里的邮箱地址也会影响关联系统，例如可能导致自动取消邮件列表订阅。</p>
     <p></p>
     <p>以下备用地址当前已注册到您的账户：</p>
     <ul>
--- a/templates/base/base.html
+++ b/templates/base/base.html
@@ -1,7 +1,7 @@
 {%load pgseo%}{%seo_values as page_meta%}<!doctype html>
 <html lang="{{seo.lang|default:'zh'}}">
  <head>
-  <title>{%block fulltitle%}{%if seo.title%}{{seo.title}}{%else%}PostgreSQL：{%block title%}{%endblock%}{%endif%}{%endblock%}</title>
+  <title>{%block fulltitle%}{%if page_meta.title%}{{page_meta.title}}{%else%}PostgreSQL {%block title%}{%endblock%}{%endif%}{%endblock%}</title>
   <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
   <meta http-equiv="Content-Type" content="text/xhtml; charset=utf-8" />
   {%block meta%}{%endblock%}
--- a/templates/base/cocpage.html
+++ b/templates/base/cocpage.html
@@ -6,19 +6,19 @@
 {%block cocmain%}{%endblock%}
   </div>
   <div class="col-lg-3">
-    <h1>翻译</h1>
+    <h2>翻译</h2>
     <ul>
-      <li><span lang="en"><a href="/about/policies/coc/">English</a></span></li>
+      <li><span lang="en"><a href="https://www.postgresql.org/about/policies/coc/">English</a></span></li>
 {%for l in "pages/about/policies/coc"|list_templates|sort_lower%}
       <li><span lang="{{l}}"><a href="/about/policies/coc/{{l}}/">{{l|languagename}}</a></span></li>
 {%endfor%}
       <li><a href="/about/policies/coc-translations/">如何翻译</a></li>
     </ul>
-    <h1>委员会</h1>
+    <h2>委员会</h2>
     <ul>
       <li><a href="/about/policies/coc_committee/">当前委员会</a></li>
     </ul>
-    <h1>年度报告</h1>
+    <h2>年度报告</h2>
     <ul>
 {% for t in "pages/about/policies/coc/reports"|list_templates|sort_lower:True %}
       <li><a href="/about/policies/coc/reports/{{t}}/">{{t}}</a></li>
--- a/templates/core/about.html
+++ b/templates/core/about.html
@@ -5,7 +5,7 @@
 <h1>关于 <img class="logo" src="/media/img/about/press/elephant.png" alt="PostgreSQL Elephant Logo" /></h1>
 
 <h2>什么是 PostgreSQL？</h2>
-<p>PostgreSQL 是一个强大的开源对象关系型数据库系统。它使用并扩展了 SQL 语言，并结合了大量功能，能够安全地存储和扩展最复杂的数据工作负载。PostgreSQL 的起源可以追溯到 1986 年，当时它还是加州大学伯克利分校 <a href="/docs/current/history.html">POSTGRES</a> 项目的一部分；其核心代码库至今已持续活跃开发近 40 年。</p>
+<p>PostgreSQL 是一个强大的开源对象关系型数据库系统。它使用并扩展了 SQL 语言，并结合了大量功能，能够安全地存储数据，并灵活扩展以应对最复杂的数据工作负载。PostgreSQL 的起源可以追溯到 1986 年，当时它还是加州大学伯克利分校 <a href="/docs/current/history.html">POSTGRES</a> 项目的一部分；其核心代码库至今已持续活跃开发近 40 年。</p>
 <p>PostgreSQL 凭借成熟的架构、可靠性、数据完整性、强大的功能集和优异的可扩展性，以及开源社区持续交付高性能创新方案的长期投入，赢得了良好声誉。PostgreSQL 可在<a href="/download/">所有主流操作系统</a>上运行，自 2001 年以来一直符合 <a href="https://en.wikipedia.org/wiki/ACID" target="_blank" rel="noopener">ACID</a> 标准，并拥有强大的附加组件，例如广受欢迎的 <a href="https://postgis.net/" target="_blank" rel="noopener">PostGIS</a> 地理空间数据库扩展。因此，PostgreSQL 成为众多个人和组织的首选开源关系型数据库，并不令人意外。</p>
 <p>使用 PostgreSQL <a href="/docs/current/tutorial.html">入门</a>从未如此简单。选择一个您想构建的项目，让 PostgreSQL 安全、稳健地存储您的数据。</p>
 
@@ -41,12 +41,12 @@
   <li>
     <strong>并发与性能</strong>
     <ul>
-      <li>索引：B-tree，多列，表达式，部分索引</li>
+      <li>索引：B-树，多列，表达式，部分索引</li>
       <li>高级索引：GiST，SP-GiST，KNN GiST，GIN，BRIN，覆盖索引，布隆过滤器</li>
       <li>复杂的查询规划器/优化器，仅索引扫描，多列统计信息</li>
       <li>事务、嵌套事务（通过保存点）</li>
       <li>多版本并发控制（MVCC）</li>
-      <li>读查询和构建 B-tree 索引的并行化</li>
+      <li>读查询和构建 B-树 索引的并行化</li>
       <li>表分区</li>
       <li>SQL 标准中定义的所有事务隔离级别，包括可串行化</li>
       <li>即时编译（JIT）表达式</li>
@@ -65,7 +65,7 @@
   <li>
     <strong>安全性</strong>
     <ul>
-      <li>认证：GSSAPI，SSPI，LDAP，SCRAM-SHA-256，Certificate，OAuth 2.0 等</li>
+      <li>认证：GSSAPI，SSPI，LDAP，SCRAM-SHA-256，证书，OAuth 2.0 等</li>
       <li>强大的访问控制系统</li>
       <li>列级和行级安全性</li>
       <li>使用证书和附加方法的多因素认证</li>
@@ -87,7 +87,7 @@
     <ul>
       <li>支持国际字符集，例如通过 ICU 排序规则</li>
       <li>不区分大小写和不区分重音的排序规则</li>
-      <li>全文搜索</li>
+      <li>全文检索</li>
     </ul>
   </li>
 </ul>
--- a/templates/docs/docspage.html
+++ b/templates/docs/docspage.html
@@ -1,7 +1,7 @@
 {% extends "base/base.html" %}
 {% load pgfilters %}
 
-{% block title %}文档：{{page.display_version}}：{{page.title}}{% endblock %}
+{% block title %}{{page.display_version}} 文档 · {{page.title}}{% endblock %}
 
 {% block layoutblock %}
     <div class="container-fluid margin">
--- a/templates/index.html
+++ b/templates/index.html
@@ -48,7 +48,7 @@
       <time datetime="{{ release.date|date:"Y-m-d" }}">{{ release.date|date:"Y-m-d" }}</time>
     </a>
     {% endif %}
-    <h1 class="pg-hero__title">PostgreSQL：世界上最先进的开源关系型数据库</h1>
+    <h1 class="pg-hero__title">PostgreSQL 世界上最先进的开源关系型数据库</h1>
     {% with v=versions.0 %}
     <div class="pg-hero__actions">
       <a class="pg-btn pg-btn--light" role="button" href="/download/">下载 <i class="fa fa-arrow-right" aria-hidden="true"></i></a>
--- a/templates/pages/about/pgcenter.html
+++ b/templates/pages/about/pgcenter.html
@@ -12,7 +12,7 @@
 本站的页面结构、文案与手册内容译自 postgresql.org 及其文档，随上游更新持续同步。除翻译之外，本站还补充了少量面向中文读者的内容，例如中文社区的活动、资源与镜像说明；这些补充内容在页面中会尽量标明。
 </p>
 <p>
-译文力求忠实原文，但翻译难免存在疏漏。<strong>凡译文与英文原文有出入之处，均以原文为准。</strong>每个页面右上角的外链图标可以直接跳转到 postgresql.org 上的对应英文页面。
+译文力求忠实原文，但翻译难免存在疏漏。<strong>凡译文与英文原文有出入之处，均以原文为准。</strong>页面右上角的外链图标可前往对应的上游原文。
 </p>
 
 <h2>版权与许可</h2>
--- a/templates/pages/about/policies/coc/zh.html
+++ b/templates/pages/about/policies/coc/zh.html
@@ -1,127 +1 @@
-{%extends "base/cocpage.html"%}
-{%load pgfilters%}
-{%block title%}行为准则{%endblock%}
-{%block cocmain%}
-<h1>行为准则 <i class="fas fa-gavel"></i></h1>
-
-<p><em>Last updated: May 2, 2022. {%git_changes_link%}</em></p>
-
-<h2>序言</h2>
-
-<p>PostgreSQL 项目以我们的代码和工作质量，以及社区的技术和专业成就而自豪。我们希望所有的参与者品行专业，以礼相待，互惠互利，尊重所有用户和开发者。</p>
-
-<p>为此，我们制定了社区互动和社区参与项目工作的行为准则。这个准则涵盖了社区成员之间的所有互动，无论是否在 postgresql.org 基础架构中，只要没有其他更优先的行为准则（例如会议的行为准则），均以此准则为准。</p>
-
-<h2>包容和适当的行为</h2>
-
-<p>我们鼓励任何对使用 PostgreSQL 有兴趣的人参加 PostgreSQL 项目，无论在软件或一般技术方面的经验水平如何。我们鼓励所有用户对 PostgreSQL 做出发展和贡献，英雄不问出处。</p>
-
-<p>我们鼓励对软件和社区目前的现状及未来的发展方向进行有深思的，建设性的讨论。我们讨论的焦点应该是代码和相关技术，社区项目以及基础设施建设。</p>
-
-<p>我们拒绝和反对人身攻击以及对人格特征的负面评论。个人特征包括但不限于年龄，种族，国籍或血统，宗教，性别或性取向。</p>
-
-<p>违反本行为准则的其他行为包括但不限于：对个人或群体的暴力威胁、对专业，社区或项目破坏性的威胁、任何形式的性骚扰、从事可能使 PostgreSQL 项目名誉受损的行为、以及予以警示后仍拒绝停止不当行为等。</p>
-
-<h2>报复</h2>
-
-<p>明确禁止任何人对根据本《行为准则》提出投诉或协助调查此类投诉的人进行报复。报复行动的形式包括：</p>
-
-  <ul>
-      <li>进一步的人身攻击（公共或私人）；</li>
-
-      <li>破坏个人的职业声誉，或其雇主/同事/客户及社区社会地位的行为；</li>
-
-      <li>威胁个人隐私，人身安全，健康，住所或家庭的行为。</li>
-</ul>
-
-<p>对报复行为的处理方式将与任何其他违反本行为准则的行为相同。</p>
-
-<h2>行为准则委员会</h2>
-
-<p>核心团队将任命一个行为准则委员会来接收和调查所有投诉，并任命一名委员会主席。除核心团队成员外，社区的任何成员都可以自愿加入委员会。由于核心团队负责监督委员会，核心团队成员将不会在委员会任职，以防止利益冲突。该委员会的成员名单将随时向公众公开，并可在<a href="/about/policies/coc_committee/">此处</a>查看。</p>
-
-<p>委员会成员将每年更新一次。核心团队或委员会主席将通过社区官方渠道宣布年度会员遴选过程的开始和结束日期。</p>
-
-<p>任何想在委员会中任职的社区成员都将填写一份初步调查表，以备核心团队和现任委员会审核。现任委员会成员将先审查候选人，如有必要将进行面试，之后根据审核结果向核心团队进行推荐，最终由核心团队将为委员会选择新成员。</p>
-
-<p>委员会的过渡期最长为一个月，以便卸任成员和新成员做好知识与职责交接。</p>
-
-<p>虽然委员会成员人数没有规定，可以随时任职，但委员会始终至少由四人组成。委员会成员的任期为一（1）年。委员会成员连任不得超过三（3）届。</p>
-
-<p>委员会成员为核心团队服务，而如果核心团队认为某委员会成员不履行其职务职责，核心团队可免除该委员会成员的职责。</p>
-
-<p>如果某委员会成员不能或不愿履行其职务的责任，委员会也可以主动联系核心团队，协调免除该委员会成员的职责。</p>
-
-<h2>举报</h2>
-
-<p>如果您是不当行为的接受者，或目睹此类行为，请立即将其报告给行为规范委员会，网址为 coc@postgresql.org。如果不幸的是您希望对委员会的某个成员提出投诉，您可以改为单独联系任何其他委员会成员。</p>
-
-<p>在您的报告中，请尽可能多地提供详细信息：</p>
-
-  <ul>
-      <li>事件描述；</li>
-
-      <li>事件发生的时间；</li>
-
-      <li>任何电子讨论的屏幕截图；</li>
-
-      <li>姓名（如果可能的话）、任何证人的联系方式；</li>
-
-      <li>您认为对调查有帮助的任何其他信息。</li>
-
-  </ul>
-
-<h2>投诉处理</h2>
-
-<p>委员会将及时确认并调查每起举报事件。在各方合作下，委员会将争取在收到投诉后两周内完成调查。</p>
-
-<p>因充分调查的需要，事件报告和调查活动将尽可能保密。</p>
-
-<p>委员会届时将通知申诉人和被指控的违反者处理决定。如果调查仍在进行中，委员会将向当事方提供一份现况报告和预计的完成日期。</p>
-
-<p>调查可能会因缺乏配合而延长时间。委员会将尽一切努力完成调查，调查结束后尽快将调查结果提交申诉人和被指控的违反者，并通知他们委员会的决定。</p>
-
-<p>如果 CoC 委员会成员或核心团队成员提出投诉或被投诉，投诉处理将照常进行，但提出投诉或被投诉的委员会成员或核心团队成员，将被要求停止以委员会或核心团队成员角色处理此案件。</p>
-
-<p>可能的结果包括但不限于：</p>
-
-  <ul>
-      <li>确定不违反《行为准则》;</li>
-
-      <li>对所涉个人可接受的行为进行具体指导的私人训斥;</li>
-
-      <li>对个人可接受的行为进行具体指导的公开谴责;</li>
-
-      <li>暂时或永久禁止使用某些或所有社区管理的空间，包括但不限于社区邮件列表、论坛、IRC 和丧失承诺特权;</li>
-
-      <li>决定公开和/或私下道歉。</li>
-  </ul>
-
-<p>由于涉及临时禁令或永久禁令的裁定的严重性，以及结果需要采取的相关技术和基础设施行动，在将决定通知有关各方之前，这一调查结果将由核心团队审查。如果核心团队不同意委员会的调查结果，则核心团队将向委员会陈述其理由。委员会将考虑核心团队的建议，并在需要时进行进一步讨论。如果核心团队和委员会无法就决定达成一致，则以核心团队的决定为准。</p>
-
-<p>委员会可能认定，违反这项政策是无意的，可能是文化差异造成的（例如，使用俚语，在一种语言中无害，在另一种语言中具有冒犯性），或者是出于对幽默的误解。这类案件将被视为教训，而不应被谴责。如果委员会确定举报的事件属于无意违反事件的类别，则委员会将判断其他人是否可能再次发生该事件，如果是，则在社区内分享这个信息，以防止同样的事情再次发生。</p>
-
-<p>如果任何一方认为确定不正确，则可在其决定后的一周内向委员会提出上诉。上诉应包括新的信息，消息来源，证人等，以便委员会提供补充资料，以重新审议这一事件。</p>
-
-<p>事件报告，调查，委员会决定以及所有相关事项的记录将从事件档案中最后一项的日期起保留三个日历年。委员会将采取适当措施，确保所有报告和讨论的机密性。</p>
-
-<p>委员会将制作一份年度报告，总结上一年的每年第一季度末之前收到的投诉类型以及为解决这些投诉所采取的措施，并与社区分享该报告。投诉和行为将被匿名化，以保护所有相关方的身份。</p>
-
-<h2>诚实行事</h2>
-
-<p>对事件进行投诉的任何人都应本着诚信的态度行事，并且有合理的理由相信所披露的信息表明违反了本政策。任何证明不成立的指控，以及被证明是恶意或有意为虚假的指控，都将被视为严重的社区违规行为，并且违反了本《行为准则》。</p>
-
-<h2>行为准则更新</h2>
-
-<p>委员会或核心团队将根据：所报告的事件、法律专业人士的建议、社区成员的建议、社区文化发展、及其他需要修订的事件等，考虑对本行为准则的更新，至少每年一次。</p>
-
-<p>建议修订《行为准则》的社区成员可以通过电子邮件 <a href="mailto:coc@postgresql.org">coc@postgresql.org</a> 向委员会提交其想法。</p>
-
-<h2>会议与活动</h2>
-
-<p>我们鼓励每个活动的组织者为其活动创建并维护行为准则。这些行为准则可能与本行为准则相同或不同。每个活动的组织者都有责任及时，客观的处理其活动中的不当行为。</p>
-
-<h2>结论</h2>
-
-<p>我们鼓励社区成员之间建立适当和合作的关系：然而，成员必须对可能被其他成员认为是冒犯的行为敏感，并必须避免从事这种行为。在与社区的所有互动中，我们希望您运用的专业判断力，让讨论专注于推动我们的项目和社区朝着面向所有人的积极方向发展。</p>
-{% endblock %}
+{%extends "pages/about/policies/coc.html"%}
--- a/templates/pages/about/policies/funds-group.html
+++ b/templates/pages/about/policies/funds-group.html
@@ -138,7 +138,7 @@
 
 <ul>
   <li>资金小组的所有现任成员均可提出动议</li>
-  <li>任何其他现任成员必须对所有动议进行附议</li>
+  <li>每项动议都必须由另一位现任成员附议</li>
   <li>资金小组的所有现任成员均有投票资格</li>
   <li>
     辩论：
@@ -146,7 +146,7 @@
       <li>动议获得附议后开始</li>
       <li>在邮件列表上进行</li>
       <li>
-        自动议获得附议后不得少于 72 小时结束，除非动议中特别要求了更长的辩论期（任何情况下辩论不得少于 72 小时）
+        不得早于动议获得附议后的 72 小时结束，除非动议中特别要求了更长的辩论期（任何情况下辩论不得少于 72 小时）
       </li>
       <li>
         可以通过一项无辩论动议进行延长，该动议须经附议并通过公开投票方式批准
--- a/templates/pages/about/policies/news-and-events.html
+++ b/templates/pages/about/policies/news-and-events.html
@@ -31,13 +31,13 @@
 <ul>
     <li>所有条目必须以某种直接且明显的方式与 PostgreSQL 相关。</li>
     <li>仅含链接的活动或新闻永远不会被接受。</li>
-    <li>PostgreSQL 项目的网络团队可以永久禁止任何因社区成员对准确性、道德或合法性提出实质性投诉的组织。
+    <li>PostgreSQL 项目的网站团队可以永久禁止任何因社区成员对准确性、道德或合法性提出实质性投诉的组织。
     </li>
-    <li>网络团队不承诺及时审批公告。如果在特定日期发布对您至关重要，您需要在提交之前联系 pgsql-www 邮件列表或核心团队成员。
+    <li>网站团队不承诺及时审批公告。如果在特定日期发布对您至关重要，您需要在提交之前联系 pgsql-www 邮件列表或核心团队成员。
     </li>
-    <li>赞助商页面上列出的公司可以在网络团队的自行决定下，获得新闻和活动的任何限制（如允许的发布频率）的豁免。
+    <li>赞助商页面上列出的公司可以在网站团队的自行决定下，获得新闻和活动的任何限制（如允许的发布频率）的豁免。
     </li>
-    <li>网络团队保留部分或全部重写新闻或活动帖子以提高清晰度的权利。
+    <li>网站团队保留部分或全部重写新闻或活动帖子以提高清晰度的权利。
     </li>
     <li>从 PostgreSQL 发布日（包括任何 Beta 或候选发布版本（RC））的 UTC 午夜开始，在此后 72 小时的 UTC 午夜之前，不会批准任何新的公告请求。例如，如果 PostgreSQL
         新版本于 2018-02-08 发布，则在 2018-02-08 0:00 UTC 到 2018-02-11 0:00 UTC
@@ -122,7 +122,7 @@
 
 <p>开源会议和数据库会议，虽然不是主要关于 PostgreSQL，但包含重要的 PostgreSQL 内容（如多个演讲场次和一个展位），可以在活动列表中列出。此外，这些活动的征稿可以作为新闻被接受。</p>
 
-<p>更常规的活动，如每月的本地聚会或小型会议，将不会被列出。会议必须包含重要的 PostgreSQL 内容才值得列出，相对重要性的决定由网络团队作出。</p>
+<p>更常规的活动，如每月的本地聚会或小型会议，将不会被列出。会议必须包含重要的 PostgreSQL 内容才值得列出，相对重要性的决定由网站团队作出。</p>
 
 <h2>培训活动</h2>
 
--- a/templates/pages/about/policies/services-and-hosting.html
+++ b/templates/pages/about/policies/services-and-hosting.html
@@ -20,7 +20,7 @@
     <li>公司名称</li>
     <li>准确、完整的联系信息，包括电子邮件、网站、电话和注册办公地址
     </li>
-    <li>一段 15 至 50 字的公司服务摘要，详细说明公司为 PostgreSQL 提供的具体服务。
+    <li>一段 15 至 50 个词的公司服务摘要，详细说明公司为 PostgreSQL 提供的具体服务。
     </li>
     <li>地理位置信息</li>
 </ul>
--- a/templates/pages/about/press.html
+++ b/templates/pages/about/press.html
@@ -1,10 +1,10 @@
 {%extends "base/page.html"%}
-{%block title%}新闻{%endblock%}
+{%block title%}媒体资料{%endblock%}
 {%block contents%}
-<h1>新闻 <i class="far fa-newspaper"></i></h1>
+<h1>媒体资料 <i class="far fa-newspaper"></i></h1>
 
 <p>
-  如有任何新闻咨询，请联系 PostgreSQL 公关团队：<a href="mailto:press@postgresql.org">press@postgresql.org</a>
+  如有任何媒体咨询，请联系 PostgreSQL 公关团队：<a href="mailto:press@postgresql.org">press@postgresql.org</a>
 </p>
 
 <h2>PostgreSQL 18 新闻资料</h2>
--- a/templates/pages/about/press/faq.html
+++ b/templates/pages/about/press/faq.html
@@ -1,14 +1,14 @@
 {%extends "base/page.html"%}
-{%block title%}PostgreSQL 新闻常见问题{%endblock%}
+{%block title%}媒体常见问题{%endblock%}
 {%block contents%}
 
-<h1>常见问题</h1>
+<h1>媒体常见问题</h1>
 
 <p><strong>问：PostgreSQL 的当前版本是什么？</strong><br />
 答：18，于 2025 年 9 月 25 日发布。这是我们在超过 39 年开发历程中的第 35 个主要版本。我们每年发布一个新的 PostgreSQL 版本，这在 SQL 数据库中是独一无二的。</p>
 
 <p><strong>问：PostgreSQL 项目是否有行为准则？</strong><br />
-答：PostgreSQL 项目以代码质量和工作成果以及社区的技术和专业成就为荣。我们期望每位参与者都以专业的方式行事，以共同的礼貌和共同的利益行事，尊重所有用户和开发者。为此，我们制定了<a href="/about/policies/coc/">行为准则</a>，用于规范社区互动和项目工作及社区整体参与。</p>
+答：PostgreSQL 项目以代码质量和工作成果以及社区的技术和专业成就为荣。我们期望每位参与者都以专业的方式行事，以礼相待，维护共同利益，尊重所有用户和开发者。为此，我们制定了<a href="/about/policies/coc/">行为准则</a>，用于规范社区互动和项目工作及社区整体参与。</p>
 
 <p><strong>问：版本管理是如何运作的？</strong><br />
 答：PostgreSQL <a href="/support/versioning/">版本管理</a>采用两部分编号方案。第一个数字（例如 18）代表主要版本，即新功能版本。第二个数字代表补丁版本，即错误修复/安全修复版本。例如，"18.1" 是 PostgreSQL 18 的第一个补丁版本。更多信息请参阅 <a href="/support/versioning/">PostgreSQL 版本管理</a>页面。
@@ -19,7 +19,7 @@
 </p>
 
 <p><strong>问：为什么旧版本标记为 9.6.6 或 9.5.9？</strong><br />
-答：因为我们项目历史悠久，前两位小数代表主要版本。因此 9.6、9.5 等都是主要版本。次要版本的编号类似于 9.6.6。从版本 10 开始，项目采用了两部分版本编号方案。</p>
+答：因为我们项目历史悠久，版本号的前两部分代表主要版本。因此 9.6、9.5 等都是主要版本。次要版本的编号类似于 9.6.6。从版本 10 开始，项目采用了两部分版本编号方案。</p>
 
 <p><strong>问：PostgreSQL 的许可证是什么？费用是多少？</strong><br />
 答：PostgreSQL 以 <a href="https://opensource.org/licenses/PostgreSQL" target="_blank" rel="noopener">OSI 批准的 PostgreSQL 许可证</a>发布。即使用于商业软件产品也无需付费。请参阅 <a href="/about/licence/">PostgreSQL 许可证</a>。</p>
@@ -31,10 +31,10 @@
 答：在任何时候都有数千人在为 PostgreSQL 生态系统做贡献！核心数据库软件有超过 700 名贡献者。与其他开源项目一样，我们依赖数百名社区成员提供文档、翻译、推广、会议、网站开发、基础设施以及点对点支持。此外还有许多与 PostgreSQL 相关的其他项目，包括驱动程序、库、扩展等。</p>
 
 <p><strong>问：全球有多少 PostgreSQL 用户？</strong><br />
-答：由于我们通过开源世界的广泛分发和宽松的许可证，很难准确回答这个问题。大多数用户通过 Linux 发行版获取 PostgreSQL，或者通过包含 PostgreSQL 的许多其他产品、开源软件和硬件设备获取。许多指标，如 <a href="https://db-engines.com/en/ranking" target="_blank" rel="noopener">DB-Engines 排名</a>，表明 PostgreSQL 的采用持续快速增长，包括 DB-Engines 将 PostgreSQL 评为 <a href="https://db-engines.com/en/blog_post/76" target="_blank" rel="noopener">2017 年度最佳数据库管理系统</a>、<a href="https://db-engines.com/en/blog_post/79" target="_blank" rel="noopener">2018 年</a>、<a href="https://db-engines.com/en/blog_post/85" target="_blank" rel="noopener">2020 年</a>和 <a href="https://db-engines.com/en/blog_post/106" target="_blank" rel="noopener">2023 年</a>的最佳数据库管理系统。我们知道的是，PostgreSQL 比以往任何时候都更受欢迎，下载量和安装量逐年增长。</p>
+答：由于 PostgreSQL 通过开源渠道广泛分发，且采用宽松许可证，很难准确统计用户数量。大多数用户通过 Linux 发行版获取 PostgreSQL，或者通过包含 PostgreSQL 的许多其他产品、开源软件和硬件设备获取。许多指标，如 <a href="https://db-engines.com/en/ranking" target="_blank" rel="noopener">DB-Engines 排名</a>，表明 PostgreSQL 的采用持续快速增长，包括 DB-Engines 将 PostgreSQL 评为 <a href="https://db-engines.com/en/blog_post/76" target="_blank" rel="noopener">2017 年度最佳数据库管理系统</a>、<a href="https://db-engines.com/en/blog_post/79" target="_blank" rel="noopener">2018 年</a>、<a href="https://db-engines.com/en/blog_post/85" target="_blank" rel="noopener">2020 年</a>和 <a href="https://db-engines.com/en/blog_post/106" target="_blank" rel="noopener">2023 年</a>的最佳数据库管理系统。我们知道的是，PostgreSQL 比以往任何时候都更受欢迎，下载量和安装量逐年增长。</p>
 
 <p><strong>问：我们可以与你们的一些用户交流吗？</strong><br />
-答：请联系 <a href="mailto:press@postgresql.org">press@postgresql.org</a>，我们的新闻志愿者将尽力安排联系。</p>
+答：请联系 <a href="mailto:press@postgresql.org">press@postgresql.org</a>，我们的媒体志愿者将尽力安排联系。</p>
 
 <p><strong>问：哪家公司拥有 PostgreSQL？</strong><br />
 答：没有。我们是一个由志愿者和公司组成的非注册协会，在 PostgreSQL 许可证下共享代码。PostgreSQL 项目涉及数十家公司，它们要么支持 PostgreSQL 贡献者，要么直接向我们的代码仓库贡献企业项目。我们的一些主要企业赞助商列在<a href="/about/sponsors/">赞助商页面</a>上，还有更多公司以其他方式为项目做出贡献。</p>
@@ -46,7 +46,7 @@
 答：PostgreSQL 项目得到了多家公司的支持，这些公司销售基于 PostgreSQL 构建的产品或服务，并反过来向项目贡献代码、资金和人力时间。它们都不“拥有”PostgreSQL，也没有任何单一公司负责 PostgreSQL 的代码开发。这与 Linux、Apache 和 FreeBSD 的情况相同。</p>
 
 <p><strong>问：PostgreSQL 与 MySQL 相比如何？</strong><br />
-答：这是一个可以引发数小时讨论的话题。PostgreSQL 采用宽松许可证，由其社区拥有；MySQL 采用 GPL 许可证，由 Oracle 拥有。除此之外，每位数据库用户都应做出自己的评估；开源软件使得进行比较非常容易。我们建议您查看<a href="/about/">关于 PostgreSQL</a> 页面上 PostgreSQL 功能和特性的高级概述。</p>
+答：这是一个可以引发数小时讨论的话题。PostgreSQL 采用宽松许可证，由其社区拥有；MySQL 采用 GPL 许可证，由 Oracle 拥有。除此之外，每位数据库用户都应做出自己的评估；开源软件使得进行比较非常容易。我们建议您查看<a href="/about/">关于 PostgreSQL</a> 页面上 PostgreSQL 功能和特性概览。</p>
 
 <p><strong>问：PostgreSQL 与 Oracle/DB2/SQL Server/Informix 相比如何？</strong><br />
 答：我们的功能集通常被认为与其他领先的 SQL 关系数据库管理系统非常有竞争力。有些功能是它们有而 PostgreSQL 没有的，反之亦然。我们有许多从其他数据库系统迁移过来的用户，他们对自己的 PostgreSQL 系统完全满意。</p>
--- a/templates/pages/about/press/presskit10/cn.html
+++ b/templates/pages/about/press/presskit10/cn.html
@@ -1,5 +1,5 @@
 {%extends "base/page.html"%}
-{%block title%}PostgreSQL 10.0 版本发布说明{%endblock%}
+{%block title%}PostgreSQL 10 新闻资料包{%endblock%}
 {%block contents%}
 
 <div><span>This page in: </span>
@@ -20,7 +20,7 @@
     / <span lang="vi"><a href="/about/press/presskit10/vi/">Vietnamese</a></span><br/><br/>
 </div>
 
-<h1>PostgreSQL 10.0 版本发布说明 <img class="logo" src="/media/img/about/press/elephant.png" alt="PostgreSQL Elephant Logo" /></h1>
+<h1>PostgreSQL 10 新闻资料包 <img class="logo" src="/media/img/about/press/elephant.png" alt="PostgreSQL Elephant Logo" /></h1>
 
 <h2>目录</h2>
 
@@ -29,7 +29,7 @@
  <li><a href="#features">特性列表</a></li>
  <li><a href="#download">下载地址</a></li>
  <li><a href="#docs">文档</a></li>
- <li><a href="#license">版权声明</a></li>
+ <li><a href="#license">许可证</a></li>
  <li><a href="#contact">联系方式</a></li>
  <li><a href="#graphics">相关图片和标识</a></li>
  <li><a href="#about">关于 PostgreSQL 数据库</a></li>
@@ -50,7 +50,7 @@
 
 <b>逻辑复制-用于分发数据的发布/订阅（publish/subscribe）框架</b>
 
-<p>PostgreSQL 10 的逻辑复制对现有的 PostgreSQL 复制特性进行了扩展，新版本的逻辑复制可以将单个数据库级别或者表级别的改动（modifications）发送至不同的 PostgreSQL 数据库。这意味着用户可以细粒度地将数据变化发送到不同的数据库集群（database clusters），甚至在大版本升级期间，实现零停机（zero-downtime）。</p>
+<p>PostgreSQL 10 的逻辑复制对现有的 PostgreSQL 复制特性进行了扩展，新版本的逻辑复制可以将单个数据库级别或者表级别的改动（modifications）发送至不同的 PostgreSQL 数据库。这意味着用户可以细粒度地将数据变化发送到不同的数据库集簇（database clusters），甚至在大版本升级期间，实现零停机（zero-downtime）。</p>
 
 <p>"自 9.3 版本发布后，Yandex 公司便广泛使用 PostgreSQL 数据库产品，现在我们惊讶于 PostgreSQL 10，因为 10 版本终于给我们带来了翘首以盼的分区功能和内置的逻辑复制。这将会使得 PostgreSQL 应用到 Yandex 公司更多的服务上，"
 来自 <a href="https://www.yandex.com/" target="_blank" rel="noopener">Yandex 公司</a>的 DBA 团队负责人 Vladimir Borodin 表示。</p>
@@ -65,13 +65,13 @@
 
 </p>
 
-<b>同步复制的优选提交（Quorum Commit）  - 更加自信地分发数据</b>
+<b>同步复制的基于法定人数的提交（Quorum Commit）  - 更加自信地分发数据</b>
 
-<p>PostgreSQL 10 引入了优化提交机制（Quorum Commit），这使得主数据库在接收到远程备库更新成功的确认信息时具备灵活性。数据库管理员现在可以指定当固定数量的备库已确认对更新进行写入后，数据才被认为是安全写入。
+<p>PostgreSQL 10 引入了基于法定人数的提交机制（Quorum Commit），这使得主数据库在接收到远程备库更新成功的确认信息时具备灵活性。数据库管理员现在可以指定当固定数量的备库已确认对更新进行写入后，数据才被认为是安全写入。
 
 </p>
 
-<p>"PostgreSQL 10.0 版本中的同步复制的优选提交（Quorum Commit，即通过读写副本数的优化来平衡读写性能）功能，从应用程序的角度给了我们更多的选择，以几乎零停机的代价来扩展我们基础设施的能力。这允许我们可以连续发布或是更新数据库基础设施而不必忍受长时间停机维护的代价,"
+<p>"PostgreSQL 10.0 版本中的同步复制的基于法定人数的提交（Quorum Commit）功能，从应用程序的角度给了我们更多的选择，以几乎零停机的代价来扩展我们基础设施的能力。这允许我们可以连续发布或是更新数据库基础设施而不必忍受长时间停机维护的代价,"
 <a href="https://www.simple.com/" target="_blank" rel="noopener">Simple Finance</a> 的高级基础设施工程师 Curt Micol 先生说道。</p>
 
 <b>SCRAM-SHA-256 安全认证机制 - 让你的数据存取更安全</b>
@@ -104,10 +104,10 @@
 HTML 格式文档和 man 格式的文档随 PostgreSQL 一起安装，用户也可以免费地在网站上对<a href="https://www.postgresql.org/docs/10/">在线文档</a>进行浏览。
 </p>
 
-<a name="license"></a><h2>版权声明</h2>
+<a name="license"></a><h2>许可证</h2>
 <p>
-PostgreSQL 使用 <a href="/about/licence">PostgreSQL 版权声明</a>，它是类似 BSD 的版权。由于这个经 <a href="http://www.opensource.org/licenses/postgresql">OSI 认证的版权</a>不限制
-PostgreSQL 在商业环境和有版权的应用程序中使用，因此被公认为是非常有灵活性和对商业应用是友好的。加上有多个公司的支持和源代码归属公共所有，我们的版权使得 PostgreSQL 在那些希望在自己的产品里嵌入数据库的厂商中很流行，因为他们不用担心费用、嵌入软件的版权锁以及版权条款的改变。
+PostgreSQL 使用 <a href="/about/licence">PostgreSQL 许可证</a>，它是类似 BSD 的许可证。由于这个经 <a href="http://www.opensource.org/licenses/postgresql">OSI 认证的许可证</a>不限制
+PostgreSQL 在商业环境和专有应用程序中使用，因此被公认为是非常有灵活性和对商业应用是友好的。加上有多个公司的支持和源代码归属公共所有，这一许可证使得 PostgreSQL 在那些希望在自己的产品里嵌入数据库的厂商中很流行，因为他们不用担心费用、供应商锁定以及许可条款的改变。
 </p>
 
 <a name="contact"></a><h2>联系方式</h2>
@@ -161,10 +161,10 @@
 DBA 团队负责人 Vladimir Borodin 说道。
 </p>
 <p>
-<a href="https://yandex.com/" target="_blank" rel="noopener">Yandex 公司</a>是一家使用机器学习技术提供人工智能产品和服务的技术公司。Yandex 公司的目标是帮助客户和商业伙们更好地探索在线和离线业务。从 1997 年起，Yandex 公司已交付了世界级的、本地业务相关的搜索和信息服务。另外，Yandex 公司也为全球上百万的用户开发了市场领先的按需乘车服务、导航产品以及其他移动应用。可以通过<a href="https://yandex.com/company/contacts/" target="_blank" rel="noopener"> https://yandex.com/company/contacts/</a> 网址来访问 Yandex 公司。
+<a href="https://yandex.com/" target="_blank" rel="noopener">Yandex 公司</a>是一家使用机器学习技术提供人工智能产品和服务的技术公司。Yandex 公司的目标是帮助客户和商业伙伴更好地探索在线和离线业务。从 1997 年起，Yandex 公司已交付了世界级的、本地业务相关的搜索和信息服务。另外，Yandex 公司也为全球上百万的用户开发了市场领先的按需乘车服务、导航产品以及其他移动应用。可以通过<a href="https://yandex.com/company/contacts/" target="_blank" rel="noopener"> https://yandex.com/company/contacts/</a> 网址来访问 Yandex 公司。
 
 </p>
-<p>"PostgreSQL 10.0 版本中的同步复制的优选提交（Quorum Commit，即通过读写副本数的优化来平衡读写性能）功能，从应用程序的角度给了我们更多的选择，以几乎零停机的代价来扩展我们基础设施的能力。这允许我们可以连续发布或是更新数据库基础设施而不必忍受长时间停机维护的代价。"
+<p>"PostgreSQL 10.0 版本中的同步复制的基于法定人数的提交（Quorum Commit）功能，从应用程序的角度给了我们更多的选择，以几乎零停机的代价来扩展我们基础设施的能力。这允许我们可以连续发布或是更新数据库基础设施而不必忍受长时间停机维护的代价。"
  来自 <a href="https://www.simple.com/" target="_blank" rel="noopener">Simple 财务</a>公司的高级基础设施工程师 Curt Micol 先生说道。
 </p>
 
--- a/templates/pages/about/press/presskit11/cn.html
+++ b/templates/pages/about/press/presskit11/cn.html
@@ -19,9 +19,9 @@
 
 <p>PostgreSQL 11 在目前版本已有了按值列表或是按范围作为分区键值的分区表功能外，又增加了按哈希键值分区的功能，也称之为“Hash 分区”。PostgreSQL 11 还通过在分区功能中使用外部数据包装器 <a href="https://www.postgresql.org/docs/current/postgres-fdw.html">postgres_fdw</a> 的功能，也进一步提升了数据聚合能力。</p>
 
-<p>为了帮助管理分区，PostgreSQL 11 引入了将不含有分区键值的记录自动转入缺省分区的功能，并增加了在（主表）执行创建主键、外键、索引和触发器时，会将这些操作全部自动复制给所有分区表的功能。另外 PostgreSQL 11 现在也支持当记录中的分区键值字段被更新后，会自动将该记录移至新的正确的分区表中的功能。</p>
+<p>为了帮助管理分区，PostgreSQL 11 引入了将不匹配任何现有分区的记录自动放入默认分区的功能，并增加了在（主表）执行创建主键、外键、索引和触发器时，会将这些操作全部自动复制给所有分区表的功能。另外 PostgreSQL 11 现在也支持当记录中的分区键值字段被更新后，会自动将该记录移至新的正确的分区表中的功能。</p>
 
-<p>PostgreSQL 11 版本通过使用新的分区消除策略来提升查询分区表的性能。另外，PostgreSQL 11 现在在分区表上也支持流行的“UPSERT”功能，这可以帮助用户在处理应用数据时，简化应用程序的开发，减少网络负载。</p>
+<p>PostgreSQL 11 版本通过使用新的分区剪枝策略来提升查询分区表的性能。另外，PostgreSQL 11 现在在分区表上也支持流行的“UPSERT”功能，这可以帮助用户在处理应用数据时，简化应用程序的开发，减少网络负载。</p>
 
 <h3 id="-1">支持内置事务的存储过程</h3>
 
@@ -31,13 +31,13 @@
 
 <h3 id="-2">并行查询的增强</h3>
 
-<p>PostgreSQL 11 提升了并行查询的性能，通过更加有效的分区数据扫描，在并行顺序扫描和哈希聚合方面性能有了更大的改进。即使是组成 UNION 的查询子句不能并行处理时，PostgreSQL 现在也可以对使用 <code>UNION</code> 的 SELECT 查询并行处理。</p>
+<p>PostgreSQL 11 提升了并行查询的性能，通过更加有效的分区数据扫描，在并行顺序扫描和哈希连接方面性能有了更大的改进。即使是组成 UNION 的查询子句不能并行处理时，PostgreSQL 现在也可以对使用 <code>UNION</code> 的 SELECT 查询并行处理。</p>
 
-<p>PostgreSQL 11 也对几种数据集的定义指令增加了并行处理功能，最显著的就是通过 <code>CREATE INDEX</code> 指令创建的 B-树索引。其他几种支持并行化操作的还有 <code>CREATE TABLE .. AS</code>、<code>SELECT INTO</code>、<code>CREATE MATERIALIZED VIEW</code> 等创建表和物化视图的操作。</p>
+<p>PostgreSQL 11 也对几种数据定义命令增加了并行处理功能，最显著的就是通过 <code>CREATE INDEX</code> 指令创建的 B-树索引。其他几种支持并行化操作的还有 <code>CREATE TABLE .. AS</code>、<code>SELECT INTO</code>、<code>CREATE MATERIALIZED VIEW</code> 等创建表和物化视图的操作。</p>
 
 <h3 id="jit">表达式的 (JIT) 编译</h3>
 
-<p>PostgreSQL 11 版本引入了 JIT 编译来加速查询中的表达式的计算和执行。JIT 表达式的编译使用 LLVM 项目编译器的架构来提升在 WHERE 条件、指定列表、聚合、投影以及一些内部操作的表达式的编译执行。</p>
+<p>PostgreSQL 11 版本引入了 JIT 编译来加速查询中的表达式的计算和执行。JIT 表达式的编译使用 LLVM 项目编译器的架构来提升在 WHERE 条件、目标列表、聚合、投影以及一些内部操作的表达式的编译执行。</p>
 
 <p>要使用 JIT 编译，用户需要安装 LLVM 相关的依赖包，并在系统中启用 JIT 编译，可通过在 PostgreSQL 的配置文件中设置 <code>jit = on</code>，或是在 PostgreSQL 当前会话中执行 <code>SET jit = on</code> 指令均可启用 JIT。</p>
 
@@ -48,7 +48,7 @@
 <ul>
 <li>改变 <code>ALTER TABLE .. ADD COLUMN .. DEFAULT ..</code> 指令在带有非 <code>NULL</code> 缺省值时需要重写整个表的操作，新的变化对这样的指令带来了极大的性能量级提升；</li>
 
-<li>“覆盖索引”操作，允许用户在创建一个索引通过使用 <code>INCLUDE</code> 选项来增加额外字段，这样会对无 B-树索引列的查询来使用 Index-Only 的扫描有很大好处；</li>
+<li>“覆盖索引”允许通过 <code>INCLUDE</code> 子句将非键列存入索引，从而让更多查询使用仅索引扫描；这些附加列甚至可以使用 B-树不支持索引的数据类型；</li>
 
 <li>更多的窗口函数功能，包括允许 <code>RANGE</code> 来使用 <code>PRECEDING</code> / <code>FOLLOWING</code> 和 <code>GROUPS</code>，窗口的非包含功能等；</li>
 
--- a/templates/pages/about/press/presskit12/zh.html
+++ b/templates/pages/about/press/presskit12/zh.html
@@ -2,7 +2,7 @@
 
 {% block presskit_heading %}PostgreSQL 12 新闻资料包{% endblock presskit_heading %}
 {% block presskit_content %}
-<h1>PostgreSQL 12 版本发布!</h1>
+<h2>PostgreSQL 12 版本发布!</h2>
 
 <p>PostgreSQL 全球开发组今天宣布发布 PostgreSQL 12，这是世界上最先进的开源数据库的最新版本。</p>
 
@@ -16,11 +16,11 @@
 
 <p>PostgreSQL 12 为其索引系统和分区功能提供了显著的性能和维护增强。</p>
 
-<p>B-树索引，PostgreSQL 中的标准索引类型，在 PostgreSQL 12 中得到优化，以更好地处理索引经常被修改的工作负载。使用 TPC-C 基准测试，PostgreSQL 12 平均将空间利用率降低了约 40%，并整体提升了查询性能。</p>
+<p>B-树索引，PostgreSQL 中的标准索引类型，在 PostgreSQL 12 中得到优化，以更好地处理索引经常被修改的工作负载。使用 TPC-C 基准测试，PostgreSQL 12 平均减少约 40% 的空间占用，并整体提升了查询性能。</p>
 
 <p>分区表上的查询也有明显改进，特别是针对拥有数千个分区、但只需要从有限子集提取数据的表。PostgreSQL 12 还改进了通过 <code>INSERT</code> 和 <code>COPY</code> 向分区表添加数据的性能，并支持在不阻塞查询的情况下向表附加新分区。</p>
 
-<p>PostgreSQL 12 还带来了其他索引增强，进一步改善整体性能，包括降低 GiST、GIN 和 SP-GiST 索引类型生成预写式日志（WAL）的开销，支持在 GiST 索引上创建覆盖索引（<code>INCLUDE</code> 子句），支持使用距离运算符（<code>&lt;-&gt;</code>）对 SP-GiST 索引执行 K 最近邻查询，以及让 <code>CREATE STATISTICS</code> 现在支持最常见值（MCV）统计，从而帮助在列分布不均匀时生成更好的查询计划。</p>
+<p>PostgreSQL 12 还带来了其他索引增强，进一步改善整体性能，包括降低 GiST、GIN 和 SP-GiST 索引类型生成预写式日志（WAL）的开销，支持在 GiST 索引上创建覆盖索引（<code>INCLUDE</code> 子句），支持使用距离操作符（<code>&lt;-&gt;</code>）对 SP-GiST 索引执行 K 最近邻查询，以及让 <code>CREATE STATISTICS</code> 现在支持高频值（MCV）统计，从而帮助在列分布不均匀时生成更好的查询计划。</p>
 
 <p>使用 LLVM 的即时编译（JIT）在 PostgreSQL 11 中引入，现在在 PostgreSQL 12 中默认启用。JIT 编译可以为 <code>WHERE</code> 子句、目标列表、聚合和某些内部操作中的表达式执行带来性能收益，前提是您的 PostgreSQL 安装在编译或打包时包含 LLVM。</p>
 
--- a/templates/pages/about/press/presskit13/zh.html
+++ b/templates/pages/about/press/presskit13/zh.html
@@ -2,7 +2,7 @@
 
 {% block presskit_heading %}PostgreSQL 13 新闻资料包{% endblock presskit_heading %}
 {% block presskit_content %}
-<h1>PostgreSQL 13 正式发布！</h1>
+<h2>PostgreSQL 13 正式发布！</h2>
 
 <p>
   PostgreSQL 全球开发组今天宣布 <a href="https://www.postgresql.org/docs/13/release-13.html">PostgreSQL 13</a> 正式发布。作为世界上<a href="https://www.postgresql.org">最先进的开源数据库</a>，PostgreSQL 13 是目前的最新版本。
@@ -35,7 +35,7 @@
 </p>
 
 <p>
-  在 PostgreSQL 13 中，更多类型的<a href="https://www.postgresql.org/docs/13/functions-aggregate.html">聚合</a>和<a href="https://www.postgresql.org/docs/13/queries-table-expressions.html#QUERIES-GROUPING-SETS">分组</a>可以利用 PostgreSQL 的高效哈希聚合功能，因为具有大聚合的查询不必完全放在内存中。带有<a href="https://www.postgresql.org/docs/13/ddl-partitioning.html">分区表</a>的查询性能得到了提高，因为现在有更多情况可以修剪分区并且可以直接连接分区。
+  在 PostgreSQL 13 中，更多类型的<a href="https://www.postgresql.org/docs/13/functions-aggregate.html">聚合</a>和<a href="https://www.postgresql.org/docs/13/queries-table-expressions.html#QUERIES-GROUPING-SETS">分组</a>可以利用 PostgreSQL 的高效哈希聚合功能，因为具有大聚合的查询不必完全放在内存中。带有<a href="https://www.postgresql.org/docs/13/ddl-partitioning.html">分区表</a>的查询性能得到了提高，因为现在有更多情况可以对分区剪枝并且可以直接连接分区。
 </p>
 
 <h3 id="-">管理优化</h3>
@@ -49,7 +49,7 @@
 </p>
 
 <p>
-  <a href="https://www.postgresql.org/docs/13/warm-standby.html#STREAMING-REPLICATION-SLOTS">复制槽（Replication slots）</a>用于防止预写式日志（WAL）在备库收到之前被删除，可以在 PostgreSQL 13 中进行调整以指定<a href="https://www.postgresql.org/docs/13/runtime-config-replication.html#GUC-MAX-SLOT-WAL-KEEP-SIZE">要保留的 WAL 文件的最大数量</a>，并有助于避免磁盘空间不足的错误。
+  <a href="https://www.postgresql.org/docs/13/warm-standby.html#STREAMING-REPLICATION-SLOTS">复制槽（Replication slots）</a>用于防止预写式日志（WAL）在备库收到之前被删除，可以在 PostgreSQL 13 中进行调整以指定<a href="https://www.postgresql.org/docs/13/runtime-config-replication.html#GUC-MAX-SLOT-WAL-KEEP-SIZE">WAL 文件的最大保留量</a>，并有助于避免磁盘空间不足的错误。
 </p>
 
 <p>
@@ -70,7 +70,7 @@
 <h3 id="-">安全增强</h3>
 
 <p>
-  PostgreSQL 的扩展系统是其强大功能的关键组成部分，因为它允许开发人员扩展其功能。在以前的版本中，新的扩展只能由数据库超级用户安装。为了更轻松地利用 PostgreSQL 的可扩展性，PostgreSQL 13 添加了"<a href="https://www.postgresql.org/docs/13/sql-createextension.html">可信扩展</a>“的概念，该概念允许数据库用户使用安装超级用户标记为”受信任"的扩展。某些内置扩展默认情况下标记为受信任，包括 <a href="https://www.postgresql.org/docs/13/pgcrypto.html"><code>pgcrypto</code></a>, <a href="https://www.postgresql.org/docs/13/tablefunc.html"><code>tablefunc</code></a>, <a href="https://www.postgresql.org/docs/13/hstore.html"><code>hstore</code></a> 等。
+  PostgreSQL 的扩展系统是其强大功能的关键组成部分，因为它允许开发人员扩展其功能。在以前的版本中，新的扩展只能由数据库超级用户安装。为了更轻松地利用 PostgreSQL 的可扩展性，PostgreSQL 13 引入了“<a href="https://www.postgresql.org/docs/13/sql-createextension.html">受信任的扩展</a>”概念，允许对当前数据库具有 <code>CREATE</code> 权限的非超级用户安装被标记为“受信任”的扩展。某些内置扩展默认情况下标记为受信任，包括 <a href="https://www.postgresql.org/docs/13/pgcrypto.html"><code>pgcrypto</code></a>, <a href="https://www.postgresql.org/docs/13/tablefunc.html"><code>tablefunc</code></a>, <a href="https://www.postgresql.org/docs/13/hstore.html"><code>hstore</code></a> 等。
 </p>
 
 <p>
--- a/templates/pages/about/press/presskit14/zh.html
+++ b/templates/pages/about/press/presskit14/zh.html
@@ -2,7 +2,7 @@
 
 {% block presskit_heading %}PostgreSQL 14 新闻资料包{% endblock presskit_heading %}
 {% block presskit_content %}
-<h1>PostgreSQL 14 发布！</h1>
+<h2>PostgreSQL 14 发布！</h2>
 
 <p>PostgreSQL 全球开发组今天宣布 <a href="https://www.postgresql.org/docs/14/release-14.html">PostgreSQL 14</a> 正式发布，这是世界上<a href="https://www.postgresql.org/">最先进的开源数据库</a>的最新版本。</p>
 
@@ -32,17 +32,17 @@
 
 <p><a href="https://www.postgresql.org/docs/14/sql-createforeigndatawrapper.html">外部数据包装器</a>用于跨 PostgreSQL 和其他数据库处理联合工作负载，现在可以利用 PostgreSQL 14 中的并行查询特性。本版本在 <a href="https://www.postgresql.org/docs/14/postgres-fdw.html"><code>postgres_fdw</code></a>（与其他 PostgreSQL 数据库接口的外部数据包装器）中实现了这一能力。</p>
 
-<p>除了支持并行查询之外，<code>postgres_fdw</code> 现在还可以使用 <a href="https://www.postgresql.org/docs/14/sql-importforeignschema.html"><code>IMPORT FOREIGN SCHEMA</code></a> 指令在外部表上批量插入数据并导入表分区。</p>
+<p>除了支持并行查询之外，<code>postgres_fdw</code> 现在还支持向外部表批量插入数据，并可通过 <a href="https://www.postgresql.org/docs/14/sql-importforeignschema.html"><code>IMPORT FOREIGN SCHEMA</code></a> 命令导入分区表的定义。</p>
 
 <h3 id="-">管理和可观察性</h3>
 
-<p>PostgreSQL 14 将其性能提升扩展到了 <a href="https://www.postgresql.org/docs/14/routine-vacuuming.html">vacuum</a> 系统，包括减少 B-树索引开销的优化。此版本还添加了 vacuum 的“紧急模式”，用于防止事务 ID 环绕。<a href="https://www.postgresql.org/docs/14/sql-analyze.html"><code>ANALYZE</code></a> 用于收集数据库统计信息，基于自身的性能改进，现在在 PostgreSQL 14 中运行得明显更快。</p>
+<p>PostgreSQL 14 将其性能提升扩展到了 <a href="https://www.postgresql.org/docs/14/routine-vacuuming.html">vacuum</a> 系统，包括减少 B-树索引开销的优化。此版本还添加了 vacuum 的“紧急模式”，用于防止事务 ID 回卷。<a href="https://www.postgresql.org/docs/14/sql-analyze.html"><code>ANALYZE</code></a> 用于收集数据库统计信息，基于自身的性能改进，现在在 PostgreSQL 14 中运行得明显更快。</p>
 
 <p><a href="https://www.postgresql.org/docs/14/runtime-config-client.html#GUC-DEFAULT-TOAST-COMPRESSION">现在可以配置</a> PostgreSQL 的 <a href="https://www.postgresql.org/docs/14/storage-toast.html">TOAST</a> 系统的压缩选项。TOAST 系统用于存储较大的数据，例如文本块或几何图形。</p>
 
 <p>PostgreSQL 14 为 TOAST 列添加了 LZ4 压缩，同时保留对 <code>pglz</code> 压缩的支持。</p>
 
-<p>PostgreSQL 14 添加了几个新功能来帮助监控和可观察性，包括<a href="https://www.postgresql.org/docs/14/progress-reporting.html#COPY-PROGRESS-REPORTING">跟踪 <code>COPY</code> 命令的进度</a>、<a href="https://www.postgresql.org/docs/14/monitoring-stats.html#MONITORING-PG-STAT-WAL-VIEW">预写日志（WAL）活动</a>和<a href="https://www.postgresql.org/docs/14/monitoring-stats.html#MONITORING-PG-STAT-REPLICATION-SLOTS-VIEW">复制槽统计信息</a>。启用 <a href="https://www.postgresql.org/docs/14/runtime-config-statistics.html#GUC-COMPUTE-QUERY-ID"><code>compute_query_id</code></a> 可以让您通过多个 PostgreSQL 特性（包括 <a href="https://www.postgresql.org/docs/14/monitoring-stats.html#MONITORING-PG-STAT-ACTIVITY-VIEW"><code>pg_stat_activity</code></a>、<a href="https://www.postgresql.org/docs/14/sql-explain.html"><code>EXPLAIN VERBOSE</code></a> 和其他功能）唯一跟踪某个查询。</p>
+<p>PostgreSQL 14 添加了几个新功能来帮助监控和可观察性，包括<a href="https://www.postgresql.org/docs/14/progress-reporting.html#COPY-PROGRESS-REPORTING">跟踪 <code>COPY</code> 命令的进度</a>、<a href="https://www.postgresql.org/docs/14/monitoring-stats.html#MONITORING-PG-STAT-WAL-VIEW">预写式日志（WAL）活动</a>和<a href="https://www.postgresql.org/docs/14/monitoring-stats.html#MONITORING-PG-STAT-REPLICATION-SLOTS-VIEW">复制槽统计信息</a>。启用 <a href="https://www.postgresql.org/docs/14/runtime-config-statistics.html#GUC-COMPUTE-QUERY-ID"><code>compute_query_id</code></a> 可以让您通过多个 PostgreSQL 特性（包括 <a href="https://www.postgresql.org/docs/14/monitoring-stats.html#MONITORING-PG-STAT-ACTIVITY-VIEW"><code>pg_stat_activity</code></a>、<a href="https://www.postgresql.org/docs/14/sql-explain.html"><code>EXPLAIN VERBOSE</code></a> 和其他功能）唯一跟踪某个查询。</p>
 
 <h3 id="sql-">SQL 性能、一致性和便利性</h3>
 
--- a/templates/pages/about/press/presskit15/zh.html
+++ b/templates/pages/about/press/presskit15/zh.html
@@ -28,7 +28,7 @@
 <p>PostgreSQL 的<a href="https://www.postgresql.org/docs/15/monitoring-stats.html">服务器级统计信息</a>现在被收集到共享内存中，从而移除了统计收集进程，也不再需要周期性地将这些数据写入磁盘。</p>
 <p>PostgreSQL 15 现在允许将 <a href="https://www.postgresql.org/docs/15/collation.html">ICU 排序规则</a>设为整个集簇或单个数据库的默认排序规则。</p>
 <p>此版本还新增了一个内建扩展 <a href="https://www.postgresql.org/docs/15/pgwalinspect.html"><code>pg_walinspect</code></a>，使用户可以直接通过 SQL 接口检查预写式日志文件的内容。</p>
-<p>PostgreSQL 15 还允许<a href="https://www.postgresql.org/docs/15/ddl-schemas.html#DDL-SCHEMAS-PATTERNS">从 <code>public</code>（即默认）模式中撤销除数据库所有者之外所有用户的 <code>CREATE</code> 权限</a>。</p>
+<p>PostgreSQL 15 调整了新建数据库的默认权限：<a href="https://www.postgresql.org/docs/15/ddl-schemas.html#DDL-SCHEMAS-PATTERNS">不再向 <code>PUBLIC</code> 授予 <code>public</code> 模式的 <code>CREATE</code> 权限</a>。升级现有数据库时，其原有权限设置保持不变。</p>
 <p>PostgreSQL 15 删除了长期弃用的“独占备份”模式，同时也移除了 PL/Python 对 Python 2 的支持。</p>
 
 <h3 id="about-postgresql">关于 PostgreSQL</h3>
--- a/templates/pages/about/press/presskit16/zh.html
+++ b/templates/pages/about/press/presskit16/zh.html
@@ -10,7 +10,7 @@
 <p>PostgreSQL 16 通过新的查询规划器优化提升了现有 PostgreSQL 功能的性能。在该最新版本中，<a href="https://www.postgresql.org/docs/16/parallel-query.html">查询规划器可以并行执行</a> <code>FULL</code> 和 <code>RIGHT</code> <a href="https://www.postgresql.org/docs/16/queries-table-expressions.html#QUERIES-JOIN">连接</a>，为使用带有 <code>DISTINCT</code> 或 <code>ORDER BY</code> 子句的<a href="https://www.postgresql.org/docs/16/functions-aggregate.html">聚合函数</a>的查询生成更优的执行计划，利用增量排序来处理 <a href="https://www.postgresql.org/docs/16/queries-select-lists.html#QUERIES-DISTINCT"><code>SELECT DISTINCT</code></a> 查询，并优化<a href="https://www.postgresql.org/docs/16/sql-expressions.html#SYNTAX-WINDOW-FUNCTIONS">窗口函数</a>，使其执行更加高效。它还改进了 <code>RIGHT</code> 和 <code>OUTER</code>“反连接”（anti-joins），使用户能够识别出不在已连接表中的数据行。</p>
 <p>该版本包含单一和并发操作中使用 <a href="https://www.postgresql.org/docs/16/sql-copy.html"><code>COPY</code></a> 进行批量加载的改进，测试显示在某些情况下性能提升高达 300%。PostgreSQL 16 增加了对使用 libpq 的客户端的<a href="https://www.postgresql.org/docs/16/libpq-connect.html#LIBPQ-CONNECT-LOAD-BALANCE-HOSTS">负载均衡</a>支持，并改进了 vacuum 策略，减少全表冻结的必要性。此外，PostgreSQL 16 引入了在 x86 和 ARM 架构上使用 <code>SIMD</code> 的 CPU 加速，从而在处理 ASCII 和 JSON 字符串以及执行数组和子事务搜索时，性能有所提升。</p>
 <h3 id="-">逻辑复制</h3>
-<p><a href="https://www.postgresql.org/docs/16/logical-replication.html">逻辑复制</a>允许用户将数据流复制到其他可以解析 PostgreSQL 逻辑复制协议的节点或订阅者。在 PostgreSQL 16 中，用户可以从备节点（standby）执行逻辑复制，这意味着备节点可以将逻辑变更发布到其他服务器。这为开发者提供了新的工作负载分布选项——例如，使用备节点而不是更繁忙的主节点通过逻辑复制将更改应用到下级订阅端。</p>
+<p><a href="https://www.postgresql.org/docs/16/logical-replication.html">逻辑复制</a>允许用户将数据流复制到其他可以解析 PostgreSQL 逻辑复制协议的节点或订阅者。在 PostgreSQL 16 中，用户可以从备库（standby）执行逻辑复制，这意味着备库可以将逻辑变更发布到其他服务器。这为开发者提供了新的工作负载分布选项——例如，使用备库而不是更繁忙的主库通过逻辑复制将更改应用到下级订阅端。</p>
 <p>此外，PostgreSQL 16 中对逻辑复制进行了多项性能改进。订阅者现在可以使用并行方式来处理大型事务。对于没有<a href="https://www.postgresql.org/docs/16/ddl-constraints.html#DDL-CONSTRAINTS-PRIMARY-KEYS">主键</a>的表，订阅者可以使用 B-树索引而不是顺序扫描来查找行。在某些条件下，用户还可以使用二进制格式加速初始表同步。</p>
 <p>PostgreSQL 16 逻辑复制的访问控制做了多项改进，包括新的<a href="https://www.postgresql.org/docs/16/predefined-roles.html">预定义角色</a> <code>pg_create_subscription</code>，该角色允许用户新建逻辑订阅。</p>
 <p>该版本开始支持双向逻辑复制功能，可以在两个不同发布者的表之间进行数据复制。</p>
--- a/templates/pages/about/press/presskit17/zh.html
+++ b/templates/pages/about/press/presskit17/zh.html
@@ -3,7 +3,7 @@
 {% block presskit_heading %}PostgreSQL 17 新闻资料包{% endblock presskit_heading %}
 {% block presskit_content %}
 <p><strong>2024 年 9 月 26 日</strong> - <a href="https://www.postgresql.org">PostgreSQL 全球开发组</a>今日宣布发布 <a href="https://www.postgresql.org/docs/17/release-17.html">PostgreSQL 17</a>，这是世界上最先进的开源数据库的最新版本。</p>
-<p>PostgreSQL 17 建立在数十年的开源开发积累之上，在继续适应新兴数据访问与存储模式的同时，进一步提升了性能与可扩展性。这个版本的 <a href="https://www.postgresql.org">PostgreSQL</a> 带来了显著的整体性能提升，包括重构后的 vacuum 内存管理实现、面向高并发工作负载的存储访问优化、批量加载与导出加速，以及面向索引的查询执行改进。PostgreSQL 17 也为新工作负载与关键系统带来了同样有价值的能力，例如通过 SQL/JSON 的 <code>JSON_TABLE</code> 命令增强开发者体验，以及通过逻辑复制增强来简化高可用工作负载与主版本升级的管理。</p>
+<p>PostgreSQL 17 建立在数十年的开源开发积累之上，在继续适应新兴数据访问与存储模式的同时，进一步提升了性能与可伸缩性。这个版本的 <a href="https://www.postgresql.org">PostgreSQL</a> 带来了显著的整体性能提升，包括重构后的 vacuum 内存管理实现、面向高并发工作负载的存储访问优化、批量加载与导出加速，以及面向索引的查询执行改进。PostgreSQL 17 也为新工作负载与关键系统带来了同样有价值的能力，例如通过 SQL/JSON 的 <code>JSON_TABLE</code> 命令增强开发者体验，以及通过逻辑复制增强来简化高可用工作负载与主版本升级的管理。</p>
 <p>PostgreSQL 核心团队成员 Jonathan Katz 表示：“PostgreSQL 17 充分展示了推动 PostgreSQL 持续发展的全球开源社区，如何为数据库使用过程中的各类用户构建真正有价值的增强能力。无论是支撑大规模数据库运行的性能改进，还是进一步强化开发者体验的新特性，PostgreSQL 17 都将全面提升您的数据管理体验。”</p>
 <p>PostgreSQL 是一个以可靠性、健壮性和可扩展性著称的创新型数据管理系统。得益于全球开发者社区 25 年以上的开源开发积累，它已成为各类组织首选的开源关系型数据库。</p>
 
@@ -15,7 +15,7 @@
 <h3 id="further-expansion-of-a-robust-developer-experience">进一步扩展稳健的开发者体验</h3>
 <p>PostgreSQL 是<a href="https://www.postgresql.org/about/news/postgresql-92-released-1415/">首个引入 JSON 支持的关系型数据库（2012 年）</a>，而 PostgreSQL 17 则继续完善其对 SQL/JSON 标准的实现。现在，<a href="https://www.postgresql.org/docs/17/functions-json.html#FUNCTIONS-SQLJSON-TABLE"><code>JSON_TABLE</code></a> 已可在 PostgreSQL 17 中使用，使开发者能够将 JSON 数据转换为标准 PostgreSQL 表。PostgreSQL 17 还支持 <a href="https://www.postgresql.org/docs/17/functions-json.html#FUNCTIONS-JSON-CREATION-TABLE">SQL/JSON 构造函数</a>（<code>JSON</code>、<code>JSON_SCALAR</code>、<code>JSON_SERIALIZE</code>）以及<a href="https://www.postgresql.org/docs/17/functions-json.html#SQLJSON-QUERY-FUNCTIONS">查询函数</a>（<code>JSON_EXISTS</code>、<code>JSON_QUERY</code>、<code>JSON_VALUE</code>），为开发者提供了更多与 JSON 数据交互的方式。此版本还增加了更多 <a href="https://www.postgresql.org/docs/17/functions-json.html#FUNCTIONS-SQLJSON-PATH-OPERATORS"><code>jsonpath</code> 表达式</a>，重点加强了将 JSON 数据转换为原生 PostgreSQL 数据类型（例如数值、布尔值、字符串与日期/时间类型）的能力。</p>
 <p>PostgreSQL 17 还为 <a href="https://www.postgresql.org/docs/17/sql-merge.html"><code>MERGE</code></a> 增加了更多功能，包括 <code>RETURNING</code> 子句以及更新<a href="https://www.postgresql.org/docs/17/sql-createview.html">视图</a>的能力。除此之外，PostgreSQL 17 还为批量加载和数据导出带来了新能力：使用 <a href="https://www.postgresql.org/docs/17/sql-copy.html"><code>COPY</code></a> 导出大行数据时，性能最高可提升 2 倍。当源编码与目标编码一致时，<code>COPY</code> 的性能也会进一步提升；同时它还增加了新的 <code>ON_ERROR</code> 选项，允许在插入出错时继续导入。</p>
-<p>此版本进一步扩展了分区数据管理以及跨远程 PostgreSQL 实例的数据处理能力。PostgreSQL 17 现已支持在<a href="https://www.postgresql.org/docs/17/ddl-partitioning.html">分区表</a>上使用身份列与排除约束。用于在远程 PostgreSQL 实例上执行查询的 <a href="https://www.postgresql.org/docs/17/postgres-fdw.html">PostgreSQL 外部数据包装器</a>（<a href="https://www.postgresql.org/docs/17/postgres-fdw.html"><code>postgres_fdw</code></a>）现在还可以将 <code>EXISTS</code> 与 <code>IN</code> 子查询下推到远程服务器，以获得更高的执行效率。</p>
+<p>此版本进一步扩展了分区数据管理以及跨远程 PostgreSQL 实例的数据处理能力。PostgreSQL 17 现已支持在<a href="https://www.postgresql.org/docs/17/ddl-partitioning.html">分区表</a>上使用标识列与排他约束。用于在远程 PostgreSQL 实例上执行查询的 <a href="https://www.postgresql.org/docs/17/postgres-fdw.html">PostgreSQL 外部数据包装器</a>（<a href="https://www.postgresql.org/docs/17/postgres-fdw.html"><code>postgres_fdw</code></a>）现在还可以将 <code>EXISTS</code> 与 <code>IN</code> 子查询下推到远程服务器，以获得更高的执行效率。</p>
 <p>PostgreSQL 17 还引入了一个内建、平台无关且保证不可变的排序规则提供程序。它的排序语义与 <code>C</code> 排序规则类似，但采用 <code>UTF-8</code> 编码而不是 <code>SQL_ASCII</code>。使用这一新的排序规则提供程序，可以确保基于文本的查询无论在何处运行 PostgreSQL，都能返回一致的排序结果。</p>
 
 <h3 id="logical-replication-enhancements-for-high-availability-and-major-version-upgrades">面向高可用与主版本升级的逻辑复制增强</h3>
--- a/templates/pages/about/press/presskit18/zh.html
+++ b/templates/pages/about/press/presskit18/zh.html
@@ -3,7 +3,7 @@
 {% block presskit_heading %}PostgreSQL 18 新闻资料包{% endblock presskit_heading %}
 {% block presskit_content %}
 <p><strong>2025 年 9 月 25 日</strong> - <a href="https://www.postgresql.org">PostgreSQL 全球开发组</a>今日宣布发布 <a href="https://www.postgresql.org/docs/18/release-18.html">PostgreSQL 18</a>，这是世界上最先进的开源数据库的最新版本。</p>
-<p>PostgreSQL 18 通过全新的 I/O 子系统提升了各类工作负载的性能；在从存储读取数据时，性能提升最高可达 3 倍，同时还扩大了能够利用索引的查询范围。此版本降低了主版本升级带来的扰动，加快了升级速度，并缩短了升级完成后恢复到预期性能所需的时间。开发者也能从 PostgreSQL 18 中直接受益，例如可在查询时计算数值的虚拟生成列，以及能够为 UUID 带来更佳索引与读取性能的 <code>uuidv7()</code> 函数。借助对 OAuth 2.0 身份验证的支持，PostgreSQL 18 也更易于与单点登录（SSO）系统集成。</p>
+<p>PostgreSQL 18 通过全新的 I/O 子系统提升了各类工作负载的性能；在从存储读取数据时，性能提升最高可达 3 倍，同时还扩大了能够利用索引的查询范围。此版本降低了主版本升级带来的扰动，加快了升级速度，并缩短了升级完成后恢复到预期性能所需的时间。开发者也能从 PostgreSQL 18 中直接受益，例如可在查询时计算值的虚拟生成列，以及能够为 UUID 带来更佳索引与读取性能的 <code>uuidv7()</code> 函数。借助对 OAuth 2.0 身份验证的支持，PostgreSQL 18 也更易于与单点登录（SSO）系统集成。</p>
 <p>PostgreSQL 核心团队成员 Jonathan Katz 表示：“全球开源社区的持续投入塑造了每一次 PostgreSQL 发布，并不断交付契合用户数据场景的功能。PostgreSQL 18 延续了该项目长期以来可靠、稳健的数据管理传统，同时继续扩展其可支持的工作负载范围。”</p>
 <p>PostgreSQL 是一个以可靠性、健壮性和可扩展性著称的创新型数据管理系统。得益于全球开发者社区近 30 年的开源开发积累，它已成为各类组织首选的开源关系型数据库。</p>
 
@@ -17,12 +17,12 @@
 <p>此外，执行主版本升级的工具 <a href="https://www.postgresql.org/docs/18/pgupgrade.html"><code>pg_upgrade</code></a> 在 PostgreSQL 18 中也获得了多项增强。例如，当数据库包含大量表、序列等对象时，升级速度会更快。此版本还允许通过 <code>--jobs</code> 参数并行执行检查，并新增了 <code>--swap</code> 参数，通过交换升级目录来替代复制、克隆或链接文件的方式。</p>
 
 <h3 id="查询与整体性能提升">查询与整体性能增强</h3>
-<p>PostgreSQL 18 进一步提升了查询性能，并通过一系列可自动生效的优化让工作负载运行得更快。此版本在<a href="https://www.postgresql.org/docs/18/indexes-multicolumn.html">多列 B-树索引</a>上引入了“跳跃扫描”能力，可提升查询在缺少一个或多个前缀索引列等值条件（<code>=</code>）时的执行效率。它还能够优化 <code>WHERE</code> 子句中包含 <code>OR</code> 条件的查询，使其更好地利用索引，从而显著缩短执行时间。PostgreSQL 18 还在表连接的规划与执行方面做出多项改进，包括提升哈希连接性能，以及允许合并连接使用增量排序等。此外，<a href="https://www.postgresql.org/docs/18/gin.html">GIN 索引</a>现在也支持并行构建，与 B-树和 <a href="https://www.postgresql.org/docs/current/brin.html">BRIN 索引</a>一道具备这一能力。</p>
+<p>PostgreSQL 18 进一步提升了查询性能，并通过一系列可自动生效的优化让工作负载运行得更快。此版本在<a href="https://www.postgresql.org/docs/18/indexes-multicolumn.html">多列 B-树索引</a>上引入了“跳跃扫描”能力，可提升查询在缺少一个或多个前缀索引列等值条件（<code>=</code>）时的执行效率。它还能够优化 <code>WHERE</code> 子句中包含 <code>OR</code> 条件的查询，使其更好地利用索引，从而显著缩短执行时间。PostgreSQL 18 还在表连接的规划与执行方面做出多项改进，包括提升哈希连接性能，以及允许归并连接使用增量排序等。此外，<a href="https://www.postgresql.org/docs/18/gin.html">GIN 索引</a>现在也支持并行构建，与 B-树和 <a href="https://www.postgresql.org/docs/current/brin.html">BRIN 索引</a>一道具备这一能力。</p>
 <p>该版本还继续增强 PostgreSQL 对硬件加速的支持，例如为 <code>popcount</code> 函数加入了对 ARM NEON 与 SVE CPU 内置指令的支持，而该函数会被 <a href="https://www.postgresql.org/docs/18/functions-bitstring.html"><code>bit_count</code></a> 等内部能力使用。</p>
 
 <h3 id="提升开发者体验">持续强化开发者体验</h3>
-<p>PostgreSQL 18 引入了<a href="https://www.postgresql.org/docs/18/sql-createtable.html#SQL-CREATETABLE-PARMS-GENERATED-STORED">虚拟生成列</a>，它在查询时计算数值而不是直接存储，并已成为生成列的默认选项。此外，存储型生成列现在也可以参与逻辑复制。</p>
-<p>此版本还新增了在 <a href="https://www.postgresql.org/docs/18/dml-returning.html"><code>RETURNING</code> 子句</a>中同时访问修改前（<code>OLD</code>）与当前（<code>NEW</code>）数值的能力，适用于 <code>INSERT</code>、<code>UPDATE</code>、<code>DELETE</code> 和 <code>MERGE</code> 命令。PostgreSQL 18 还通过 <a href="https://www.postgresql.org/docs/18/functions-uuid.html#FUNC_UUID_GEN_TABLE"><code>uuidv7()</code></a> 新增 UUIDv7 生成能力，可生成按时间戳排序的随机 UUID，以支持更好的缓存策略。与此同时，<a href="https://www.postgresql.org/docs/18/functions-uuid.html#FUNC_UUID_GEN_TABLE"><code>uuidv4()</code></a> 也已作为 <code>gen_random_uuid()</code> 的别名提供。</p>
+<p>PostgreSQL 18 引入了<a href="https://www.postgresql.org/docs/18/sql-createtable.html#SQL-CREATETABLE-PARMS-GENERATED-STORED">虚拟生成列</a>，它在查询时计算值而不是直接存储，并已成为生成列的默认选项。此外，存储型生成列现在也可以参与逻辑复制。</p>
+<p>此版本还新增了在 <a href="https://www.postgresql.org/docs/18/dml-returning.html"><code>RETURNING</code> 子句</a>中同时访问修改前（<code>OLD</code>）与当前（<code>NEW</code>）值的能力，适用于 <code>INSERT</code>、<code>UPDATE</code>、<code>DELETE</code> 和 <code>MERGE</code> 命令。PostgreSQL 18 还通过 <a href="https://www.postgresql.org/docs/18/functions-uuid.html#FUNC_UUID_GEN_TABLE"><code>uuidv7()</code></a> 新增 UUIDv7 生成能力，可生成按时间戳排序的随机 UUID，以支持更好的缓存策略。与此同时，<a href="https://www.postgresql.org/docs/18/functions-uuid.html#FUNC_UUID_GEN_TABLE"><code>uuidv4()</code></a> 也已作为 <code>gen_random_uuid()</code> 的别名提供。</p>
 <p>PostgreSQL 18 还增加了<a href="https://www.postgresql.org/docs/18/sql-createtable.html#SQL-CREATETABLE-PARMS-UNIQUE">时态约束</a>支持。借助 <code>WITHOUT OVERLAPS</code> 子句，可在 <code>PRIMARY KEY</code> 与 <code>UNIQUE</code> 约束中对范围建立不重叠约束；借助 <code>PERIOD</code> 子句，则可在 <code>FOREIGN KEY</code> 约束上定义时间约束关系。</p>
 <p>最后，PostgreSQL 18 通过 <a href="https://www.postgresql.org/docs/18/sql-createforeigntable.html"><code>CREATE FOREIGN TABLE ... LIKE</code></a> 命令，让用户能够基于本地表定义，更方便地创建外部表的模式结构。</p>
 
--- a/templates/pages/about/press/presskit90/zh_cn.html
+++ b/templates/pages/about/press/presskit90/zh_cn.html
@@ -11,7 +11,7 @@
  &nbsp; <a href="#additional_features">特性列表</a><br />
  &nbsp; <a href="#download">下载地址</a><br />
  &nbsp; <a href="#docs">文档</a><br />
- &nbsp; <a href="#license">版权声明</a><br />
+ &nbsp; <a href="#license">许可证</a><br />
  &nbsp; <a href="#contact">联系方式</a><br />
  &nbsp; <a href="#quoted_companies">相关商业公司信息</a><br />
  &nbsp; <a href="#companies">企业支持</a><br />
@@ -56,7 +56,7 @@
 </h3>
 <ul>
 <li>集成自动升级功能（pg_upgrade）</li>
-<li>RADIUS 授权</li>
+<li>RADIUS 认证</li>
 <li>密码强度检查（passwordcheck）</li>
 <li>更方便的数据库权限管理指令（GRANT ON ALL and DEFAULT PERMISSIONS）</li>
 </ul>
@@ -121,9 +121,9 @@
 
 <p>HTML 文档和 man 手册页会和 PostgreSQL 同步安装，用户也可以免费地通过在线方式对<a href="https://www.postgresql.org/docs/9.0/">在线文档</a>进行浏览、搜索、回复意见。</p>
 
-<a name="license"></a><h2>版权声明</h2>
-<p>
-PostgreSQL 使用 <a href="/about/licence">PostgreSQL 版权</a>，它是类似 BSD 的版权，只要求对有版权的源代码保留其版权和授权的信息即可。由于这个经 <a href="http://www.opensource.org/licenses/postgresql">OSI 认证的版权</a>不限制 PostgreSQL 在商业环境和有版权的应用程序中使用，因此被公认为是非常有灵活性和对商业应用是友好的。加上有多个公司的支持和源代码归属公共所有，我们的版权使得 PostgreSQL 在那些希望在自己的产品里嵌入数据库的厂商中很流行，因为他们不用担心费用、嵌入软件的版权锁以及版权条款的改变。
+<a name="license"></a><h2>许可证</h2>
+<p>
+PostgreSQL 使用 <a href="/about/licence">PostgreSQL 许可证</a>，它是类似 BSD 的许可证，只要求对获许可的源代码保留其版权和许可信息即可。由于这个经 <a href="http://www.opensource.org/licenses/postgresql">OSI 认证的许可证</a>不限制 PostgreSQL 在商业环境和专有应用程序中使用，因此被公认为是非常有灵活性和对商业应用是友好的。加上有多个公司的支持和源代码归属公共所有，这一许可证使得 PostgreSQL 在那些希望在自己的产品里嵌入数据库的厂商中很流行，因为他们不用担心费用、供应商锁定以及许可条款的改变。
 </p>
 
 <a name="contact"></a><h2>联系方式</h2>
--- a/templates/pages/about/press/presskit91/zh_cn.html
+++ b/templates/pages/about/press/presskit91/zh_cn.html
@@ -12,7 +12,7 @@
  &nbsp; <a href="#anniversary">25 年的数据库创新</a><br />
  &nbsp; <a href="#download">下载地址</a><br />
  &nbsp; <a href="#docs">文档</a><br />
- &nbsp; <a href="#license">版权声明</a><br />
+ &nbsp; <a href="#license">许可证</a><br />
  &nbsp; <a href="#contact">联系方式</a><br />
  &nbsp; <a href="#quoted_companies">相关商业公司信息</a><br />
  &nbsp; <a href="#companies">企业支持</a><br />
@@ -20,7 +20,7 @@
 
 <a name="original_release"></a><h2>新版本发布说明的原文</h2>
 <p>
-<b>2011/09/12:</b> PostgreSQL 全球开发组宣布业界领先的开源关系数据库 PostgreSQL 9.1 版发布，该版本增加了很多创新性的技术、强大的可扩展性以及类似同步复制、最近相邻索引和外部数据封装等功能。
+<b>2011/09/12:</b> PostgreSQL 全球开发组宣布业界领先的开源关系数据库 PostgreSQL 9.1 版发布，该版本增加了很多创新性的技术、强大的可扩展性以及类似同步复制、最近邻索引和外部数据包装器等功能。
 </p><p>
 “PostgreSQL 9.1 版本提供了开源数据库最为高端的企业级功能，它的背后是一个充满生气和创新能力的、是经证明可以引导客户走向成功的社区。PostgreSQL 也在创建和运行云计算方面做好了准备，“VMware 公司的研发副总裁 Charles Fan 说道。
 </p>
@@ -35,16 +35,16 @@
 </ul><p>
 “Heroku 运行着一个世界上最大的 PostgreSQL 数据库”，Heroku 公司的创始人之一，James Lindenbaum 先生说道，”9.1 版本提供的同步复制技术给我们客户带来了创新的方法来保护关键事务数据，并且也验证了 PostgreSQL 在快速增长的数据仓库方面应用的能力。“
 </p>
-<h3>（艺术级）的高级功能</h3>
+<h3>前沿特性</h3>
 <p>
 社区的开发人员创新地开发了几项最前沿的功能，9.1 版本包括以下几项数据库业界全新的功能，如：
 </p><ul>
-<li><b>最近相邻索引技术</b>: 索引是按照”距离“来达到在查询中快速定位和文字搜索。</li>
-<li><b>可串行化的快照隔离</b>: 使用”真正的可串行化“，确保并行事务的一致性。</li>
+<li><b>最近邻索引技术</b>: 索引是按照”距离“来达到在查询中快速定位和文字搜索。</li>
+<li><b>可串行化快照隔离</b>: 使用”真正的可串行化“，确保并行事务的一致性。</li>
 <li><b>可更新的 CTE 功能</b>: 在一个查询中执行复杂的多阶段数据更新。</li>
 <li><b>安全增强型版本</b>: 发布军用级安全和强制访问控制的版本。</li>
 </ul><p>
-”OpenERP 软件依赖于具有企业级特性的 PostgreSQL，来为我们用户的每天操作实现快速、安全和可伸缩性的商业应用。在高并发和复杂事务条件下的数据集成性对我们来说是关键，我们对新的 PostgreSQL 9.1 版本中可串行化快照隔离技术非常感兴趣！“OpenERP 社区经理 Olivier Dony 说道。
+”OpenERP 软件依赖于具有企业级特性的 PostgreSQL，来为我们用户的每天操作实现快速、安全和可伸缩性的商业应用。在高并发和复杂事务条件下的数据完整性对我们来说是关键，我们对新的 PostgreSQL 9.1 版本中可串行化快照隔离技术非常感兴趣！“OpenERP 社区经理 Olivier Dony 说道。
 </p>
 <h3>可扩展的数据库引擎</h3>
 <p>
@@ -74,12 +74,12 @@
 <dt><b>支持多语言数据库的字段级排序规则:</b></dt><dd> 用户现在可以为单个字段上的字符串设置排序规则。这使得真正的多语言数据库成为可能，每个文本字段都可以对应不同语言，并按照该语言正确建立索引和排序。</dd>
 <dt><b>高性能的不记录日志表:</b></dt><dd> 当性能比持久性更重要时，不记录日志表提供了一种在 PostgreSQL 中管理数据并提升性能的方式。去除日志记录可降低 I/O 开销，与记录日志的数据表相比，性能最高可提升 10 倍。适合使用不记录日志表的场景包括 Web 会话数据、实时日志、ETL，以及函数使用的临时/中间表。</dd>
 </dl>
-<h3>（艺术级）的高级功能</h3>
+<h3>前沿特性</h3>
 <p>
 我们社区的开发人员创新地开发了几项最前沿的功能，9.1 版本包括以下几项数据库业界全新的功能，如：
 </p><dl>
 <dt><b>强大的相似性搜索（KNN 索引）:</b></dt><dd> 使用 PostgreSQL KNN-Gist 索引创建一个新的应用可以避免代价高昂的数据表扫描。通过使用数学上的“距离”计算用于索引和搜索，KNN 技术增强了 PostgreSQL 的查询能力。这些索引可用于增强一般的文字搜索、文本相似性搜索、地理空间位置比较和其他查询。在一个无索引的表搜索中，不使用 KNN 功能搜索需花 2 秒，而使用后仅需 20ms。文字搜索经过配置在不改变任何 SQL 语句的情况下，也可以支持类似 LIKE '%string%'样的查询。PostgreSQL 是业界内第一个使用 KNN 技术的数据库系统。</dd>
-<dt><b>可串行化的快照隔离:</b></dt><dd> 通过在事务运行时自动侦测已使用的冲突条件来允许用户强制多次地执行复杂的用户自定义的商业规则而不会有阻隔。这个功能目前也是仅 PostgreSQL 具有。</dd>
+<dt><b>可串行化快照隔离:</b></dt><dd> 通过在事务运行时自动侦测已使用的冲突条件来允许用户强制多次地执行复杂的用户自定义的商业规则而不会有阻隔。这个功能目前也是仅 PostgreSQL 具有。</dd>
 <dt><b>可更新的 CTE 功能 (wCTE):</b></dt><dd> 支持用户在一个语句内执行多个、级联、相关记录的更新。通过使用一个查询的结果去执行另一个查询，用户可以递归地、分级地、跨外键地或是更加创新的方式来更新数据。PostgreSQL 为这项 SQL 特性提供了一个最为完整和有灵活性解决方案。</dd>
 <dt><b>安全增强型版本 (SE-Postgres):</b></dt><dd> 通过与 SE-Linux 的全集成，发布军用级安全和强制访问控制的版本。SE-Linux 是通过修改 Linux 核心来强化安全相关功能的 NSA 项目。它现已支持主要的 Linux 发布版本，包括但不限于 Red Hat, CentOS, Debian 和 Ubuntu。PostgreSQL 是唯一与 SE-Linux 集成的数据库系统。</dd>
 </dl>
@@ -113,7 +113,7 @@
 <a name="download"></a><h2>下载</h2>
 <ul>
 <li><a href="/download">下载页面</a>包括安装程序和一些工具软件</li>
-<li><a href="https://www.postgresql.org/ftp/source/v9.1.0">原代码</a></li>
+<li><a href="https://www.postgresql.org/ftp/source/v9.1.0">源代码</a></li>
 <li><a href="/download/windows">Windows 平台的安装程序</a></li>
 <li><a href="/download/linux">Linux 平台的安装程序</a>和一些软件包的版本库</li>
 <li><a href="/download/solaris">Solaris 平台安装程序</a></li>
@@ -132,9 +132,9 @@
 <p>HTML 文档和 man 手册页会和 PostgreSQL 同步安装，用户也可以免费地通过在线方式对<a href="https://www.postgresql.org/docs/9.1/">在线文档</a>进行浏览、搜索、留言。</p>
 
 
-<a name="license"></a><h2>版权声明</h2>
+<a name="license"></a><h2>许可证</h2>
 <p>
-PostgreSQL 使用 <a href="/about/licence">PostgreSQL 版权</a>，它是类似 BSD 的版权，只要求对有版权的源代码保留其版权和授权的信息即可。由于这个经 <a href="http://www.opensource.org/licenses/postgresql">OSI 认证的版权</a>不限制 PostgreSQL 在商业环境和有版权的应用程序中使用，因此被公认为是非常有灵活性和对商业应用是友好的。加上有多个公司的支持和源代码归属公共所有，我们的版权使得 PostgreSQL 在那些希望在自己的产品里嵌入数据库的厂商中很流行，因为他们不用担心费用、嵌入软件的版权锁以及版权条款的改变。
+PostgreSQL 使用 <a href="/about/licence">PostgreSQL 许可证</a>，它是类似 BSD 的许可证，只要求对获许可的源代码保留其版权和许可信息即可。由于这个经 <a href="http://www.opensource.org/licenses/postgresql">OSI 认证的许可证</a>不限制 PostgreSQL 在商业环境和专有应用程序中使用，因此被公认为是非常有灵活性和对商业应用是友好的。加上有多个公司的支持和源代码归属公共所有，这一许可证使得 PostgreSQL 在那些希望在自己的产品里嵌入数据库的厂商中很流行，因为他们不用担心费用、供应商锁定以及许可条款的改变。
 </p>
 
 
@@ -171,10 +171,10 @@
 
 “Heroku 运行着一个世界上最大的 PostgreSQL 数据库”，Heroku 公司的创始人之一，James Lindenbaum 先生说道，”9.1 版本提供的同步复制技术给我们客户带来了创新的方法来保护关键事务数据，并且也验证了 PostgreSQL 在快速增长的数据仓库方面应用的能力。“
 </p><p>
-<b>关于 Heroku 公司:</b> <a href="http://www.heroku.com">Heroku</a> 公司是一家致力于提供易用、自动化、可靠性高的开放平台的服务商，总部位于加州的三藩市。Heroku 公司是 Ruby 技术和云计算架构的积极支持者。它最近被选为“道琼斯 FASTech 咨询委员会 50 家初创公司观察"表之一，2010 年 4 月被高德纳咨询公司列为”最棒的应用平台服务商“, 被 onDemand 公司列为最好的 100 私人公司，被 ReadWriteWeb 网站列为”2009 最佳产品“和”2010 最佳平台“。2011/04，Heroku 公司被三藩市的商业周刊列为海湾区”最佳工作地点“第 2 名。更多信息可浏览<a href="http://www.heroku.com">网站</a>和<a href="http://blog.heroku.com">博客</a>或是 <a href="http://twitter.com/heroku">Twitter</a>。Heroku 公司完全拥有 salesforce.com 网站，联系方式：<a href="mailto:jill@blackswansmedia.com">Jill Ratkevic</a>
+<b>关于 Heroku 公司:</b> <a href="http://www.heroku.com">Heroku</a> 公司是一家致力于提供易用、自动化、可靠性高的开放平台的服务商，总部位于加州的三藩市。Heroku 公司是 Ruby 技术和云计算架构的积极支持者。它最近被选为“道琼斯 FASTech 咨询委员会 50 家初创公司观察"表之一，2010 年 4 月被高德纳咨询公司列为”最棒的应用平台服务商“, 被 onDemand 公司列为最好的 100 私人公司，被 ReadWriteWeb 网站列为”2009 最佳产品“和”2010 最佳平台“。2011/04，Heroku 公司被三藩市的商业周刊列为海湾区”最佳工作地点“第 2 名。更多信息可浏览<a href="http://www.heroku.com">网站</a>和<a href="http://blog.heroku.com">博客</a>或是 <a href="http://twitter.com/heroku">Twitter</a>。Heroku 是 salesforce.com 的全资子公司，联系方式：<a href="mailto:jill@blackswansmedia.com">Jill Ratkevic</a>
 </p><p>
 
-”OpenERP 软件依赖于具有企业级特性的 PostgreSQL，来为我们用户的每天操作实现快速、安全和可伸缩性的商业应用。在高并发和复杂事务条件下的数据集成性对我们来说是关键，我们对新的 PostgreSQL 9.1 版本中可串行化快照隔离技术非常感兴趣！同步复制技术和性能优化是两个我们期待新版本 PostgreSQL 的主要原因，PostgreSQL 是开源软件的典型代表。“OpenERP 社区经理 Olivier Dony 说道。
+”OpenERP 软件依赖于具有企业级特性的 PostgreSQL，来为我们用户的每天操作实现快速、安全和可伸缩性的商业应用。在高并发和复杂事务条件下的数据完整性对我们来说是关键，我们对新的 PostgreSQL 9.1 版本中可串行化快照隔离技术非常感兴趣！同步复制技术和性能优化是两个我们期待新版本 PostgreSQL 的主要原因，PostgreSQL 是开源软件的典型代表。“OpenERP 社区经理 Olivier Dony 说道。
 </p><p>
 <b>关于 OpenERP:</b> <a href="http://www.openerp.com">OpenERP S.A.</a> 是 OpenERP 的开发公司，OpenERP 是一个综合的开源商业应用套件，包括 CRM、销售、财务、项目管理、仓库管理、制造、人力资源及更多功能。2005 年由现 CEO Fabien Pinckaers 创建，OpenERP 形成了社区、合作者、开发者一个完整唯一的生态链。社区（2011/08 统计有 1500 名活跃成员）每天都在向 OpenERP 贡献新的活力。遍布于 65 个国家的网络上的合作者可以在本地交付系统。OpenERP 也提供在线服务（SAAS），它是世界上安装最多的商业套件（每天多达 1000 次下载）。联系方式：<a href="mailto:press@openerp.com">Nicoleta Gherlea</a> +32-2-290-34-90
 </p>
--- a/templates/pages/about/press/presskit92/zh_CN.html
+++ b/templates/pages/about/press/presskit92/zh_CN.html
@@ -11,7 +11,7 @@
  &nbsp; <a href="#features">特性列表</a><br />
  &nbsp; <a href="#download">下载地址</a><br />
  &nbsp; <a href="#docs">文档</a><br />
- &nbsp; <a href="#license">版权声明</a><br />
+ &nbsp; <a href="#license">许可证</a><br />
  &nbsp; <a href="#contact">联系方式</a><br />
  &nbsp; <a href="#quoted_companies">相关商业公司信息</a><br />
  &nbsp; <a href="#companies">企业支持</a><br />
@@ -20,22 +20,22 @@
 <a name="original_release"></a><h2>新版本发布说明的原文</h2>
 
 <p>
-2012/09/10: PostgreSQL 全球开发组宣布业界领先的开源关系数据库 PostgreSQL 9.2 版发布，由于 Beta 测试版本在 5 月即已发布，开发人员和软件厂商称赞该版本在性能、可扩展性和灵活性方面得到大的提升。大量的用户期待切换至新的版本。
+2012/09/10: PostgreSQL 全球开发组宣布业界领先的开源关系数据库 PostgreSQL 9.2 版发布，由于 Beta 测试版本在 5 月即已发布，开发人员和软件厂商称赞该版本在性能、可伸缩性和灵活性方面得到大的提升。大量的用户期待切换至新的版本。
 </p>
 
 <p>
 “PostgreSQL 9.2 版将提供原生 JSON 支持、覆盖索引、复制和性能改进等众多特性。我们热切期待这一版本发布，并会在 PostgreSQL 社区发布后立即以 Early Access 形式向用户提供。”EngineYard 公司的高级数据管理工程师 Ines Sombra 说道。
 </p>
 
-<h3>性能提升和可扩展性</h3>
+<h3>性能提升和可伸缩性</h3>
 
 <p>
-垂直可扩展性的提升增加了 PostgreSQL 在大型服务器上有效利用硬件资源的能力，高级锁的管理、写性能、索引扫描以及其他对硬件的底层操作允许 PostgreSQL 处理海量负载，从数据上与 PostgreSQL 9.1 版本对比：
+垂直可伸缩性的提升增加了 PostgreSQL 在大型服务器上有效利用硬件资源的能力，高级锁的管理、写性能、索引扫描以及其他对硬件的底层操作允许 PostgreSQL 处理海量负载，从数据上与 PostgreSQL 9.1 版本对比：
 </p>
 
 <ul>
 <li>最高可达每秒 350,000 次读查询 (相比以前快 4 倍)</li>
-<li>数据仓库中只使用索引扫描的查询（最高可比以前快 20 倍）</li>
+<li>数据仓库中使用仅索引扫描的查询（最高可比以前快 20 倍）</li>
 <li>最高可达每秒 14,000 次数据写操作（相比以前快 5 倍）</li>
 </ul>
 <p>
@@ -99,7 +99,7 @@
 
 <a name="license"></a><h2>许可证</h2>
 <p>
-PostgreSQL 使用 <a href="/about/licence">PostgreSQL 版权声明</a>，它是类似 BSD 的版权，只要求对有版权的源代码保留其版权和授权的信息即可。由于这个经 <a href="http://www.opensource.org/licenses/postgresql">OSI 认证的版权</a>不限制 PostgreSQL 在商业环境和有版权的应用程序中使用，因此被公认为是非常有灵活性和对商业应用是友好的。加上有多个公司的支持和源代码归属公共所有，我们的版权使得 PostgreSQL 在那些希望在自己的产品里嵌入数据库的厂商中很流行，因为他们不用担心费用、嵌入软件的版权锁以及版权条款的改变。
+PostgreSQL 使用 <a href="/about/licence">PostgreSQL 许可证</a>，它是类似 BSD 的许可证，只要求对获许可的源代码保留其版权和许可信息即可。由于这个经 <a href="http://www.opensource.org/licenses/postgresql">OSI 认证的许可证</a>不限制 PostgreSQL 在商业环境和专有应用程序中使用，因此被公认为是非常有灵活性和对商业应用是友好的。加上有多个公司的支持和源代码归属公共所有，这一许可证使得 PostgreSQL 在那些希望在自己的产品里嵌入数据库的厂商中很流行，因为他们不用担心费用、供应商锁定以及许可条款的改变。
 </p>
 
 <a name="contact"></a><h2>联系方式</h2>
--- a/templates/pages/about/press/presskit93/zh_CN.html
+++ b/templates/pages/about/press/presskit93/zh_CN.html
@@ -1,8 +1,8 @@
 {%extends "base/page.html"%}
-{%block title%}PostgreSQL 9.3 版本发布说明{%endblock%}
+{%block title%}PostgreSQL 9.3 新闻资料包{%endblock%}
 {%block contents%}
 
-<h1>PostgreSQL 9.3 版本发布说明</h1>
+<h1>PostgreSQL 9.3 新闻资料包</h1>
 
 <h2>目录</h2>
 
@@ -11,7 +11,7 @@
  &nbsp; <a href="#features">特性列表</a><br />
  &nbsp; <a href="#download">下载地址</a><br />
  &nbsp; <a href="#docs">文档</a><br />
- &nbsp; <a href="#license">版权声明</a><br />
+ &nbsp; <a href="#license">许可证</a><br />
  &nbsp; <a href="#contact">联系方式</a><br />
  &nbsp; <a href="#quoted_companies">相关商业公司引语与全文</a><br />
  &nbsp; <a href="#companies">企业支持</a><br />
@@ -43,7 +43,7 @@
 </p>
 
 <p>
-“PostgreSQL 因为它众所周知的稳定性、强壮性、数据一致性、安全性、ACID 事务性和 SQL 标准规范性一直就是我的最喜欢的选择”，Gandi.net 网站的研发经理 Pascal Bouchareine 说道，“我对 9.3 版本中快速的失效转移功能尤其感到兴奋”。
+“PostgreSQL 因为它众所周知的稳定性、强壮性、数据一致性、安全性、ACID 事务性和 SQL 标准规范性一直就是我的最喜欢的选择”，Gandi.net 网站的研发经理 Pascal Bouchareine 说道，“我对 9.3 版本中快速的故障切换功能尤其感到兴奋”。
 </p>
 
 <p>
@@ -52,7 +52,7 @@
 
 <ul>
 <li>数据页的校验：帮助管理员快速检查失效磁盘和可能导致数据出错的有问题硬件</li>
-<li>快速失效转移：可以实现亚秒级的从主库向副本的切换，支持“电信级”高可用性</li>
+<li>快速故障切换：可以实现亚秒级的从主库向副本的切换，支持“电信级”高可用性</li>
 <li>流式重建：在主机失效后可以更加简单、快速地重新配置级联复制</li>
 </ul>
 
@@ -113,9 +113,9 @@
 HTML 格式文档和 man 格式的文档随 PostgreSQL 一起安装，用户也可以免费地在网站上对<a href="https://www.postgresql.org/docs/9.3/">在线文档</a>进行浏览、搜索和反馈评论。
 </p>
 
-<a name="license"></a><h2>版权声明</h2>
+<a name="license"></a><h2>许可证</h2>
 <p>
-PostgreSQL 使用 <a href="/about/licence">PostgreSQL 版权声明</a>，它是类似 BSD 的版权。由于这个经 <a href="http://www.opensource.org/licenses/postgresql">OSI 认证的版权</a>不限制 PostgreSQL 在商业环境和有版权的应用程序中使用，因此被公认为是非常有灵活性和对商业应用是友好的。加上有多个公司的支持和源代码归属公共所有，我们的版权使得 PostgreSQL 在那些希望在自己的产品里嵌入数据库的厂商中很流行，因为他们不用担心费用、嵌入软件的版权锁以及版权条款的改变。
+PostgreSQL 使用 <a href="/about/licence">PostgreSQL 许可证</a>，它是类似 BSD 的许可证。由于这个经 <a href="http://www.opensource.org/licenses/postgresql">OSI 认证的许可证</a>不限制 PostgreSQL 在商业环境和专有应用程序中使用，因此被公认为是非常有灵活性和对商业应用是友好的。加上有多个公司的支持和源代码归属公共所有，这一许可证使得 PostgreSQL 在那些希望在自己的产品里嵌入数据库的厂商中很流行，因为他们不用担心费用、供应商锁定以及许可条款的改变。
 </p>
 
 <a name="contact"></a><h2>联系方式</h2>
@@ -160,7 +160,7 @@
 
 <b>Gandi.net 公司</b>
 
-<blockquote>"我们需要使用复制技术，对最近这项特性的改进非常感兴趣，我对 9.3 版本中的快速失效转移功能尤其兴奋。我们在 Gandi IAAS/PAAS 平台上已经使用 PostgreSQL 很长时间，最近我们也使用它创建我们的在线系统每天来存储、计算、输出数百万条记录，真的是很简单"。
+<blockquote>"我们需要使用复制技术，对最近这项特性的改进非常感兴趣，我对 9.3 版本中的快速故障切换功能尤其兴奋。我们在 Gandi IAAS/PAAS 平台上已经使用 PostgreSQL 很长时间，最近我们也使用它创建我们的在线系统每天来存储、计算、输出数百万条记录，真的是很简单"。
 <br /><br />
 "PostgreSQL 因为它众所周知的稳定性、强壮性、数据一致性、安全性、ACID 事务性和 SQL 标准规范性一直就是我的最喜欢的选择，现在它变得更加现代、充分使用了操作系统提供的功能，我们可见到很大的性能提升和失效时更加容易的管理。它就是适用于关键业务的最好的数据库"，Gandi.net 网站的研发经理 Pascal Bouchareine 说。
 </blockquote>
--- a/templates/pages/developer/backend.html
+++ b/templates/pages/developer/backend.html
@@ -44,9 +44,9 @@
 <p>查询通过 TCP/IP 或 Unix Domain 套接字以数据包的形式到达后端。它被加载到一个字符串中，然后传递给<a
 href="https://wiki.postgresql.org/wiki/Backend_flowchart#parser">解析器，</a>其中词法扫描器 <a
 href="https://git.postgresql.org/gitweb/?p=postgresql.git;a=blob;f=src/backend/parser/scan.l">scan.l</a>
-将查询分解为记号（token）。解析器使用 <a
+将查询分解为词元（token）。解析器使用 <a
 href="https://git.postgresql.org/gitweb/?p=postgresql.git;a=blob;f=src/backend/parser/gram.y">gram.y</a>
-和这些记号来识别查询类型，并加载相应的查询特定结构，如 <a
+和这些词元来识别查询类型，并加载相应的查询特定结构，如 <a
 href="https://git.postgresql.org/gitweb/?p=postgresql.git;a=blob;f=src/include/nodes/parsenodes.h">CreateStmt</a>
 或 <a
 href="https://git.postgresql.org/gitweb/?p=postgresql.git;a=blob;f=src/include/nodes/parsenodes.h">SelectStmt。</a></p>
@@ -69,7 +69,7 @@
 条目，它们链接在一起组成查询的<em>目标列表</em>。目标列表存储在 Query.targetList 中，由 <a
 href="https://git.postgresql.org/gitweb/?p=postgresql.git;a=blob;f=src/backend/parser/parse_target.c">transformTargetList()</a> 生成。</p>
 
-<p>其他查询元素，如聚集（<em>SUM()</em>）、<em>GROUP
+<p>其他查询元素，如聚合（<em>SUM()</em>）、<em>GROUP
 BY</em> 和 <em>ORDER BY</em> 也存储在各自的 Query 字段中。</p>
 
 <p>下一步是由可能适用于查询的任何<em>视图</em>或<em>规则</em>对 Query 进行修改。这由<a href="https://wiki.postgresql.org/wiki/Backend_flowchart#rewrite">重写</a>系统执行。</p>
--- a/templates/pages/docs/faq.html
+++ b/templates/pages/docs/faq.html
@@ -4,10 +4,10 @@
 
 <h1>常见问题 <i class="fa fa-question-circle"></i></h1>
 
-<h2>新闻常见问题</h2>
+<h2>媒体常见问题</h2>
 
 <p>
-  关于我们收到的许多 PostgreSQL 问题的<a href="/about/press/faq/">概要介绍</a>，请查阅我们的<a href="/about/press/faq/">新闻常见问题</a>。
+  关于我们收到的许多 PostgreSQL 问题的<a href="/about/press/faq/">概要介绍</a>，请查阅我们的<a href="/about/press/faq/">媒体常见问题</a>。
 </p>
 
 <h2>通用常见问题</h2>
--- a/templates/pages/download/linux/debian.html
+++ b/templates/pages/download/linux/debian.html
@@ -7,7 +7,7 @@
 
 <h1>Linux 下载（Debian） <img class="logo" src="/media/img/debian.svg" alt="Debian 标志" /></h1>
 <p>
-所有 Debian 版本默认都包含 PostgreSQL。但是，Debian 会“快照”一个特定版本的 PostgreSQL，该版本在 Debian 版本的整个生命周期内受到支持。PostgreSQL 项目维护了一个 <a href="#apt">Apt 仓库</a>，提供所有受支持的 PostgreSQL 版本。
+所有 Debian 版本默认都包含 PostgreSQL。但是，Debian 会选定一个 PostgreSQL 版本，该版本在 Debian 版本的整个生命周期内受到支持。PostgreSQL 项目维护了一个 <a href="#apt">Apt 仓库</a>，提供所有受支持的 PostgreSQL 版本。
 </p>
 
 <h2>发行版自带</h2>
--- a/templates/pages/download/linux/redhat.html
+++ b/templates/pages/download/linux/redhat.html
@@ -19,18 +19,17 @@
 </p>
 <p>以及其他。</p>
 <p>
-这些平台默认提供 PostgreSQL。但是，每个版本的平台通常会“快照”一个特定版本的
-PostgreSQL，并在该平台的整个生命周期内提供支持。由于这通常意味着提供的版本并非用户所需，PostgreSQL
+这些平台默认提供 PostgreSQL。但是，各发行版版本通常会选定一个 PostgreSQL 版本，并在该平台的整个生命周期内提供支持。由于这通常意味着提供的版本并非用户所需，PostgreSQL
 项目为最常见的发行版提供了一个包含所有受支持版本的<a href="#yum">软件包仓库</a>。
 </p>
 
 <a name="yum"></a>
-<h2>PostgreSQL Yum Repository</h2>
+<h2>PostgreSQL YUM 仓库</h2>
 <p>
-<a href="https://yum.postgresql.org" target="_blank" rel="noopener">PostgreSQL Yum Repository</a> 将与您的常规系统和补丁管理集成，并在 PostgreSQL 的整个支持<a href="/support/versioning/">生命周期</a>内为所有受支持的 PostgreSQL 版本提供自动更新。
+<a href="https://yum.postgresql.org" target="_blank" rel="noopener">PostgreSQL YUM 仓库</a> 将与您的常规系统和补丁管理集成，并在 PostgreSQL 的整个支持<a href="/support/versioning/">生命周期</a>内为所有受支持的 PostgreSQL 版本提供自动更新。
 </p>
 <p>
-PostgreSQL Yum Repository 目前支持：
+PostgreSQL YUM 仓库 目前支持：
 </p>
 <p>
   <ul>
@@ -43,12 +42,12 @@
 <p><strong><sup>*</sup>注意</strong>：由于 Fedora 的支持周期较短，并非所有受支持的 PostgreSQL 版本都一定能在该平台上使用。我们不建议将 Fedora 用于服务器部署。
 </p>
 <p>
-PostgreSQL Yum Repository 最近开始<a href="https://yum.postgresql.org/news/postgresql-rpms-multiple-rhel-minor-versions/" target="blank">支持
+PostgreSQL YUM 仓库 最近开始<a href="https://yum.postgresql.org/news/postgresql-rpms-multiple-rhel-minor-versions/" target="blank">支持
 RHEL 及其衍生发行版的最近两个小版本</a>，例如 RHEL 10.0
 和 10.1。
 </p>
 <p>
-要使用 PostgreSQL Yum Repository，请按以下步骤操作：
+要使用 PostgreSQL YUM 仓库，请按以下步骤操作：
 </p>
 <ol>
   <li>选择平台：<select id="platform" class="custom-select"></select></li>
--- a/templates/pages/download/linux/suse.html
+++ b/templates/pages/download/linux/suse.html
@@ -4,9 +4,9 @@
 
 <h1>Linux 下载（SUSE） <img class="logo" src="/media/img/suse.svg" alt="SUSE Logo" /></h1>
 <p>
-所有 SUSE 版本默认都提供 PostgreSQL。但是，SUSE Linux 会“快照”一个特定版本的 PostgreSQL，并在该 SUSE 版本的整个生命周期内提供支持。
+所有 SUSE 版本默认都提供 PostgreSQL。但是，SUSE Linux 会选定一个 PostgreSQL 版本，并在该 SUSE 版本的整个生命周期内提供支持。
 </p>
-<a name="zypper"></a><h2>PostgreSQL RPM Repository</h2>
+<a name="zypper"></a><h2>PostgreSQL RPM 仓库</h2>
 <p>
 该仓库可与常规系统及补丁管理工具集成，并在 PostgreSQL 的整个支持<a href="/support/versioning/">生命周期</a>内为 PostgreSQL 14 及以上版本提供自动更新。
 </p>
--- a/templates/pages/download/linux/ubuntu.html
+++ b/templates/pages/download/linux/ubuntu.html
@@ -7,7 +7,7 @@
 
 <h1>Linux 下载（Ubuntu） <img class="logo" src="/media/img/ubuntu.svg" alt="Ubuntu 标志" /></h1>
 <p>
-所有 Ubuntu 版本默认都提供 PostgreSQL。但是，Ubuntu 会“快照”一个特定版本的 PostgreSQL，并在该 Ubuntu 版本的整个生命周期内提供支持。PostgreSQL 项目维护了一个 <a href="#apt">Apt 仓库</a>，提供所有受支持的 PostgreSQL 版本。
+所有 Ubuntu 版本默认都提供 PostgreSQL。但是，Ubuntu 会选定一个 PostgreSQL 版本，并在该 Ubuntu 版本的整个生命周期内提供支持。PostgreSQL 项目维护了一个 <a href="#apt">Apt 仓库</a>，提供所有受支持的 PostgreSQL 版本。
 </p>
 
 <h2>发行版自带</h2>
--- a/templates/profserv/list.html
+++ b/templates/profserv/list.html
@@ -2,7 +2,7 @@
 {%block title%}{{title}}{%endblock%}
 {%block contents%}
 <h1>{{title}} <i class="fa fa-question-circle"></i></h1>
-<p>以下{{whatname}}在{{regionname}}注册。</p>
+<p>以下公司在{{regionname}}提供{{whatname}}。</p>
 
 {% for s in services %}
   <h2 class="news">
